{
  "name": "solace-panel",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Lighting Engine panel for the Solace Home Assistant integration",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "lit": "^3.2.1"
  }
}
