/* Solace lighting panel — generated by panel/build.mjs. Do not edit by hand. */
var wt=Object.defineProperty;var _t=Object.getOwnPropertyDescriptor;var d=(n,s,e,t)=>{for(var a=t>1?void 0:t?_t(s,e):s,i=n.length-1,r;i>=0;i--)(r=n[i])&&(a=(t?r(s,e,a):r(a))||a);return t&&a&&wt(s,e,a),a};var xe=globalThis,$e=xe.ShadowRoot&&(xe.ShadyCSS===void 0||xe.ShadyCSS.nativeShadow)&&"adoptedStyleSheets"in Document.prototype&&"replace"in CSSStyleSheet.prototype,Re=Symbol(),Ze=new WeakMap,oe=class{constructor(s,e,t){if(this._$cssResult$=!0,t!==Re)throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");this.cssText=s,this.t=e}get styleSheet(){let s=this.o,e=this.t;if($e&&s===void 0){let t=e!==void 0&&e.length===1;t&&(s=Ze.get(e)),s===void 0&&((this.o=s=new CSSStyleSheet).replaceSync(this.cssText),t&&Ze.set(e,s))}return s}toString(){return this.cssText}},Ve=n=>new oe(typeof n=="string"?n:n+"",void 0,Re),y=(n,...s)=>{let e=n.length===1?n[0]:s.reduce((t,a,i)=>t+(r=>{if(r._$cssResult$===!0)return r.cssText;if(typeof r=="number")return r;throw Error("Value passed to 'css' function must be a 'css' function result: "+r+". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.")})(a)+n[i+1],n[0]);return new oe(e,n,Re)},Ge=(n,s)=>{if($e)n.adoptedStyleSheets=s.map(e=>e instanceof CSSStyleSheet?e:e.styleSheet);else for(let e of s){let t=document.createElement("style"),a=xe.litNonce;a!==void 0&&t.setAttribute("nonce",a),t.textContent=e.cssText,n.appendChild(t)}},Ce=$e?n=>n:n=>n instanceof CSSStyleSheet?(s=>{let e="";for(let t of s.cssRules)e+=t.cssText;return Ve(e)})(n):n;var{is:kt,defineProperty:At,getOwnPropertyDescriptor:Mt,getOwnPropertyNames:Et,getOwnPropertySymbols:Tt,getPrototypeOf:zt}=Object,ye=globalThis,Je=ye.trustedTypes,Rt=Je?Je.emptyScript:"",Ct=ye.reactiveElementPolyfillSupport,le=(n,s)=>n,ce={toAttribute(n,s){switch(s){case Boolean:n=n?Rt:null;break;case Object:case Array:n=n==null?n:JSON.stringify(n)}return n},fromAttribute(n,s){let e=n;switch(s){case Boolean:e=n!==null;break;case Number:e=n===null?null:Number(n);break;case Object:case Array:try{e=JSON.parse(n)}catch{e=null}}return e}},we=(n,s)=>!kt(n,s),Ye={attribute:!0,type:String,converter:ce,reflect:!1,useDefault:!1,hasChanged:we};Symbol.metadata??=Symbol("metadata"),ye.litPropertyMetadata??=new WeakMap;var N=class extends HTMLElement{static addInitializer(s){this._$Ei(),(this.l??=[]).push(s)}static get observedAttributes(){return this.finalize(),this._$Eh&&[...this._$Eh.keys()]}static createProperty(s,e=Ye){if(e.state&&(e.attribute=!1),this._$Ei(),this.prototype.hasOwnProperty(s)&&((e=Object.create(e)).wrapped=!0),this.elementProperties.set(s,e),!e.noAccessor){let t=Symbol(),a=this.getPropertyDescriptor(s,t,e);a!==void 0&&At(this.prototype,s,a)}}static getPropertyDescriptor(s,e,t){let{get:a,set:i}=Mt(this.prototype,s)??{get(){return this[e]},set(r){this[e]=r}};return{get:a,set(r){let l=a?.call(this);i?.call(this,r),this.requestUpdate(s,l,t)},configurable:!0,enumerable:!0}}static getPropertyOptions(s){return this.elementProperties.get(s)??Ye}static _$Ei(){if(this.hasOwnProperty(le("elementProperties")))return;let s=zt(this);s.finalize(),s.l!==void 0&&(this.l=[...s.l]),this.elementProperties=new Map(s.elementProperties)}static finalize(){if(this.hasOwnProperty(le("finalized")))return;if(this.finalized=!0,this._$Ei(),this.hasOwnProperty(le("properties"))){let e=this.properties,t=[...Et(e),...Tt(e)];for(let a of t)this.createProperty(a,e[a])}let s=this[Symbol.metadata];if(s!==null){let e=litPropertyMetadata.get(s);if(e!==void 0)for(let[t,a]of e)this.elementProperties.set(t,a)}this._$Eh=new Map;for(let[e,t]of this.elementProperties){let a=this._$Eu(e,t);a!==void 0&&this._$Eh.set(a,e)}this.elementStyles=this.finalizeStyles(this.styles)}static finalizeStyles(s){let e=[];if(Array.isArray(s)){let t=new Set(s.flat(1/0).reverse());for(let a of t)e.unshift(Ce(a))}else s!==void 0&&e.push(Ce(s));return e}static _$Eu(s,e){let t=e.attribute;return t===!1?void 0:typeof t=="string"?t:typeof s=="string"?s.toLowerCase():void 0}constructor(){super(),this._$Ep=void 0,this.isUpdatePending=!1,this.hasUpdated=!1,this._$Em=null,this._$Ev()}_$Ev(){this._$ES=new Promise(s=>this.enableUpdating=s),this._$AL=new Map,this._$E_(),this.requestUpdate(),this.constructor.l?.forEach(s=>s(this))}addController(s){(this._$EO??=new Set).add(s),this.renderRoot!==void 0&&this.isConnected&&s.hostConnected?.()}removeController(s){this._$EO?.delete(s)}_$E_(){let s=new Map,e=this.constructor.elementProperties;for(let t of e.keys())this.hasOwnProperty(t)&&(s.set(t,this[t]),delete this[t]);s.size>0&&(this._$Ep=s)}createRenderRoot(){let s=this.shadowRoot??this.attachShadow(this.constructor.shadowRootOptions);return Ge(s,this.constructor.elementStyles),s}connectedCallback(){this.renderRoot??=this.createRenderRoot(),this.enableUpdating(!0),this._$EO?.forEach(s=>s.hostConnected?.())}enableUpdating(s){}disconnectedCallback(){this._$EO?.forEach(s=>s.hostDisconnected?.())}attributeChangedCallback(s,e,t){this._$AK(s,t)}_$ET(s,e){let t=this.constructor.elementProperties.get(s),a=this.constructor._$Eu(s,t);if(a!==void 0&&t.reflect===!0){let i=(t.converter?.toAttribute!==void 0?t.converter:ce).toAttribute(e,t.type);this._$Em=s,i==null?this.removeAttribute(a):this.setAttribute(a,i),this._$Em=null}}_$AK(s,e){let t=this.constructor,a=t._$Eh.get(s);if(a!==void 0&&this._$Em!==a){let i=t.getPropertyOptions(a),r=typeof i.converter=="function"?{fromAttribute:i.converter}:i.converter?.fromAttribute!==void 0?i.converter:ce;this._$Em=a;let l=r.fromAttribute(e,i.type);this[a]=l??this._$Ej?.get(a)??l,this._$Em=null}}requestUpdate(s,e,t,a=!1,i){if(s!==void 0){let r=this.constructor;if(a===!1&&(i=this[s]),t??=r.getPropertyOptions(s),!((t.hasChanged??we)(i,e)||t.useDefault&&t.reflect&&i===this._$Ej?.get(s)&&!this.hasAttribute(r._$Eu(s,t))))return;this.C(s,e,t)}this.isUpdatePending===!1&&(this._$ES=this._$EP())}C(s,e,{useDefault:t,reflect:a,wrapped:i},r){t&&!(this._$Ej??=new Map).has(s)&&(this._$Ej.set(s,r??e??this[s]),i!==!0||r!==void 0)||(this._$AL.has(s)||(this.hasUpdated||t||(e=void 0),this._$AL.set(s,e)),a===!0&&this._$Em!==s&&(this._$Eq??=new Set).add(s))}async _$EP(){this.isUpdatePending=!0;try{await this._$ES}catch(e){Promise.reject(e)}let s=this.scheduleUpdate();return s!=null&&await s,!this.isUpdatePending}scheduleUpdate(){return this.performUpdate()}performUpdate(){if(!this.isUpdatePending)return;if(!this.hasUpdated){if(this.renderRoot??=this.createRenderRoot(),this._$Ep){for(let[a,i]of this._$Ep)this[a]=i;this._$Ep=void 0}let t=this.constructor.elementProperties;if(t.size>0)for(let[a,i]of t){let{wrapped:r}=i,l=this[a];r!==!0||this._$AL.has(a)||l===void 0||this.C(a,void 0,i,l)}}let s=!1,e=this._$AL;try{s=this.shouldUpdate(e),s?(this.willUpdate(e),this._$EO?.forEach(t=>t.hostUpdate?.()),this.update(e)):this._$EM()}catch(t){throw s=!1,this._$EM(),t}s&&this._$AE(e)}willUpdate(s){}_$AE(s){this._$EO?.forEach(e=>e.hostUpdated?.()),this.hasUpdated||(this.hasUpdated=!0,this.firstUpdated(s)),this.updated(s)}_$EM(){this._$AL=new Map,this.isUpdatePending=!1}get updateComplete(){return this.getUpdateComplete()}getUpdateComplete(){return this._$ES}shouldUpdate(s){return!0}update(s){this._$Eq&&=this._$Eq.forEach(e=>this._$ET(e,this[e])),this._$EM()}updated(s){}firstUpdated(s){}};N.elementStyles=[],N.shadowRootOptions={mode:"open"},N[le("elementProperties")]=new Map,N[le("finalized")]=new Map,Ct?.({ReactiveElement:N}),(ye.reactiveElementVersions??=[]).push("2.1.2");var Fe=globalThis,Xe=n=>n,_e=Fe.trustedTypes,Qe=_e?_e.createPolicy("lit-html",{createHTML:n=>n}):void 0,nt="$lit$",F=`lit$${Math.random().toFixed(9).slice(2)}$`,rt="?"+F,Lt=`<${rt}>`,j=document,de=()=>j.createComment(""),ue=n=>n===null||typeof n!="object"&&typeof n!="function",De=Array.isArray,St=n=>De(n)||typeof n?.[Symbol.iterator]=="function",Le=`[ 	
\f\r]`,he=/<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g,et=/-->/g,tt=/>/g,I=RegExp(`>|${Le}(?:([^\\s"'>=/]+)(${Le}*=${Le}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`,"g"),st=/'/g,at=/"/g,ot=/^(?:script|style|textarea|title)$/i,Ue=n=>(s,...e)=>({_$litType$:n,strings:s,values:e}),h=Ue(1),K=Ue(2),es=Ue(3),q=Symbol.for("lit-noChange"),p=Symbol.for("lit-nothing"),it=new WeakMap,B=j.createTreeWalker(j,129);function lt(n,s){if(!De(n)||!n.hasOwnProperty("raw"))throw Error("invalid template strings array");return Qe!==void 0?Qe.createHTML(s):s}var Nt=(n,s)=>{let e=n.length-1,t=[],a,i=s===2?"<svg>":s===3?"<math>":"",r=he;for(let l=0;l<e;l++){let c=n[l],b,v,o=-1,u=0;for(;u<c.length&&(r.lastIndex=u,v=r.exec(c),v!==null);)u=r.lastIndex,r===he?v[1]==="!--"?r=et:v[1]!==void 0?r=tt:v[2]!==void 0?(ot.test(v[2])&&(a=RegExp("</"+v[2],"g")),r=I):v[3]!==void 0&&(r=I):r===I?v[0]===">"?(r=a??he,o=-1):v[1]===void 0?o=-2:(o=r.lastIndex-v[2].length,b=v[1],r=v[3]===void 0?I:v[3]==='"'?at:st):r===at||r===st?r=I:r===et||r===tt?r=he:(r=I,a=void 0);let f=r===I&&n[l+1].startsWith("/>")?" ":"";i+=r===he?c+Lt:o>=0?(t.push(b),c.slice(0,o)+nt+c.slice(o)+F+f):c+F+(o===-2?l:f)}return[lt(n,i+(n[e]||"<?>")+(s===2?"</svg>":s===3?"</math>":"")),t]},pe=class n{constructor({strings:s,_$litType$:e},t){let a;this.parts=[];let i=0,r=0,l=s.length-1,c=this.parts,[b,v]=Nt(s,e);if(this.el=n.createElement(b,t),B.currentNode=this.el.content,e===2||e===3){let o=this.el.content.firstChild;o.replaceWith(...o.childNodes)}for(;(a=B.nextNode())!==null&&c.length<l;){if(a.nodeType===1){if(a.hasAttributes())for(let o of a.getAttributeNames())if(o.endsWith(nt)){let u=v[r++],f=a.getAttribute(o).split(F),S=/([.?@])?(.*)/.exec(u);c.push({type:1,index:i,name:S[2],strings:f,ctor:S[1]==="."?Ne:S[1]==="?"?Pe:S[1]==="@"?He:ee}),a.removeAttribute(o)}else o.startsWith(F)&&(c.push({type:6,index:i}),a.removeAttribute(o));if(ot.test(a.tagName)){let o=a.textContent.split(F),u=o.length-1;if(u>0){a.textContent=_e?_e.emptyScript:"";for(let f=0;f<u;f++)a.append(o[f],de()),B.nextNode(),c.push({type:2,index:++i});a.append(o[u],de())}}}else if(a.nodeType===8)if(a.data===rt)c.push({type:2,index:i});else{let o=-1;for(;(o=a.data.indexOf(F,o+1))!==-1;)c.push({type:7,index:i}),o+=F.length-1}i++}}static createElement(s,e){let t=j.createElement("template");return t.innerHTML=s,t}};function Q(n,s,e=n,t){if(s===q)return s;let a=t!==void 0?e._$Co?.[t]:e._$Cl,i=ue(s)?void 0:s._$litDirective$;return a?.constructor!==i&&(a?._$AO?.(!1),i===void 0?a=void 0:(a=new i(n),a._$AT(n,e,t)),t!==void 0?(e._$Co??=[])[t]=a:e._$Cl=a),a!==void 0&&(s=Q(n,a._$AS(n,s.values),a,t)),s}var Se=class{constructor(s,e){this._$AV=[],this._$AN=void 0,this._$AD=s,this._$AM=e}get parentNode(){return this._$AM.parentNode}get _$AU(){return this._$AM._$AU}u(s){let{el:{content:e},parts:t}=this._$AD,a=(s?.creationScope??j).importNode(e,!0);B.currentNode=a;let i=B.nextNode(),r=0,l=0,c=t[0];for(;c!==void 0;){if(r===c.index){let b;c.type===2?b=new me(i,i.nextSibling,this,s):c.type===1?b=new c.ctor(i,c.name,c.strings,this,s):c.type===6&&(b=new Oe(i,this,s)),this._$AV.push(b),c=t[++l]}r!==c?.index&&(i=B.nextNode(),r++)}return B.currentNode=j,a}p(s){let e=0;for(let t of this._$AV)t!==void 0&&(t.strings!==void 0?(t._$AI(s,t,e),e+=t.strings.length-2):t._$AI(s[e])),e++}},me=class n{get _$AU(){return this._$AM?._$AU??this._$Cv}constructor(s,e,t,a){this.type=2,this._$AH=p,this._$AN=void 0,this._$AA=s,this._$AB=e,this._$AM=t,this.options=a,this._$Cv=a?.isConnected??!0}get parentNode(){let s=this._$AA.parentNode,e=this._$AM;return e!==void 0&&s?.nodeType===11&&(s=e.parentNode),s}get startNode(){return this._$AA}get endNode(){return this._$AB}_$AI(s,e=this){s=Q(this,s,e),ue(s)?s===p||s==null||s===""?(this._$AH!==p&&this._$AR(),this._$AH=p):s!==this._$AH&&s!==q&&this._(s):s._$litType$!==void 0?this.$(s):s.nodeType!==void 0?this.T(s):St(s)?this.k(s):this._(s)}O(s){return this._$AA.parentNode.insertBefore(s,this._$AB)}T(s){this._$AH!==s&&(this._$AR(),this._$AH=this.O(s))}_(s){this._$AH!==p&&ue(this._$AH)?this._$AA.nextSibling.data=s:this.T(j.createTextNode(s)),this._$AH=s}$(s){let{values:e,_$litType$:t}=s,a=typeof t=="number"?this._$AC(s):(t.el===void 0&&(t.el=pe.createElement(lt(t.h,t.h[0]),this.options)),t);if(this._$AH?._$AD===a)this._$AH.p(e);else{let i=new Se(a,this),r=i.u(this.options);i.p(e),this.T(r),this._$AH=i}}_$AC(s){let e=it.get(s.strings);return e===void 0&&it.set(s.strings,e=new pe(s)),e}k(s){De(this._$AH)||(this._$AH=[],this._$AR());let e=this._$AH,t,a=0;for(let i of s)a===e.length?e.push(t=new n(this.O(de()),this.O(de()),this,this.options)):t=e[a],t._$AI(i),a++;a<e.length&&(this._$AR(t&&t._$AB.nextSibling,a),e.length=a)}_$AR(s=this._$AA.nextSibling,e){for(this._$AP?.(!1,!0,e);s!==this._$AB;){let t=Xe(s).nextSibling;Xe(s).remove(),s=t}}setConnected(s){this._$AM===void 0&&(this._$Cv=s,this._$AP?.(s))}},ee=class{get tagName(){return this.element.tagName}get _$AU(){return this._$AM._$AU}constructor(s,e,t,a,i){this.type=1,this._$AH=p,this._$AN=void 0,this.element=s,this.name=e,this._$AM=a,this.options=i,t.length>2||t[0]!==""||t[1]!==""?(this._$AH=Array(t.length-1).fill(new String),this.strings=t):this._$AH=p}_$AI(s,e=this,t,a){let i=this.strings,r=!1;if(i===void 0)s=Q(this,s,e,0),r=!ue(s)||s!==this._$AH&&s!==q,r&&(this._$AH=s);else{let l=s,c,b;for(s=i[0],c=0;c<i.length-1;c++)b=Q(this,l[t+c],e,c),b===q&&(b=this._$AH[c]),r||=!ue(b)||b!==this._$AH[c],b===p?s=p:s!==p&&(s+=(b??"")+i[c+1]),this._$AH[c]=b}r&&!a&&this.j(s)}j(s){s===p?this.element.removeAttribute(this.name):this.element.setAttribute(this.name,s??"")}},Ne=class extends ee{constructor(){super(...arguments),this.type=3}j(s){this.element[this.name]=s===p?void 0:s}},Pe=class extends ee{constructor(){super(...arguments),this.type=4}j(s){this.element.toggleAttribute(this.name,!!s&&s!==p)}},He=class extends ee{constructor(s,e,t,a,i){super(s,e,t,a,i),this.type=5}_$AI(s,e=this){if((s=Q(this,s,e,0)??p)===q)return;let t=this._$AH,a=s===p&&t!==p||s.capture!==t.capture||s.once!==t.once||s.passive!==t.passive,i=s!==p&&(t===p||a);a&&this.element.removeEventListener(this.name,this,t),i&&this.element.addEventListener(this.name,this,s),this._$AH=s}handleEvent(s){typeof this._$AH=="function"?this._$AH.call(this.options?.host??this.element,s):this._$AH.handleEvent(s)}},Oe=class{constructor(s,e,t){this.element=s,this.type=6,this._$AN=void 0,this._$AM=e,this.options=t}get _$AU(){return this._$AM._$AU}_$AI(s){Q(this,s)}};var Pt=Fe.litHtmlPolyfillSupport;Pt?.(pe,me),(Fe.litHtmlVersions??=[]).push("3.3.3");var ct=(n,s,e)=>{let t=e?.renderBefore??s,a=t._$litPart$;if(a===void 0){let i=e?.renderBefore??null;t._$litPart$=a=new me(s.insertBefore(de(),i),i,void 0,e??{})}return a._$AI(n),a};var Ie=globalThis,$=class extends N{constructor(){super(...arguments),this.renderOptions={host:this},this._$Do=void 0}createRenderRoot(){let s=super.createRenderRoot();return this.renderOptions.renderBefore??=s.firstChild,s}update(s){let e=this.render();this.hasUpdated||(this.renderOptions.isConnected=this.isConnected),super.update(s),this._$Do=ct(e,this.renderRoot,this.renderOptions)}connectedCallback(){super.connectedCallback(),this._$Do?.setConnected(!0)}disconnectedCallback(){super.disconnectedCallback(),this._$Do?.setConnected(!1)}render(){return q}};$._$litElement$=!0,$.finalized=!0,Ie.litElementHydrateSupport?.({LitElement:$});var Ht=Ie.litElementPolyfillSupport;Ht?.({LitElement:$});(Ie.litElementVersions??=[]).push("4.2.2");var A=n=>(s,e)=>{e!==void 0?e.addInitializer(()=>{customElements.define(n,s)}):customElements.define(n,s)};var Ot={attribute:!0,type:String,converter:ce,reflect:!1,hasChanged:we},Ft=(n=Ot,s,e)=>{let{kind:t,metadata:a}=e,i=globalThis.litPropertyMetadata.get(a);if(i===void 0&&globalThis.litPropertyMetadata.set(a,i=new Map),t==="setter"&&((n=Object.create(n)).wrapped=!0),i.set(e.name,n),t==="accessor"){let{name:r}=e;return{set(l){let c=s.get.call(this);s.set.call(this,l),this.requestUpdate(r,c,n,!0,l)},init(l){return l!==void 0&&this.C(r,void 0,n,l),l}}}if(t==="setter"){let{name:r}=e;return function(l){let c=this[r];s.call(this,l),this.requestUpdate(r,c,n,!0,l)}}throw Error("Unsupported decorator location: "+t)};function m(n){return(s,e)=>typeof e=="object"?Ft(n,s,e):((t,a,i)=>{let r=a.hasOwnProperty(i);return a.constructor.createProperty(i,t),r?Object.getOwnPropertyDescriptor(a,i):void 0})(n,s,e)}function T(n){return m({...n,state:!0,attribute:!1})}var ht=(n,s)=>n.connection.subscribeMessage(s,{type:"solace/subscribe"}),te=(n,s)=>n.connection.sendMessagePromise({type:"solace/set_house",values:s}),dt=(n,s)=>n.connection.sendMessagePromise({type:"solace/set_ramp",ramp:s}),ut=(n,s,e)=>n.connection.sendMessagePromise({type:"solace/set_room",subentry_id:s,values:e}),pt=(n,s,e,t)=>n.connection.sendMessagePromise({type:"solace/set_light",subentry_id:s,entity_id:e,values:t}),mt=(n,s,e)=>n.connection.sendMessagePromise({type:"solace/set_zones",subentry_id:s,zones:e.map(({clear:t,...a})=>a)});var Ae=(n,s,e,t)=>n.connection.sendMessagePromise({type:"solace/room_action",subentry_id:s,action:e,...t===void 0?{}:{level:t}});var M=y`
  :host {
    --sol-page: var(--primary-background-color, #111213);
    --sol-card: var(--ha-card-background, var(--card-background-color, #1c1c1d));
    --sol-block: var(--secondary-background-color, #232325);
    --sol-control: #242426;

    --sol-text: var(--primary-text-color, #e8eaed);
    --sol-text-2: #c8ccd0;
    --sol-text-3: var(--secondary-text-color, #9aa0a6);
    --sol-text-4: #6f7377;
    --sol-faint: #5c6063;

    --sol-cyan: var(--primary-color, #4fc3f7);
    --sol-cyan-tint: rgba(79, 195, 247, 0.14);
    --sol-cyan-track: #3b4a52;
    --sol-amber: var(--state-icon-active-color, #ffb74d);
    --sol-amber-light: #ffcc80;
    --sol-amber-surface: #2a2418;
    --sol-amber-track: #4a3f28;
    --sol-green: var(--success-color, #66bb6a);

    --sol-track: #3a3d40;
    --sol-track-sm: #35383b;
    --sol-hair: var(--divider-color, rgba(255, 255, 255, 0.08));
    --sol-hair-faint: rgba(255, 255, 255, 0.05);

    --sol-r-pill: 14px;
    --sol-r-card: 12px;
    --sol-r-block: 8px;
    --sol-r-control: 6px;
    --sol-shadow: 0 1px 3px rgba(0, 0, 0, 0.35);

    --sol-series-ambience: #5c8dd6;
    --sol-series-start: var(--primary-color, #4fc3f7);
    --sol-series-sunset: #ffb74d;
    --sol-series-dusk: #7e57c2;
    --sol-series-full: #f06292;

    display: block;
    color: var(--sol-text);
    font-family: var(--ha-font-family-body, Roboto, system-ui, sans-serif);
  }

  /* Numeric readouts are tabular everywhere — a value that shifts width while a slider
     is dragged reads as flicker. */
  .tab-num {
    font-variant-numeric: tabular-nums;
  }

  .card {
    background: var(--sol-card);
    border-radius: var(--sol-r-card);
    box-shadow: var(--sol-shadow);
    padding: 15px 18px 16px;
  }

  .card-head {
    display: flex;
    align-items: center;
    gap: 9px;
    margin-bottom: 12px;
  }
  .card-head ha-icon {
    --mdc-icon-size: 19px;
    color: var(--sol-cyan);
  }
  .card-head h2 {
    margin: 0;
    font-size: 15px;
    font-weight: 500;
  }

  .eyebrow {
    font-size: 10.5px;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: var(--sol-text-4);
  }

  .caption {
    font-size: 11.5px;
    line-height: 1.5;
    color: var(--sol-text-4);
  }

  /* Focus ring, everywhere, per the handoff. */
  :focus-visible {
    outline: 2px solid var(--sol-cyan);
    outline-offset: 2px;
  }
`;var be=760,ge=258,w={x0:58,x1:744,y0:16,y1:206},se=null,Dt=n=>(se||(se=document.createElement("canvas").getContext("2d"),se&&(se.font="10px Roboto, system-ui, sans-serif")),se?se.measureText(n).width:n.length*5.4),k=class extends ${constructor(){super(...arguments);this.series=[];this.refLines=[];this.xTicks=[];this.yTicks=[];this.facts=[];this.xDomain=[0,1];this.yDomain=[0,1];this.marker=null;this.xTitle="";this.yTitle="";this.shade=null;this.shadeLabel=""}sx(e){let[t,a]=this.xDomain;return w.x0+(e-t)/(a-t||1)*(w.x1-w.x0)}sy(e){let[t,a]=this.yDomain;return w.y1-(e-t)/(a-t||1)*(w.y1-w.y0)}path(e){let t="",a=!1;for(let[i,r]of e){if(!isFinite(i)||!isFinite(r)){a=!1;continue}t+=`${a?"L":"M"}${this.sx(i).toFixed(2)} ${this.sy(r).toFixed(2)} `,a=!0}return t.trim()}packed(){let e=[];return this.refLines.filter(t=>isFinite(t.x)).map(t=>({...t,px:this.sx(t.x)})).sort((t,a)=>t.px-a.px).map(t=>{let a=Dt(t.label)/2+4,i=0;for(;e[i]!==void 0&&e[i]>t.px-a;)i++;return e[i]=t.px+a,{...t,row:i}})}render(){let e=this.packed();return h`
      <div class="plot">
        <svg viewBox="0 0 ${be} ${ge}" role="img">
          <defs>
            <linearGradient id="kgrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stop-color="#cfe0ff" />
              <stop offset="50%" stop-color="#ffe4c4" />
              <stop offset="100%" stop-color="#ff9a3c" />
            </linearGradient>
          </defs>

          ${this.shade?K`<rect
                x=${this.sx(this.shade[0])}
                y=${w.y0}
                width=${Math.max(0,this.sx(this.shade[1])-this.sx(this.shade[0]))}
                height=${w.y1-w.y0}
                fill="rgba(240,98,146,.10)" />`:p}

          ${this.yTicks.map(t=>K`<line x1=${w.x0} x2=${w.x1} y1=${this.sy(t.value)} y2=${this.sy(t.value)} stroke="rgba(255,255,255,.07)" stroke-width="1" />`)}
          ${this.xTicks.map(t=>K`<line y1=${w.y0} y2=${w.y1} x1=${this.sx(t.value)} x2=${this.sx(t.value)} stroke="rgba(255,255,255,.07)" stroke-width="1" />`)}
          ${this.refLines.map(t=>K`<line y1=${w.y0} y2=${w.y1} x1=${this.sx(t.x)} x2=${this.sx(t.x)} stroke=${t.colour} stroke-width="1" stroke-dasharray="4 3" />`)}
          ${this.series.map(t=>K`<path d=${this.path(t.points)} fill="none"
                stroke=${t.gradient?`url(#${t.gradient})`:t.colour}
                stroke-width=${t.width??2}
                stroke-linejoin="round"
                stroke-linecap="round"
                stroke-dasharray=${t.dashed?"4 3":"0"} />`)}
          ${this.marker?K`<circle cx=${this.sx(this.marker.x)} cy=${this.sy(this.marker.y)} r="5"
                fill="var(--sol-cyan)" stroke="var(--sol-card)" stroke-width="1.5" />`:p}
        </svg>

        ${this.yTicks.map(t=>h`<div
              class="lbl y"
              style="left:${w.x0/be*100}%;top:${this.sy(t.value)/ge*100}%"
            >
              ${t.label}
            </div>`)}
        ${this.xTicks.map(t=>h`<div
              class="lbl x"
              style="left:${this.sx(t.value)/be*100}%;top:${w.y1/ge*100}%"
            >
              ${t.label}
            </div>`)}
        ${e.map(t=>h`<div
              class="lbl ann"
              style="left:${t.px/be*100}%;top:${(w.y0+2+t.row*13)/ge*100}%;color:${t.textColour}"
            >
              ${t.label}
            </div>`)}
        ${this.shadeLabel&&this.shade?h`<div
              class="lbl ann"
              style="left:${(this.sx(this.shade[0])+this.sx(this.shade[1]))/2/be*100}%;top:${(w.y1-16)/ge*100}%;color:var(--sol-series-full)"
            >
              ${this.shadeLabel}
            </div>`:p}
        ${this.xTitle?h`<div class="axis-title" style="left:50%;bottom:-2px;transform:translateX(-50%)">
              ${this.xTitle}
            </div>`:p}
      </div>

      ${this.facts.length?h`<div class="facts">
            ${this.facts.map(t=>h`<div class="row"><span>${t.label}</span><span>${t.value}</span></div>`)}
          </div>`:p}
    `}};k.styles=[M,y`
      :host {
        display: flex;
        gap: 14px;
        align-items: flex-start;
        flex-wrap: wrap;
      }
      .plot {
        position: relative;
        flex: 1 1 420px;
        min-width: 300px;
      }
      svg {
        display: block;
        width: 100%;
        height: auto;
      }
      .lbl {
        position: absolute;
        font-size: 10px;
        color: var(--sol-text-4);
        white-space: nowrap;
        pointer-events: none;
        font-variant-numeric: tabular-nums;
      }
      .lbl.y {
        transform: translate(-100%, -50%);
        padding-right: 6px;
      }
      .lbl.x {
        transform: translate(-50%, 2px);
      }
      .lbl.ann {
        transform: translate(-50%, 0);
        font-size: 10px;
      }
      .axis-title {
        position: absolute;
        font-size: 10.5px;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        color: var(--sol-text-4);
      }
      .facts {
        flex: 0 0 198px;
        background: #232426;
        border: 1px solid rgba(255, 255, 255, 0.07);
        border-radius: var(--sol-r-block);
        padding: 10px 12px;
        box-sizing: border-box;
      }
      .facts .row {
        display: flex;
        justify-content: space-between;
        gap: 8px;
        font-size: 11px;
        line-height: 1.75;
      }
      .facts .row span:first-child {
        color: var(--sol-text-4);
      }
      .facts .row span:last-child {
        color: var(--sol-text-2);
        font-variant-numeric: tabular-nums;
      }
      @media (max-width: 720px) {
        .facts {
          flex: 1 1 100%;
        }
      }
    `],d([m({attribute:!1})],k.prototype,"series",2),d([m({attribute:!1})],k.prototype,"refLines",2),d([m({attribute:!1})],k.prototype,"xTicks",2),d([m({attribute:!1})],k.prototype,"yTicks",2),d([m({attribute:!1})],k.prototype,"facts",2),d([m({attribute:!1})],k.prototype,"xDomain",2),d([m({attribute:!1})],k.prototype,"yDomain",2),d([m({attribute:!1})],k.prototype,"marker",2),d([m()],k.prototype,"xTitle",2),d([m()],k.prototype,"yTitle",2),d([m({attribute:!1})],k.prototype,"shade",2),d([m()],k.prototype,"shadeLabel",2),k=d([A("sol-chart")],k);var Ut=n=>{let s=n>0?"+":n<0?"\u2212":"",e=Math.abs(n);return s+(e%1===0?e.toFixed(0):String(parseFloat(e.toFixed(2))))},C=n=>n===0?"0 stops":`${Ut(n)} ${Math.abs(n)===1?"stop":"stops"}`,D=(n,s)=>Math.round(100*Math.pow(Math.max(0,Math.min(254,n))/254,s)),Be=(n,s)=>n===null?"\u2014":`\u2192 level ${n} (${D(n,s)} % light)`,_=n=>{if(n==null||!isFinite(n))return"\u2014";let s=Math.round(n*60)%1440;return s<0&&(s+=1440),`${String(Math.floor(s/60)).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`},bt=n=>{let[s,e]=n.split(":");return(parseInt(s,10)||0)+(parseInt(e,10)||0)/60},g=(n,s=0)=>n.toLocaleString("en-GB",{maximumFractionDigits:s}),gt=n=>{if(n===null)return"\u2014";let s=Math.max(0,Math.round(n));return`${Math.floor(s/60)}:${String(s%60).padStart(2,"0")}`},vt=n=>{if(!n)return"never";let s=(Date.now()-new Date(n).getTime())/1e3;return!isFinite(s)||s<0?"just now":s<60?`${Math.round(s)}s ago`:s<3600?`${Math.round(s/60)}m ago`:`${Math.round(s/3600)}h ago`},ft=n=>isFinite(n)?`${g(n,n<10?1:0)} lx`:"no reading";var W=class extends ${constructor(){super(...arguments);this.text="";this.flip=!1}render(){return this.text?h`
      <button tabindex="0" aria-label=${this.text}>
        <ha-icon icon="mdi:information-outline"></ha-icon>
      </button>
      <div class="bubble" role="tooltip">${this.text}</div>
    `:p}};W.styles=[M,y`
      :host {
        display: inline-flex;
        position: relative;
      }
      button {
        all: unset;
        cursor: help;
        display: inline-flex;
        border-radius: 50%;
        color: var(--sol-text-4);
      }
      ha-icon {
        --mdc-icon-size: 14px;
      }
      .bubble {
        position: absolute;
        bottom: calc(100% + 9px);
        left: 0;
        width: max-content;
        max-width: 272px;
        min-width: 240px;
        background: #33363a;
        color: #dfe3e6;
        border-radius: var(--sol-r-block);
        padding: 9px 11px;
        font-size: 11.5px;
        line-height: 1.5;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.55);
        opacity: 0;
        transition: opacity 0.12s ease;
        /* Never intercept the pointer — the bubble sits over controls. */
        pointer-events: none;
        z-index: 20;
      }
      :host([flip]) .bubble {
        left: auto;
        right: 0;
      }
      button:hover + .bubble,
      button:focus-visible + .bubble,
      :host(:focus-within) .bubble {
        opacity: 1;
      }
    `],d([m()],W.prototype,"text",2),d([m({type:Boolean})],W.prototype,"flip",2),W=d([A("sol-help")],W);var E=class extends ${constructor(){super(...arguments);this.value=0;this.min=-2;this.max=2;this.step=.25;this.neutral=0;this.small=!1;this.tone="cyan";this.noReset=!1}_emit(e,t){this.value=e,this.dispatchEvent(new CustomEvent("value-changed",{detail:{value:e,final:t},bubbles:!0,composed:!0}))}render(){let e=this.min<this.neutral&&this.max>this.neutral,t=(this.neutral-this.min)/(this.max-this.min)*100,a=this.value!==this.neutral;return h`
      <div class="wrap">
        <input
          type="range"
          .min=${String(this.min)}
          .max=${String(this.max)}
          .step=${String(this.step)}
          .value=${String(this.value)}
          @input=${i=>this._emit(parseFloat(i.target.value),!1)}
          @change=${i=>this._emit(parseFloat(i.target.value),!0)}
        />
        ${e?h`<div class="tick" style="left:${t}%"></div>`:p}
      </div>
      ${this.noReset?p:a?h`<button
              class="reset"
              title="Reset"
              @click=${()=>this._emit(this.neutral,!0)}
            >
              <ha-icon icon="mdi:backup-restore"></ha-icon>
            </button>`:h`<span class="spacer"></span>`}
    `}};E.styles=[M,y`
      :host {
        display: flex;
        align-items: center;
        gap: 8px;
        flex: 1;
        min-width: 0;
      }
      .wrap {
        position: relative;
        flex: 1;
        display: flex;
        align-items: center;
        min-width: 0;
      }
      input {
        -webkit-appearance: none;
        appearance: none;
        width: 100%;
        background: transparent;
        margin: 0;
        height: 20px;
        cursor: pointer;
      }
      input::-webkit-slider-runnable-track {
        height: 4px;
        border-radius: 2px;
        background: var(--sol-track);
      }
      input::-moz-range-track {
        height: 4px;
        border-radius: 2px;
        background: var(--sol-track);
      }
      :host([small]) input::-webkit-slider-runnable-track {
        height: 3px;
        background: var(--sol-track-sm);
      }
      :host([small]) input::-moz-range-track {
        height: 3px;
        background: var(--sol-track-sm);
      }
      /* Amber is reserved for light output — it is the level slider's track, and it is
         never used to make an ordinary control look lively. */
      :host([tone="amber"]) input::-webkit-slider-runnable-track {
        background: linear-gradient(90deg, #5b4a26, var(--sol-amber));
      }
      :host([tone="amber"]) input::-moz-range-track {
        background: linear-gradient(90deg, #5b4a26, var(--sol-amber));
      }
      input::-webkit-slider-thumb {
        -webkit-appearance: none;
        width: 14px;
        height: 14px;
        margin-top: -5px;
        border-radius: 50%;
        background: var(--sol-cyan);
        border: none;
      }
      input::-moz-range-thumb {
        width: 14px;
        height: 14px;
        border: none;
        border-radius: 50%;
        background: var(--sol-cyan);
      }
      :host([tone="amber"]) input::-webkit-slider-thumb {
        background: var(--sol-amber);
      }
      :host([tone="amber"]) input::-moz-range-thumb {
        background: var(--sol-amber);
      }
      :host([small]) input::-webkit-slider-thumb {
        width: 11px;
        height: 11px;
        margin-top: -4px;
      }
      :host([small]) input::-moz-range-thumb {
        width: 11px;
        height: 11px;
      }
      input:focus-visible::-webkit-slider-thumb {
        box-shadow: 0 0 0 6px rgba(79, 195, 247, 0.25);
      }
      input:focus-visible::-moz-range-thumb {
        box-shadow: 0 0 0 6px rgba(79, 195, 247, 0.25);
      }
      .tick {
        position: absolute;
        top: 4px;
        bottom: 4px;
        width: 1px;
        background: rgba(255, 255, 255, 0.22);
        pointer-events: none;
      }
      button.reset {
        all: unset;
        cursor: pointer;
        display: inline-flex;
        color: var(--sol-text-4);
        border-radius: var(--sol-r-control);
      }
      button.reset:hover {
        color: var(--sol-text-2);
      }
      button.reset ha-icon {
        --mdc-icon-size: 15px;
      }
      .spacer {
        width: 15px;
        flex: 0 0 15px;
      }
    `],d([m({type:Number})],E.prototype,"value",2),d([m({type:Number})],E.prototype,"min",2),d([m({type:Number})],E.prototype,"max",2),d([m({type:Number})],E.prototype,"step",2),d([m({type:Number})],E.prototype,"neutral",2),d([m({type:Boolean})],E.prototype,"small",2),d([m()],E.prototype,"tone",2),d([m({type:Boolean})],E.prototype,"noReset",2),E=d([A("sol-slider")],E);var z=class extends ${constructor(){super(...arguments);this.value=null;this.min=null;this.max=null;this.step=1;this.suffix="";this.clearable=!1;this.width=60}_commit(e){let t=e.trim()===""?null:parseFloat(e);t!==null&&!isFinite(t)||this.dispatchEvent(new CustomEvent("value-changed",{detail:{value:t===null&&!this.clearable?this.min??0:t},bubbles:!0,composed:!0}))}render(){return h`
      <input
        type="number"
        class=${this.value!==null?"set":""}
        style="width:${this.width}px"
        placeholder="—"
        .value=${this.value===null?"":String(this.value)}
        .min=${this.min===null?"":String(this.min)}
        .max=${this.max===null?"":String(this.max)}
        .step=${String(this.step)}
        @change=${e=>this._commit(e.target.value)}
      />
      ${this.suffix?h`<span class="suffix">${this.suffix}</span>`:p}
    `}};z.styles=[M,y`
      :host {
        display: inline-flex;
        align-items: center;
        gap: 5px;
      }
      input {
        font: inherit;
        font-size: 12.5px;
        font-variant-numeric: tabular-nums;
        color: var(--sol-text);
        background: transparent;
        border: 1px solid rgba(255, 255, 255, 0.07);
        border-radius: var(--sol-r-control);
        padding: 6px 7px;
        box-sizing: border-box;
        text-align: right;
      }
      input.set {
        background: var(--sol-control);
        border-color: rgba(79, 195, 247, 0.35);
      }
      input::placeholder {
        color: var(--sol-faint);
      }
      /* The spinners are noise at this density and steal the width. */
      input::-webkit-outer-spin-button,
      input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
      }
      input[type="number"] {
        -moz-appearance: textfield;
      }
      .suffix {
        font-size: 11px;
        color: var(--sol-text-4);
      }
    `],d([m({type:Number})],z.prototype,"value",2),d([m({type:Number})],z.prototype,"min",2),d([m({type:Number})],z.prototype,"max",2),d([m({type:Number})],z.prototype,"step",2),d([m()],z.prototype,"suffix",2),d([m({type:Boolean})],z.prototype,"clearable",2),d([m({type:Number})],z.prototype,"width",2),z=d([A("sol-number")],z);var Z=class extends ${constructor(){super(...arguments);this.options=[];this.value=""}render(){return h`<div class="track">
      ${this.options.map(e=>h`<button
          class="${e.value===this.value?"on":""} ${e.tone==="amber"?"amber":""}"
          aria-pressed=${e.value===this.value}
          @click=${()=>this.dispatchEvent(new CustomEvent("segment-changed",{detail:{value:e.value},bubbles:!0,composed:!0}))}
        >
          ${e.label}
        </button>`)}
    </div>`}};Z.styles=[M,y`
      .track {
        display: inline-flex;
        background: var(--sol-control);
        border-radius: var(--sol-r-pill);
        padding: 2px;
      }
      button {
        all: unset;
        cursor: pointer;
        font-size: 11.5px;
        font-weight: 500;
        padding: 5px 12px;
        border-radius: 12px;
        color: var(--sol-text-3);
        text-align: center;
      }
      button.on {
        background: var(--sol-cyan-track);
        color: var(--sol-cyan);
      }
      button.on.amber {
        background: var(--sol-amber-track);
        color: var(--sol-amber);
      }
    `],d([m({attribute:!1})],Z.prototype,"options",2),d([m()],Z.prototype,"value",2),Z=d([A("sol-segmented")],Z);var V=class extends ${constructor(){super(...arguments);this.label="";this.help=""}render(){return h`
      <div class="label">
        <span>${this.label}</span>
        ${this.help?h`<sol-help .text=${this.help}></sol-help>`:p}
      </div>
      <div class="control"><slot></slot></div>
      <div class="value"><slot name="value"></slot></div>
    `}};V.styles=[M,y`
      :host {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 9px 0;
      }
      .label {
        flex: 0 0 176px;
        display: flex;
        align-items: center;
        gap: 6px;
        font-size: 12.5px;
        color: var(--sol-text-2);
      }
      .control {
        flex: 1;
        min-width: 0;
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .value {
        flex: 0 0 88px;
        text-align: right;
        font-size: 12.5px;
        font-variant-numeric: tabular-nums;
        color: var(--sol-text);
      }
      @media (max-width: 560px) {
        :host {
          flex-wrap: wrap;
        }
        .label {
          flex-basis: 100%;
        }
      }
    `],d([m()],V.prototype,"label",2),d([m()],V.prototype,"help",2),V=d([A("sol-row")],V);var It={day_kelvin:"The colour held through the day, until civil dusk starts the glide down.",night_kelvin:"The colour held after the glide, until the morning release.",colour_glide_minutes:"How long the glide from day to night colour takes, starting at civil dusk. The glide is interpolated in mireds, which is what makes it look even.",colour_trim_kelvin:"A live trim added to whatever the curve says, at any time of day. Use it to nudge the whole house warmer or cooler without touching the curve.",colour_step_mired:"Step size for bulbs that cannot glide colour while their brightness is fading \u2014 they skip steps while one is running, so they are walked in bigger, less frequent moves. Smaller is not better here: a finer walk would fall behind the curve rather than track it.",colour_step_mired_smooth:"Step size for bulbs that can glide colour during a brightness fade. They never skip a step, so they can be walked finely and look continuous. This is the setting that makes the good bulbs look good.",colour_catch_up_steps:"How far a single move may reach when a bulb has fallen behind the curve. Safe at any size \u2014 a bigger jump over the same short fade is further from the failure threshold, not closer \u2014 so this is about how large a visible colour jump you will accept.",colour_step_transition_s:"The fade applied to each individual colour step. This is the number the hardware limit is computed from, never the gap between steps."},Bt={ikea:"IKEA TR\xC5DFRI",aqara_cct:"Aqara CCT",aqara_rgb:"Aqara RGB"},Me=["#4fc3f7","#ffb74d","#81c784"],G=n=>1e6/Math.max(n,1),ae=n=>1e6/Math.max(n,1),P=class extends ${constructor(){super(...arguments);this.view="day";this.draft={}}schema(e){return this.snap.house_schema.find(t=>t.key===e)}value(e){return this.draft[e]??this.snap.house[e]??this.schema(e)?.default??0}async push(e,t,a=!0){this.draft={...this.draft,[e]:t},await te(this.hass,{[e]:t}),a&&window.setTimeout(()=>{let i={...this.draft};delete i[e],this.draft=i},700)}row(e,t){let a=this.schema(e);if(!a)return p;let i=this.value(e);return h`<sol-row .label=${a.name} .help=${It[e]??""}>
      <sol-slider
        .value=${i}
        .min=${a.min}
        .max=${a.max}
        .step=${a.step}
        .neutral=${a.default}
        .noReset=${a.default!==0}
        @value-changed=${r=>this.push(e,r.detail.value,r.detail.final)}
      ></sol-slider>
      <span slot="value"
        >${t?t(i):`${g(i)}${a.unit?` ${a.unit}`:""}`}</span
      >
    </sol-row>`}kelvinAt(e){let t=this.snap.world.dusk_hour,a=this.value("morning_release_hour"),i=this.value("day_kelvin"),r=this.value("night_kelvin"),l=Math.max(this.value("colour_glide_minutes"),0)/60,c=(e-t+24)%24,b=(a-t+24)%24,v=Math.max(this.value("morning_glide_minutes"),0)/60,o;if(c>=b){let u=c-b;v<=0||u>=v?o=i:o=ae(G(r)+u/v*(G(i)-G(r)))}else l<=0||c>=l?o=r:o=ae(G(i)+c/l*(G(r)-G(i)));return Math.max(2e3,Math.min(7e3,o+this.value("colour_trim_kelvin")))}chart(){let e=this.snap.world,t=e.dusk_hour,a=[],i,r;if(this.view==="day"){for(let o=0;o<=288;o++){let u=o/288*24;a.push([u,this.kelvinAt(u)])}i=[0,24],r=[0,3,6,9,12,15,18,21,24].map(o=>({value:o,label:_(o)}))}else{for(let o=-60;o<=240;o+=2)a.push([o,this.kelvinAt((t+o/60+24)%24)]);i=[-60,240],r=[-60,0,60,120,180,240].map(o=>({value:o,label:`${o>0?"+":""}${o}`}))}let l=o=>o===null?NaN:this.view==="day"?o:((o-t+36)%24-12)*60,c=[{hour:e.sunrise_hour,label:"sunrise",c:"rgba(255,183,77,.5)",t:"var(--sol-amber)"},{hour:e.sunset_hour,label:"sunset",c:"rgba(255,183,77,.5)",t:"var(--sol-amber)"},{hour:t,label:"civil dusk",c:"rgba(149,117,205,.75)",t:"#b39ddb"},{hour:this.value("morning_release_hour"),label:"release",c:"rgba(79,195,247,.45)",t:"var(--sol-cyan)"}].map(o=>({x:l(o.hour),label:o.label,colour:o.c,textColour:o.t})).filter(o=>isFinite(o.x)&&o.x>=i[0]&&o.x<=i[1]),b=this.value("night_kelvin")+this.value("colour_trim_kelvin"),v=`${g(this.value("day_kelvin"))} \u2192 ${g(this.value("night_kelvin"))} K`;return h`<sol-chart
      .series=${[{points:a,colour:"#ffe4c4",width:2.5,gradient:"kgrad"}]}
      .refLines=${c}
      .xTicks=${r}
      .yTicks=${[2e3,3e3,4e3,5e3,6e3,7e3].map(o=>({value:o,label:`${o/1e3}k`}))}
      .xDomain=${i}
      .yDomain=${[2e3,7e3]}
      .marker=${{x:l(e.clock_hour),y:this.kelvinAt(e.clock_hour)}}
      xTitle=${this.view==="day"?"time of day":"minutes from civil dusk"}
      .facts=${[{label:"Now",value:`${_(e.clock_hour)} \xB7 ${g(this.kelvinAt(e.clock_hour))} K`},{label:"Sunset",value:_(e.sunset_hour)},{label:"Civil dusk",value:_(t)},{label:"Day \u2192 night",value:v},{label:"Glide",value:`${g(this.value("colour_glide_minutes"))} min`},{label:"Trim",value:`${this.value("colour_trim_kelvin")>0?"+":""}${g(this.value("colour_trim_kelvin"))} K`},{label:"Night holds at",value:`${g(Math.max(2e3,b))} K`}]}
    ></sol-chart>`}walk(e,t){let a=Math.max(this.snap.fade?.interval_s??60,1),i=this.snap.world.dusk_hour,r=v=>G(this.kelvinAt((i+v/60+24)%24)),l=r(0),c=[[0,ae(l)]],b=0;for(let v=a;v<=t*60;v+=a){let o=v/60,u=r(o)-l;if(Math.abs(u)>=e.step_mired){let f=Math.min(Math.floor(Math.abs(u)/e.step_mired)*e.step_mired,e.max_step_mired);c.push([o,ae(l)]),l+=u>0?f:-f,c.push([o,ae(l)]),b++}}return c.push([t,ae(l)]),{points:c,moves:b,perHour:Math.round(b/t*60)}}grouped(e){let t=[];for(let a of e){let i=`${a.step_mired}|${a.max_step_mired}|${a.step_transition_s}|${a.concurrent}`,r=t.find(c=>`${c.step_mired}|${c.max_step_mired}|${c.step_transition_s}|${c.concurrent}`===i),l=Bt[a.family]??a.family;r?(r.label+=` + ${l}`,r.count+=a.count):t.push({...a,label:l})}return t}stepChart(e){let a=this.snap.world.dusk_hour,i=[];for(let o=0;o<=20;o+=.25)i.push([o,this.kelvinAt((a+o/60+24)%24)]);let r=e.map((o,u)=>({f:o,colour:Me[u%Me.length],...this.walk(o,20)})),l=[...i.map(o=>o[1]),...r.flatMap(o=>o.points.map(u=>u[1]))],c=Math.min(...l),b=Math.max(...l),v=Math.max((b-c)*.12,20);return h`<sol-chart
      .series=${[{points:i,colour:"rgba(255,255,255,.28)",width:1.5,dashed:!0},...r.map(o=>({points:o.points,colour:o.colour,width:2}))]}
      .xTicks=${[0,5,10,15,20].map(o=>({value:o,label:`+${o}`}))}
      .yTicks=${[c,(c+b)/2,b].map(o=>({value:o,label:`${g(Math.round(o))}`}))}
      .xDomain=${[0,20]}
      .yDomain=${[c-v,b+v]}
      xTitle="minutes into the glide"
      yTitle="K"
      .facts=${[{label:"Tick",value:`every ${g(Math.round(this.snap.fade?.interval_s??0))} s`},...r.map(o=>({label:o.f.concurrent?"Smooth":"Stepped",value:`${o.f.step_mired} mired \xB7 ~${o.perHour}/h`}))]}
    ></sol-chart>`}fadeCard(){let e=this.snap.fade;if(!e||!e.families.length)return p;let t=this.grouped(e.families);return h`<div class="card full">
      <div class="card-head">
        <ha-icon icon="mdi:stairs"></ha-icon>
        <h2>How each bulb walks the curve</h2>
      </div>
      <div class="caption" style="margin-bottom:10px">
        Colour is stepped, not faded — a long colour transition underflows the bulb's own maths and
        strands it on a hardware rail, permanently and silently. So the curve is walked in small
        absolute writes. One clock serves every bulb; what differs is how big a step each family
        takes, and the dashed line is the curve they are all aiming at.
      </div>
      ${this.stepChart(t)}
      <div class="families">
        ${t.map((a,i)=>h`<div class="family">
            <div class="fam-head">
              <span class="swatch" style="background:${Me[i%Me.length]}">
              </span>
              <b>${a.label}</b>
              <span class="count">${a.count} bulb${a.count===1?"":"s"}</span>
              <span class="tag ${a.concurrent?"ok":"warn"}">
                ${a.concurrent?"glides during a fade":"waits for brightness"}
              </span>
            </div>
            <div class="caption">${a.reason}</div>
          </div>`)}
      </div>
      <div class="sub">
        ${this.row("colour_step_mired_smooth",a=>`${g(a)} mired`)}
        ${this.row("colour_step_mired",a=>`${g(a)} mired`)}
        ${this.row("colour_catch_up_steps",a=>`up to ${g(a)} steps`)}
        ${this.row("colour_step_transition_s",a=>`${g(a,1)} s`)}
        <div class="caption">
          The tick is paced for the finest family in the house. Coarser bulbs are not over-driven by
          it — they simply decline the ticks where the curve has not moved far enough to be worth a
          write.
        </div>
      </div>
    </div>`}clampedLights(){let e=this.value("day_kelvin")+this.value("colour_trim_kelvin"),t=[];for(let a of this.snap.rooms)for(let i of a.lights)i.max_kelvin<e&&t.push({name:i.name,max:i.max_kelvin});return t}render(){let e=this.value("colour_trim_kelvin"),t=this.value("day_kelvin")+e,a=this.clampedLights();return h`<div class="grid">
      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:palette"></ha-icon>
          <h2>Colour</h2>
        </div>
        <div class="kelvin-track"></div>
        ${this.row("day_kelvin",i=>`${g(i)} K`)}
        ${this.row("night_kelvin",i=>`${g(i)} K`)}
        ${this.row("colour_glide_minutes",i=>`${g(i)} min`)}
        ${this.row("colour_trim_kelvin",i=>`${i>0?"+":""}${g(i)} K`)}
        <div class="trim-caption">
          <span>${e<0?"warmer":"\xA0"}</span>
          <span>right now: <b>${g(this.kelvinAt(this.snap.world.clock_hour))} K</b></span>
          <span>${e>0?"cooler":"\xA0"}</span>
        </div>
        <div class="caption" style="margin-top:10px">
          Colour moves in steps, not one long fade. Step sizes differ per bulb family — see “How
          each bulb walks the curve” below.
        </div>
        ${a.length?h`<div class="advisory">
              <ha-icon icon="mdi:alert-outline"></ha-icon>
              <div>
                ${a.length} bulb${a.length===1?" is":"s are"} held below the day
                target by their own hardware. They accept the command and report success, so this
                is the only place it shows:
                <ul>
                  ${a.map(i=>h`<li>${i.name} — capped at ${g(i.max)} K</li>`)}
                </ul>
              </div>
            </div>`:p}
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:thermometer-lines"></ha-icon>
          <h2>What each bulb can actually do</h2>
        </div>
        <div class="caption" style="margin-bottom:8px">
          One Kelvin goes to the whole house; each bulb pins it into its own range and reports
          success either way. Broadcasting to mixed families is safe, but the families then
          visually diverge — which is the part worth seeing.
        </div>
        <div class="ranges">
          ${this.snap.rooms.flatMap(i=>i.lights.map(r=>h`<div class="range-row ${r.kelvin<t?"capped":""}">
                <span class="n" title=${r.name}>${r.name}</span>
                <span class="r tab-num">${g(r.min_kelvin)}–${g(r.max_kelvin)} K</span>
                <span class="a tab-num">${g(r.kelvin)} K${r.kelvin_clamped?" \u2691":""}</span>
              </div>`))}
        </div>
      </div>

      <div class="card full">
        <div class="head-row">
          <div class="card-head" style="margin:0">
            <ha-icon icon="mdi:chart-line"></ha-icon>
            <h2>Colour temperature</h2>
          </div>
          <sol-segmented
            .options=${[{value:"day",label:"Day"},{value:"hour",label:"Hour"}]}
            .value=${this.view}
            @segment-changed=${i=>this.view=i.detail.value}
          ></sol-segmented>
        </div>
        ${this.chart()}
      </div>

      ${this.fadeCard()}
    </div>`}};P.styles=[M,y`
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(440px, 1fr));
        gap: 14px;
      }
      @media (max-width: 520px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }
      .full {
        grid-column: 1 / -1;
      }
      .head-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 12px;
      }
      .trim-caption {
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-size: 11.5px;
        color: var(--sol-text-4);
        margin-top: 2px;
      }
      .trim-caption b {
        color: var(--sol-text);
        font-weight: 500;
        font-variant-numeric: tabular-nums;
      }
      .advisory {
        display: flex;
        gap: 8px;
        align-items: flex-start;
        margin-top: 14px;
        padding: 9px 11px;
        background: var(--sol-amber-surface);
        border-radius: var(--sol-r-block);
        font-size: 11.5px;
        line-height: 1.5;
        color: var(--sol-amber-light);
      }
      .advisory ha-icon {
        --mdc-icon-size: 16px;
        flex: 0 0 auto;
        color: var(--sol-amber);
      }
      .advisory ul {
        margin: 4px 0 0;
        padding-left: 16px;
      }
      /* One block per bulb family, each carrying the reason it is walked that way. A
         stepped family looks like a bug without it. */
      .families {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 8px;
        margin: 12px 0 4px;
      }
      .family {
        padding: 8px 10px;
        background: rgba(255, 255, 255, 0.03);
        border-radius: var(--sol-r-block);
      }
      .fam-head {
        display: flex;
        align-items: center;
        gap: 7px;
        font-size: 12px;
        margin-bottom: 4px;
        flex-wrap: wrap;
      }
      .swatch {
        width: 9px;
        height: 9px;
        border-radius: 2px;
        flex: 0 0 auto;
      }
      .fam-head .count {
        color: var(--sol-text-4);
        font-size: 11px;
      }
      .tag {
        margin-left: auto;
        padding: 1px 7px;
        border-radius: 999px;
        font-size: 10.5px;
        white-space: nowrap;
      }
      .tag.ok {
        background: rgba(129, 199, 132, 0.14);
        color: #a5d6a7;
      }
      .tag.warn {
        background: var(--sol-amber-surface);
        color: var(--sol-amber-light);
      }
      /* The Kelvin gradient is a real reference, not decoration — it is what the numbers
         on the slider mean. */
      .ranges {
        display: flex;
        flex-direction: column;
      }
      .range-row {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 104px 76px;
        gap: 8px;
        align-items: center;
        padding: 6px 0;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        font-size: 12px;
      }
      .range-row .n {
        color: var(--sol-text-2);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .range-row .r {
        color: var(--sol-text-4);
        text-align: right;
      }
      .range-row .a {
        text-align: right;
        color: var(--sol-text);
      }
      /* Amber = a hardware clamp. That is the whole reason amber is reserved. */
      .range-row.capped .a {
        color: var(--sol-amber);
      }
      .kelvin-track {
        height: 4px;
        border-radius: 2px;
        margin: 2px 0 8px;
        background: linear-gradient(
          90deg,
          #ff9a3c,
          #ffc689,
          #ffe4c4,
          #fff6ec,
          #f2f4ff,
          #cfe0ff
        );
      }
    `],d([m({attribute:!1})],P.prototype,"hass",2),d([m({attribute:!1})],P.prototype,"snap",2),d([T()],P.prototype,"view",2),d([T()],P.prototype,"draft",2),P=d([A("sol-tab-colour")],P);var H={houseBias:"Shifts every room at once, in stops. One stop is a doubling of light. Rooms keep their own offsets, so this moves the whole house without flattening the differences between rooms.",roomBias:"This room's offset from the house, in stops. Added to the house bias, never multiplied by it.",zoneBias:"A layer between the room and its individual lights. Use it when part of a room wants a different level from the rest.",ambience:"A low-light floor for this room while you are awake and it is dark outside. It only ever raises the level, never lowers it. 0 means this room follows the house-wide floor.",diminish:"Kitchen behaviour. When the near sensor reads clear the lights reduce by this much and stay there \u2014 they never switch off from diminish alone. 0 means no effect.",perLight:"Per-light offsets, in stops, added on top of the house, room and zone biases. Min is a cutoff: demand below it turns the light off entirely. Max is a clamp: the light never exceeds it.",min:"A cutoff, not a floor. If the computed level falls below this, the light goes off rather than sitting at a useless glow.",max:"A hard clamp applied last, after everything else. The light never exceeds it \u2014 this is what makes a glare cap a rule rather than a suggestion.",manual:"Manual hands this room to you and stops Solace writing to it. A touch on a physical switch does the same for a while; this switch holds until you turn it off.",zone:"A part of this area with its own bias \u2014 the office end of a living room, the sink end of a kitchen. Same four walls, so it shares the area's presence and its dials; the zone bias is an offset on top.",zoneDiminish:"When this zone's own sensor reads clear, its lights reduce by this much and stay there. They never switch off from diminish alone. 0 means no effect.",nightOff:"When you are asleep this room goes fully dark instead of dropping to the night level. Once you are up it rejoins the house at the night level, so the room you are standing in is never the dark one."},O=class extends ${constructor(){super(...arguments);this.open=new Set;this.draft={}}connectedCallback(){super.connectedCallback(),this.timer=window.setInterval(()=>this.requestUpdate(),1e3)}disconnectedCallback(){super.disconnectedCallback(),this.timer&&clearInterval(this.timer)}key(...e){return e.join("|")}local(e,t){return this.draft[e]??t}async pushHouse(e,t,a){this.draft={...this.draft,[this.key("h",e)]:t},await te(this.hass,{[e]:t}),a&&this.clearDraft(this.key("h",e))}async pushRoom(e,t,a,i=!0){typeof a=="number"&&(this.draft={...this.draft,[this.key(e,t)]:a}),await ut(this.hass,e,{[t]:a}),i&&this.clearDraft(this.key(e,t))}async pushLight(e,t,a,i){await pt(this.hass,e,t,{[a]:i})}clearDraft(e){window.setTimeout(()=>{let t={...this.draft};delete t[e],this.draft=t},700)}roomIcon(e){let t=e.toLowerCase();return t.includes("bed")?"mdi:bed":t.includes("kitchen")||t.includes("diner")?"mdi:countertop":t.includes("entry")||t.includes("hall")?"mdi:door-open":t.includes("office")?"mdi:desk":"mdi:sofa"}renderStrip(){let e=this.snap.world,t=e.night_active?"Night":e.asleep?"Asleep":"Normal",a=(e.dusk_hour-e.clock_hour+24)%24*60;return h`<div class="strip">
      <div>
        <ha-icon icon=${e.night_active?"mdi:weather-night":"mdi:weather-sunset"}></ha-icon>
        <b>${t}</b>
      </div>
      <div><span>Outdoor</span><b>${ft(e.lux)}</b></div>
      <div><span>Colour</span><b>${g(e.kelvin)} K</b></div>
      <div>
        <span>Civil dusk</span>
        <b>${_(e.dusk_hour)}</b>
        ${a<720?h`<span>(${Math.round(a)} min)</span>`:p}
      </div>
      <div><span>Updated</span><b>${vt(e.updated_at)}</b></div>
      <div>
        <span>Every</span
        ><b>${e.interval_s?`${Math.round(e.interval_s)} s`:"\u2014"}</b>
      </div>
    </div>`}renderHouseBias(){let e=this.snap.house.gamma??2.39,t=this.key("h","bias_stops"),a=this.local(t,this.snap.house.bias_stops??0),i=Math.max(0,...this.snap.rooms.map(r=>r.level??0));return h`<div class="house">
      <div class="name">
        <ha-icon icon="mdi:brightness-6"></ha-icon>
        <span>Whole house bias</span>
        <sol-help .text=${H.houseBias}></sol-help>
      </div>
      <sol-slider
        .value=${a}
        .min=${-2}
        .max=${2}
        .step=${.25}
        @value-changed=${r=>this.pushHouse("bias_stops",r.detail.value,r.detail.final)}
      ></sol-slider>
      <div class="readout tab-num">${C(a)}</div>
      <div class="cons">${i?Be(i,e):"\u2192 all rooms dark"}</div>
    </div>`}renderLight(e,t){let a=this.snap.house.gamma??2.39,i=this.key(e.subentry_id,t.entity_id,"bias"),r=this.local(i,t.bias_stops),l=t.kelvin_clamped?`held at ${g(t.max_kelvin)} K by its bulb`:"";return h`<div class="lrow">
      <div class="lname">
        <div class="n" title=${t.name}>${t.name}</div>
        <div class="s">
          ${t.group_size>1?h`<span>group of ${t.group_size} · </span>`:p}
          ${l?h`<span class="hw">${l} · </span>`:p}
          <span class="tab-num"
            >level ${t.level??"\u2014"}${t.level!==null?` (${D(t.level,a)} %)`:""}</span
          >
        </div>
      </div>
      <div class="adj">
        <sol-slider
          small
          .value=${r}
          .min=${-2}
          .max=${2}
          .step=${.25}
          @value-changed=${c=>{this.draft={...this.draft,[i]:c.detail.value},this.pushLight(e.subentry_id,t.entity_id,"bias_stops",c.detail.value),c.detail.final&&this.clearDraft(i)}}
        ></sol-slider>
        <span class="sv ${r!==0?"moved":""}">${C(r).replace(" stops","").replace(" stop","")}</span>
      </div>
      <sol-number
        clearable
        .value=${t.clamp_min}
        .min=${0}
        .max=${254}
        .width=${54}
        @value-changed=${c=>this.pushLight(e.subentry_id,t.entity_id,"clamp_min",c.detail.value)}
      ></sol-number>
      <sol-number
        clearable
        .value=${t.clamp_max}
        .min=${1}
        .max=${254}
        .width=${54}
        @value-changed=${c=>this.pushLight(e.subentry_id,t.entity_id,"clamp_max",c.detail.value)}
      ></sol-number>
    </div>`}renderGrouped(e){if(!e.zones.length)return e.lights.map(a=>this.renderLight(e,a));let t=e.lights.filter(a=>!a.zone_id);return h`
      ${e.zones.map(a=>{let i=e.lights.filter(r=>r.zone_id===a.zone_id);return i.length?h`
          <div class="zone-head">
            <span class="eyebrow">${a.name}</span>
            ${a.clear===null?p:h`<span class="pill ${a.clear?"off":"on"}"
                  >${a.clear?"clear":"occupied"}</span
                >`}
            <span class="grow"></span>
            <sol-slider
              small
              .value=${a.bias_stops}
              .min=${-2}
              .max=${2}
              .step=${.25}
              @value-changed=${r=>this.pushZone(e,a,{bias_stops:r.detail.value},r.detail.final)}
            ></sol-slider>
            <span class="zv tab-num">${C(a.bias_stops)}</span>
            ${a.presence.length?h`<sol-number
                  .value=${a.diminish_pct}
                  .min=${0}
                  .max=${100}
                  .width=${48}
                  suffix="% when clear"
                  @value-changed=${r=>this.pushZone(e,a,{diminish_pct:r.detail.value})}
                ></sol-number>`:p}
          </div>
          ${i.map(r=>this.renderLight(e,r))}
        `:p})}
      ${t.length?h`<div class="zone-head warnrow">
              <span class="eyebrow">Not in a zone</span>
              <sol-help
                flip
                .text=${"These lights take the area's own zone bias. Assign them to a zone if you want them to follow one."}
              ></sol-help>
            </div>
            ${t.map(a=>this.renderLight(e,a))}`:p}
    `}async pushZone(e,t,a,i=!0){let r=e.zones.map(l=>l.zone_id===t.zone_id?{...l,...a}:l);await mt(this.hass,e.subentry_id,r)}renderRoom(e){let t=this.snap.house.gamma??2.39,a=(e.level??0)>0,i=this.open.has(e.subentry_id),r=e.manual.active,l=this.key(e.subentry_id,"bias_stops"),c=this.local(l,Number(e.settings.bias_stops??0)),b=this.key(e.subentry_id,"zone_bias_stops"),v=this.local(b,Number(e.settings.zone_bias_stops??0));return h`<div class="room">
      <div class="room-head">
        <ha-icon class=${a?"lit":""} icon=${this.roomIcon(e.name)}></ha-icon>
        <div class="title">
          <h3>${e.name}</h3>
          <div class="sub">
            ${e.lights.length} light${e.lights.length===1?"":"s"} ·
            ${e.occupied?"occupied":"clear"} ·
            ${e.ambience_open?"dark":"daylight"}
          </div>
        </div>
        <sol-segmented
          .options=${[{value:"auto",label:"Auto"},{value:"manual",label:"Manual",tone:"amber"}]}
          .value=${r?"manual":"auto"}
          @segment-changed=${o=>Ae(this.hass,e.subentry_id,o.detail.value==="manual"?"manual":"auto")}
        ></sol-segmented>
      </div>

      ${r?h`<div class="manual-block">
            <div class="top">
              <ha-icon icon="mdi:clock-outline"></ha-icon>
              <span
                >${e.manual.switch?"Held until you switch back":`Auto resumes in ${gt(e.manual.remaining_s)}`}</span
              >
              <button class="ghost" @click=${()=>Ae(this.hass,e.subentry_id,"auto")}>
                Resume now
              </button>
            </div>
            <div class="bias-row">
              <sol-slider
                tone="amber"
                noReset
                .value=${Math.max(...e.lights.map(o=>o.current_level),0)}
                .min=${0}
                .max=${254}
                .step=${1}
                @value-changed=${o=>Ae(this.hass,e.subentry_id,"level",Math.round(o.detail.value))}
              ></sol-slider>
              <div class="readout tab-num">
                ${Math.max(...e.lights.map(o=>o.current_level),0)} ·
                ${D(Math.max(...e.lights.map(o=>o.current_level),0),t)} %
              </div>
            </div>
            <div class="note">A physical switch left on holds this room indefinitely.</div>
          </div>`:h`<div class="status">
            <div>
              <span class="eyebrow">Demand</span>
              <span class="v tab-num"
                >${e.demand===null?"\u2014":`${Math.round(e.demand*100)} %`}</span
              >
            </div>
            <div>
              <span class="eyebrow">Settled</span>
              <span class="v amber tab-num">${e.level??"\u2014"}</span>
            </div>
            <div>
              <span class="eyebrow">Output</span>
              <span class="v tab-num"
                >${e.level===null?"\u2014":`${D(e.level,t)} %`}</span
              >
            </div>
            <div>
              <span class="eyebrow">Mode</span>
              <span class="v">${e.mode??"\u2014"}</span>
            </div>
          </div>`}

      <div class="bias-row">
        <span class="lab">Room bias <sol-help .text=${H.roomBias}></sol-help></span>
        <sol-slider
          .value=${c}
          .min=${-2}
          .max=${2}
          .step=${.25}
          @value-changed=${o=>this.pushRoom(e.subentry_id,"bias_stops",o.detail.value,o.detail.final)}
        ></sol-slider>
        <span class="readout tab-num">${C(c)}</span>
        <span class="cons">${Be(e.level,t)}</span>
      </div>

      <div class="bias-row">
        <span class="lab">Zone bias <sol-help .text=${H.zoneBias}></sol-help></span>
        <sol-slider
          .value=${v}
          .min=${-1}
          .max=${1}
          .step=${.25}
          @value-changed=${o=>this.pushRoom(e.subentry_id,"zone_bias_stops",o.detail.value,o.detail.final)}
        ></sol-slider>
        <span class="readout tab-num">${C(v)}</span>
      </div>

      ${e.has_near&&!e.zones.some(o=>o.presence.length)?h`<div class="bias-row">
            <!-- Only for an UNDIVIDED area. Once zones carry their own presence, this
                 row and the per-zone ones would be two diminish controls with different
                 meanings sitting on the same card. -->
            <span class="lab">Diminish <sol-help .text=${H.diminish}></sol-help></span>
            <span class="pill ${e.near_clear?"off":"on"}"
              >near ${e.near_clear?"clear":"occupied"}</span
            >
            <span class="grow"></span>
            <sol-number
              .value=${Number(e.settings.diminish_pct??0)}
              .min=${0}
              .max=${100}
              suffix="% when clear"
              .width=${52}
              @value-changed=${o=>this.pushRoom(e.subentry_id,"diminish_pct",o.detail.value)}
            ></sol-number>
          </div>`:p}

      <button
        class="disclose"
        aria-expanded=${i}
        @click=${()=>{let o=new Set(this.open);o.has(e.subentry_id)?o.delete(e.subentry_id):o.add(e.subentry_id),this.open=o}}
      >
        <ha-icon class=${i?"open":""} icon="mdi:chevron-right"></ha-icon>
        Per-light adjustments (${e.lights.length}${e.zones.length>1?` in ${e.zones.length} zones`:""})
      </button>

      ${i?h`<div class="lights">
            <div class="lhead">
              <span class="eyebrow">Light</span>
              <span class="eyebrow">Adjustment <sol-help .text=${H.perLight}></sol-help></span>
              <span class="eyebrow">Min <sol-help flip .text=${H.min}></sol-help></span>
              <span class="eyebrow">Max <sol-help flip .text=${H.max}></sol-help></span>
            </div>
            ${this.renderGrouped(e)}
          </div>`:p}

      <div class="footer">
        <span class="lab">Ambience <sol-help flip .text=${H.ambience}></sol-help></span>
        <sol-number
          .value=${Number(e.settings.ambience_level??0)}
          .min=${0}
          .max=${254}
          .width=${56}
          suffix=${Number(e.settings.ambience_level??0)===0?`follows house (${this.snap.house.ambience_level??0})`:`= ${D(Number(e.settings.ambience_level),t)} % light`}
          @value-changed=${o=>this.pushRoom(e.subentry_id,"ambience_level",o.detail.value)}
        ></sol-number>
        <span class="grow"></span>
        <span class="lab">
          <label style="display:flex;align-items:center;gap:6px;cursor:pointer">
            <input
              type="checkbox"
              .checked=${!!e.settings.night_off}
              @change=${o=>this.pushRoom(e.subentry_id,"night_off",o.target.checked)}
            />
            Off when asleep
          </label>
          <sol-help flip .text=${H.nightOff}></sol-help>
        </span>
      </div>
    </div>`}render(){return h`
      ${this.renderStrip()} ${this.renderHouseBias()}
      <div class="grid">${this.snap.rooms.map(e=>this.renderRoom(e))}</div>
    `}};O.styles=[M,y`
      .strip {
        background: var(--sol-card);
        border-radius: var(--sol-r-card);
        padding: 10px 14px;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        font-size: 12.5px;
        color: var(--sol-text-3);
        box-shadow: var(--sol-shadow);
      }
      .strip > div {
        display: flex;
        align-items: center;
        gap: 6px;
        padding: 0 14px;
        border-left: 1px solid rgba(255, 255, 255, 0.1);
      }
      .strip > div:first-child {
        border-left: none;
        padding-left: 0;
      }
      .strip ha-icon {
        --mdc-icon-size: 17px;
      }
      .strip b {
        color: var(--sol-text);
        font-weight: 500;
        font-variant-numeric: tabular-nums;
      }

      .house {
        background: var(--sol-card);
        border-radius: var(--sol-r-card);
        padding: 12px 16px;
        margin-top: 14px;
        display: flex;
        align-items: center;
        gap: 12px;
        box-shadow: var(--sol-shadow);
      }
      .house .name {
        display: flex;
        align-items: center;
        gap: 7px;
        font-size: 13.5px;
        font-weight: 500;
        flex: 0 0 auto;
      }
      .house ha-icon {
        --mdc-icon-size: 19px;
        color: var(--sol-cyan);
      }
      .readout {
        font-size: 13px;
        font-variant-numeric: tabular-nums;
        flex: 0 0 82px;
        text-align: right;
      }
      .cons {
        font-size: 11.5px;
        color: var(--sol-text-4);
        flex: 0 0 auto;
        white-space: nowrap;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(430px, 1fr));
        gap: 14px;
        align-items: start;
        margin-top: 14px;
      }
      @media (max-width: 500px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }

      .room {
        background: var(--sol-card);
        border-radius: var(--sol-r-card);
        padding: 14px 16px 12px;
        box-shadow: var(--sol-shadow);
      }
      .room-head {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .room-head ha-icon {
        --mdc-icon-size: 21px;
        color: var(--sol-faint);
      }
      .room-head ha-icon.lit {
        color: var(--sol-amber);
      }
      .room-head .title {
        flex: 1;
        min-width: 0;
      }
      .room-head h3 {
        margin: 0;
        font-size: 16px;
        font-weight: 500;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .room-head .sub {
        font-size: 11.5px;
        color: var(--sol-text-4);
      }

      .status {
        display: flex;
        gap: 18px;
        background: var(--sol-block);
        border-radius: var(--sol-r-block);
        padding: 9px 12px;
        margin-top: 11px;
      }
      .status div {
        display: flex;
        flex-direction: column;
        gap: 2px;
      }
      .status .v {
        font-size: 13px;
        font-variant-numeric: tabular-nums;
      }
      .status .v.amber {
        color: var(--sol-amber);
        font-weight: 500;
      }

      .manual-block {
        background: var(--sol-amber-surface);
        border-radius: var(--sol-r-block);
        padding: 10px 12px;
        margin-top: 11px;
      }
      .manual-block .top {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 12.5px;
        color: var(--sol-amber-light);
      }
      .manual-block ha-icon {
        --mdc-icon-size: 16px;
      }
      button.ghost {
        all: unset;
        cursor: pointer;
        font-size: 11.5px;
        padding: 4px 10px;
        border-radius: var(--sol-r-control);
        border: 1px solid rgba(255, 183, 77, 0.4);
        color: var(--sol-amber);
        white-space: nowrap;
        margin-left: auto;
      }
      .manual-block .note {
        font-size: 11px;
        color: var(--sol-text-4);
        margin-top: 6px;
      }

      .bias-row {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-top: 11px;
        font-size: 12.5px;
        color: var(--sol-text-2);
      }
      .bias-row .lab {
        flex: 0 0 92px;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .disclose {
        all: unset;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 6px;
        margin-top: 12px;
        font-size: 12px;
        color: var(--sol-text-3);
        border-radius: var(--sol-r-control);
      }
      .disclose ha-icon {
        --mdc-icon-size: 16px;
        transition: transform 0.15s ease;
      }
      .disclose ha-icon.open {
        transform: rotate(90deg);
      }

      .lights {
        margin-top: 8px;
      }
      .zone-head {
        display: flex;
        align-items: center;
        gap: 8px;
        background: var(--sol-block);
        border-radius: var(--sol-r-control);
        padding: 5px 9px;
        margin-top: 8px;
      }
      .zone-head sol-slider {
        max-width: 120px;
      }
      .zone-head .zv {
        font-size: 11px;
        color: var(--sol-text-4);
        min-width: 46px;
        text-align: right;
      }
      .zone-head.warnrow {
        background: var(--sol-amber-surface);
      }
      .lrow {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 152px 60px 60px;
        gap: 8px;
        align-items: center;
        padding: 8px 0;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
      }
      .lrow .lname {
        min-width: 0;
      }
      .lrow .lname .n {
        font-size: 12.5px;
        color: var(--sol-text-2);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .lrow .lname .s {
        font-size: 11px;
        color: var(--sol-text-4);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .lrow .lname .s .hw {
        color: var(--sol-amber);
      }
      .adj {
        display: flex;
        align-items: center;
        gap: 6px;
      }
      .adj .sv {
        font-size: 11.5px;
        font-variant-numeric: tabular-nums;
        color: var(--sol-text-4);
        flex: 0 0 38px;
        text-align: right;
      }
      .adj .sv.moved {
        color: var(--sol-text);
      }
      .lhead {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 152px 60px 60px;
        gap: 8px;
        align-items: center;
        padding-bottom: 4px;
      }
      .lhead .eyebrow {
        display: flex;
        align-items: center;
        gap: 4px;
      }
      .footer {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-top: 10px;
        padding-top: 9px;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        font-size: 12.5px;
        color: var(--sol-text-2);
      }
      .footer .lab {
        display: flex;
        align-items: center;
        gap: 5px;
      }
      .grow {
        flex: 1;
      }
      .pill {
        font-size: 10.5px;
        letter-spacing: 0.4px;
        text-transform: uppercase;
        padding: 2px 8px;
        border-radius: 10px;
        background: var(--sol-control);
        color: var(--sol-text-3);
      }
      .pill.on {
        background: var(--sol-cyan-tint);
        color: var(--sol-cyan);
      }
      .pill.off {
        color: var(--sol-faint);
      }
    `],d([m({attribute:!1})],O.prototype,"hass",2),d([m({attribute:!1})],O.prototype,"snap",2),d([T()],O.prototype,"open",2),d([T()],O.prototype,"draft",2),O=d([A("sol-tab-home")],O);var Ee=Math.PI/180,J=[[75,11e4],[60,9e4],[45,65e3],[30,4e4],[20,22e3],[12,11e3],[8,6e3],[5,3200],[2,1200],[0,420],[-2,130],[-4,42],[-6,11],[-9,1.6],[-12,.35],[-18,.002]],jt=n=>23.44*Ee*Math.sin(2*Math.PI*(284+n)/365);var qt=(n,s,e)=>{let t=jt(n),a=e*Ee,i=(Math.sin(s*Ee)-Math.sin(a)*Math.sin(t))/(Math.cos(a)*Math.cos(t));return i>=1||i<=-1?null:12+Math.acos(i)/Ee/15},Kt=n=>{let s=Math.max(.002,Math.min(11e4,n));for(let e=0;e<J.length-1;e++){let[t,a]=J[e],[i,r]=J[e+1];if(s<=a&&s>=r){let l=(Math.log(s)-Math.log(r))/(Math.log(a)-Math.log(r));return i+l*(t-i)}}return s>J[0][1]?J[0][0]:J[J.length-1][0]},Wt=n=>{let s=2*Math.PI*(n-81)/364;return 9.87*Math.sin(2*s)-7.53*Math.cos(s)-1.5*Math.sin(s)},Zt=(n,s)=>{try{s||(s=Intl.DateTimeFormat().resolvedOptions().timeZone);let e=new Intl.DateTimeFormat("en-GB",{timeZone:s,hour12:!1,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit"}).formatToParts(new Date(n)),t=i=>Number(e.find(r=>r.type===i)?.value??0);return(Date.UTC(t("year"),t("month")-1,t("day"),t("hour")%24,t("minute"),t("second"))-n)/36e5}catch{return 0}},Te=(n,s,e)=>{let t=qt(n,s,e.lat);if(t===null)return null;let i=12-e.lon/15-Wt(n)/60+(t-12),l=Date.UTC(e.year,0,1)+(n-1)*864e5+i*36e5;return(i+Zt(l,e.timeZone)+24)%24},Y=(n,s,e)=>Te(n,Kt(s),e);var je={ambience_start_lux:"Falling edge. At or below this outdoor level it counts as dark: the ambience glow may appear, and the minimum cutoff drops out. It does NOT decide whether normal lighting runs \u2014 demand and occupancy do that.",ambience_stop_lux:"Rising edge. At or above this it counts as bright again and the glow goes. The gap between the two thresholds is deliberate hysteresis so the boundary does not flicker.",ambience_debounce_falling_s:"Extra delay before it reads dark. Leave at 0 unless you have a reason: the mmWave sensors already impose a 15 s minimum in hardware and zigbee2mqtt adds more on top.",ambience_debounce_rising_s:"Extra delay before it reads bright again. Same warning \u2014 stacking three minutes on top of the hardware delay is three minutes of lag and then a mystery.",lux_full:"The outdoor level at or below which the room wants full light. The bottom of the demand curve.",lux_window:"How far above the full point demand fades to nothing. Full point plus window IS the outdoor level at which normal lighting goes out \u2014 nothing else overrides it. Below that, an occupied room lights in proportion to how dark it is.",min_cutoff:"Below this the light goes off rather than sitting at a useless glow competing with daylight. Ignored once it is dark \u2014 after dark a level of 1 is the point, not a waste \u2014 so this only bites in daylight.",night_level:"A fixed level while night mode is latched \u2014 not a scaling. Fixed so it is predictable when you are half asleep.",night_release_lux:"Night mode ends when it gets this light outside. Getting out of bed does not end it; that is deliberate, so a 3 am trip does not relight the house at full demand.",alarm_lead_minutes:"Night mode also ends this long before your next alarm, whichever comes first.",ambience_level:"What a light shows INSTEAD OF BEING OFF, once it is dark outside and you are awake. It replaces off \u2014 it never dims a light that is already doing its job. 0 turns the feature off everywhere.",ambience_ignores_occupancy:"On: the ambience glow stays in a room nobody is in. Off: it needs occupancy like everything else.",rate_limit_step:"The most the level may move in one tick while tracking daylight. It stops the bulb visibly chasing every wobble in the lux reading. It never applies to switching on or off \u2014 those are commands, and the transition handles their smoothness.",dead_zone:"A change smaller than this writes nothing at all, so the mesh is not spammed.",update_interval_min_s:"How often to recalculate while the outdoor light is moving fast.",update_interval_home_s:"How often to recalculate when things are stable but someone is home.",update_interval_max_s:"How often to recalculate when things are stable and the house is empty.",lux_volatility_lx:"How much the outdoor reading has to swing across recent samples before the engine counts the world as moving and speeds up.",transition_on_s:"Fade length when a light goes from off to on.",transition_off_s:"Fade length when a light goes out.",transition_mode_s:"Fade length when the mode changes, and for ordinary daylight tracking.",transition_setting_s:"Fade length while you are dragging a slider on this page. Keep it short \u2014 a long glide here makes the whole panel feel broken.",morning_release_hour:"When the evening ramp lets go and the day curve takes back over. Without it the ramp would hold its last value all the next day.",gamma:"Display only. Converts a command level into the light your eye actually perceives, so the panel can say 'level 102 (18 % light)'. It is deliberately out of the command path."},U=class extends ${constructor(){super(...arguments);this.draft={};this.placed=new Set}schema(e){return this.placed.add(e),this.snap.house_schema.find(t=>t.key===e)}value(e){return this.draft[e]??this.snap.house[e]??this.schema(e)?.default??0}async push(e,t,a=!0){this.draft={...this.draft,[e]:t},await te(this.hass,{[e]:t}),a&&window.setTimeout(()=>{let i={...this.draft};delete i[e],this.draft=i},700)}row(e,t){let a=this.schema(e);if(!a)return p;let i=this.value(e);return h`<sol-row .label=${a.name} .help=${je[e]??""}>
      <sol-slider
        noReset
        .value=${i}
        .min=${a.min}
        .max=${a.max}
        .step=${a.step}
        .neutral=${a.default}
        @value-changed=${r=>this.push(e,r.detail.value,r.detail.final)}
      ></sol-slider>
      <span slot="value"
        >${t?t(i):`${g(i,a.step<1?2:0)}${a.unit?` ${a.unit}`:""}`}</span
      >
    </sol-row>`}numberRow(e){let t=this.schema(e);return t?h`<sol-row .label=${t.name} .help=${je[e]??""}>
      <span></span>
      <sol-number
        slot="value"
        .value=${this.value(e)}
        .min=${t.min}
        .max=${t.max}
        .step=${t.step}
        .width=${64}
        suffix=${t.unit??""}
        @value-changed=${a=>this.push(e,a.detail.value)}
      ></sol-number>
    </sol-row>`:p}demandChart(){let e=Math.max(this.value("lux_full"),.1),t=e+Math.max(this.value("lux_window"),1),a=this.value("ambience_start_lux"),i=[];for(let l=0;l<=160;l++){let c=Math.exp(Math.log(.1)+l/160*(Math.log(t*1.2)-Math.log(.1))),b=c<=e?1:c>=t?0:1-Math.log(c/e)/Math.log(t/e);i.push([Math.log10(c),b])}let r=[];for(let l=-1;l<=Math.ceil(Math.log10(t*1.2));l++)r.push({value:l,label:l<0?"0.1":g(Math.pow(10,l))});return h`<sol-chart
      .series=${[{points:i,colour:"var(--sol-series-start)",width:2.6}]}
      .refLines=${[{x:Math.log10(Math.max(a,.1)),label:`dark ${g(a)} lx`,colour:"rgba(240,98,146,.55)",textColour:"var(--sol-series-full)"}]}
      .xTicks=${r}
      .yTicks=${[0,.25,.5,.75,1].map(l=>({value:l,label:`${l*100} %`}))}
      .xDomain=${[-1,Math.ceil(Math.log10(t*1.2))]}
      .yDomain=${[0,1]}
      .shade=${[-1,Math.log10(Math.max(a,.1))]}
      .shadeLabel=${"dark \u2014 cutoff drops out, glow allowed"}
      xTitle="outdoor lux"
      .facts=${[{label:"Full at",value:`${g(e,1)} lx`},{label:"Out at",value:`${g(t)} lx`},{label:"Counts as dark",value:`${g(a)} lx`},{label:"Bright again",value:`${g(this.value("ambience_stop_lux"))} lx`},{label:"Now",value:g(this.snap.world.lux,1)+" lx"}]}
    ></sol-chart>`}yearChart(){let e=this.snap.world,t={lat:e.latitude,lon:e.longitude,timeZone:e.time_zone||Intl.DateTimeFormat().resolvedOptions().timeZone,year:e.year||new Date().getFullYear()},a=e.latitude,i=this.value("ambience_start_lux"),r=Math.max(this.value("lux_full"),.1),l=this.value("ambience_level")>0?i:null,c=[],b=[],v=[],o=[],u=[];for(let x=1;x<=365;x+=5){let fe=Y(x,i,t),xt=Y(x,r,t),$t=Te(x,-.833,t),yt=Te(x,-6,t);c.push([x,fe??NaN]),b.push([x,xt??NaN]),v.push([x,$t??NaN]),o.push([x,yt??NaN]),l!==null&&u.push([x,Y(x,l,t)??NaN])}let f=[...c,...b,...v,...o,...u].map(([,x])=>x).filter(x=>isFinite(x)),S=Math.floor(Math.min(...f,16)),R=Math.ceil(Math.max(...f,23)),X=[];for(let x=S;x<=R;x++)X.push({value:x,label:_(x)});let ie=[1,32,60,91,121,152,182,213,244,274,305,335,366],ve="Jan Feb Mar Apr May Jun Jul Aug Sep Oct Nov Dec".split(" ").map((x,fe)=>({value:(ie[fe]+ie[fe+1])/2,label:x})),ne=e.day_of_year,re=Y(ne,i,t),ze=Y(ne,r,t),qe=l===null?null:Y(ne,l,t),Ke=re!==null&&ze!==null?Math.round((ze-re)*60):null,We=[{points:v,colour:"var(--sol-series-sunset)",width:1.6},{points:o,colour:"var(--sol-series-dusk)",width:1.6},{points:b,colour:"var(--sol-series-full)",width:1.6},{points:c,colour:"var(--sol-series-start)",width:2.6}];return u.length&&We.unshift({points:u,colour:"var(--sol-series-ambience)",width:1.6,dashed:!0}),h`<sol-chart
      .series=${We}
      .refLines=${[{x:ne,label:"today",colour:"rgba(255,255,255,.25)",textColour:"var(--sol-text-3)"}]}
      .xTicks=${ve}
      .yTicks=${X}
      .xDomain=${[1,365]}
      .yDomain=${[S,R]}
      .marker=${re!==null?{x:ne,y:re}:null}
      .facts=${[{label:"Lighting starts",value:_(re)},{label:"Ambience on",value:qe===null?"off":_(qe)},{label:"Lights full",value:_(ze)},{label:"On threshold",value:`${g(i)} lx`},{label:"Full at",value:`${g(r,1)} lx`},{label:"Descent to full",value:Ke===null?"\u2014":`${Ke} min`},{label:"Latitude",value:`${g(a,2)}\xB0`}]}
    ></sol-chart>`}rampChart(){let e=[...this.snap.ramp].sort((u,f)=>(u.hour-18+24)%24-(f.hour-18+24)%24);if(!e.length)return p;let t=u=>(u-18+24)%24,a=t(this.value("morning_release_hour")),i=Math.max(this.value("ramp_onset_minutes"),0)/60,r=this.snap.house.gamma??2.39,l=u=>{if(u>=a)return 0;let f=t(e[0].hour);if(u<f-i)return 0;if(u<f)return i<=0?0:(u-(f-i))/i*e[0].stops;let S=t(e[e.length-1].hour);if(u>=S)return e[e.length-1].stops;for(let R=0;R<e.length-1;R++){let X=t(e[R].hour),ie=t(e[R+1].hour);if(X<=u&&u<=ie){let ve=ie-X;return ve<=0?e[R+1].stops:e[R].stops+(u-X)/ve*(e[R+1].stops-e[R].stops)}}return 0},c=Math.max(1,...this.snap.rooms.map(u=>u.level??0),161),b=[];for(let u=0;u<=288;u++){let f=u/288*a;b.push([f,c*Math.pow(2,l(f))])}let v=[];for(let u=0;u<=a;u+=2)v.push({value:u,label:_((u+18)%24)});let o=Math.max(c,...b.map(([,u])=>u));return h`<sol-chart
      .series=${[{points:b,colour:"var(--sol-series-start)",width:2.6}]}
      .refLines=${e.map(u=>({x:t(u.hour),label:`${_(u.hour)} ${C(u.stops)}`,colour:"rgba(255,183,77,.45)",textColour:"var(--sol-amber)"}))}
      .xTicks=${v}
      .yTicks=${[0,.25,.5,.75,1].map(u=>({value:u*o,label:String(Math.round(u*o))}))}
      .xDomain=${[0,a]}
      .yDomain=${[0,o]}
      xTitle="evening → morning release"
      .facts=${[{label:"Points",value:String(e.length)},{label:"Onset",value:`${g(this.value("ramp_onset_minutes"))} min`},...e.map((u,f)=>({label:`Point ${f+1}`,value:`${_(u.hour)} \xB7 ${C(u.stops)}`})),{label:"At the last point",value:`level ${Math.round(c*Math.pow(2,e[e.length-1].stops))} of ${c}`},{label:"Reference room",value:`${c} (${D(c,r)} % light)`}]}
    ></sol-chart>`}renderRamp(){let e=this.snap.ramp,t=a=>dt(this.hass,a);return h`
      <div class="ramp">
        <span class="eyebrow">Time</span>
        <span class="eyebrow">Bias</span>
        <span></span>
        ${e.map((a,i)=>h`
            <input
              type="time"
              .value=${_(a.hour)}
              @change=${r=>{let l=e.map((c,b)=>b===i?{...c,hour:bt(r.target.value)}:c);t(l)}}
            />
            <sol-slider
              .value=${a.stops}
              .min=${-4}
              .max=${1}
              .step=${.25}
              @value-changed=${r=>{r.detail.final&&t(e.map((l,c)=>c===i?{...l,stops:r.detail.value}:l))}}
            ></sol-slider>
            <button
              class="icon"
              title="Remove this point"
              @click=${()=>t(e.filter((r,l)=>l!==i))}
            >
              <ha-icon icon="mdi:close"></ha-icon>
            </button>
            <span class="caption" style="grid-column:1/-1;margin-top:-6px">
              ${_(a.hour)} → ${C(a.stops)}
            </span>
          `)}
      </div>
      <button
        class="add"
        @click=${()=>t([...e,{hour:e.length?(e[e.length-1].hour+1)%24:20,stops:e.length?e[e.length-1].stops-.5:-.5}])}
      >
        <ha-icon icon="mdi:plus"></ha-icon> Add a ramp point
      </button>
      <div class="caption" style="margin-top:8px">
        The ramp glides continuously between points, so it needs no transition of its own. It holds
        the last point until the morning release.
      </div>
    `}renderAdvanced(){let e=this.snap.house_schema.filter(t=>!this.placed.has(t.key));return e.length?h`<div class="card full">
      <div class="card-head">
        <ha-icon icon="mdi:wrench-outline"></ha-icon>
        <h2>Advanced</h2>
      </div>
      <div class="caption" style="margin-bottom:8px">
        Measured constants and plumbing. They have sensible defaults and you should not
        need them — but nothing in this engine is hardcoded, so they are here rather than
        buried in the source.
      </div>
      <div class="adv-grid">
        ${e.map(t=>this.numberRow(t.key))}
      </div>
    </div>`:p}render(){this.placed=new Set;let e=this.value("ambience_start_lux"),t=this.value("ambience_stop_lux"),i=this.value("lux_full")+this.value("lux_window"),r=this.value("ambience_debounce_falling_s")>0||this.value("ambience_debounce_rising_s")>0;return h`<div class="grid">
      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:chart-bell-curve-cumulative"></ha-icon>
          <h2>Demand curve</h2>
        </div>
        ${this.row("lux_full")} ${this.row("lux_window")} ${this.row("min_cutoff")}
        <div class="caption">
          Lights out at ${g(i)} lx — full point plus window.
        </div>
        <div class="sub">${this.demandChart()}</div>
        ${e<i?h`<div class="warn">
              <ha-icon icon="mdi:alert-outline"></ha-icon>
              <span>
                The gate shuts at ${g(e)} lx but demand does not reach zero until
                ${g(i)} lx, so the shaded part of the curve can never be seen. Neither number is
                wrong — they answer different questions — but they have to be tuned as a pair.
              </span>
            </div>`:p}
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:theme-light-dark"></ha-icon>
          <h2>Ambience — the evening glow</h2>
        </div>
        <div class="caption" style="margin-bottom:6px">
          <b>These thresholds do not decide whether normal lighting runs.</b> That is the demand
          curve above, plus occupancy. These two numbers only say when it is dark enough for the
          resting glow — and they do one other thing: below them the minimum cutoff drops out, so
          lights can reach 1 after dark instead of stopping at
          ${g(this.value("min_cutoff"))}.
        </div>
        ${this.row("ambience_start_lux")} ${this.row("ambience_stop_lux")}
        <div class="caption">
          Hysteresis gap: ${g(Math.max(0,t-e))} lx — the boundary does not
          flicker.
        </div>
        <div class="sub">
          ${this.row("ambience_level")}
          <sol-row
            .label=${"Ambience in empty rooms"}
            .help=${je.ambience_ignores_occupancy}
          >
            <label class="toggle">
              <input
                type="checkbox"
                .checked=${this.value("ambience_ignores_occupancy")>=1}
                @change=${l=>this.push("ambience_ignores_occupancy",l.target.checked?1:0)}
              />
              <span
                >${this.value("ambience_ignores_occupancy")>=1?"glow stays in empty rooms":"needs occupancy"}</span
              >
            </label>
          </sol-row>
        </div>
        <div class="sub">
          <div class="eyebrow" style="margin-bottom:4px">Debounce (seconds)</div>
          <div class="caption">
            Leave these at 0 unless you have a measured reason. The mmWave sensors already impose a
            15 s minimum in hardware, and zigbee2mqtt adds more on top of that.
          </div>
          ${this.row("ambience_debounce_falling_s")} ${this.row("ambience_debounce_rising_s")}
          ${r?h`<div class="warn">
                <ha-icon icon="mdi:alert-outline"></ha-icon>
                <span>A debounce is set. Every second here stacks on top of the hardware delay.</span>
              </div>`:p}
        </div>
      </div>

      <div class="card full">
        <div class="card-head">
          <ha-icon icon="mdi:calendar-month"></ha-icon>
          <h2>When lighting starts, across the year</h2>
        </div>
        ${this.yearChart()}
        <div class="caption" style="margin-top:10px">
          Estimated from solar geometry for a clear sky, so it answers "what will happen in March"
          — a question <code>sun.sun</code> cannot. It lands within a few minutes of the real sun
          near midsummer and about ten in deep winter. The engine itself never uses this: it reads
          civil dusk from <code>sun.sun</code>.
        </div>
      </div>

      <div class="card full">
        <div class="card-head">
          <ha-icon icon="mdi:ray-start-arrow"></ha-icon>
          <h2>Evening ramp</h2>
        </div>
        <div class="caption" style="margin-bottom:10px">
          An ordered list of points, not two fixed phases — add as many as you want.
        </div>
        ${this.renderRamp()}
        <div class="sub">
          ${this.row("ramp_onset_minutes")}
          ${this.row("morning_release_hour",l=>_(l))}
        </div>
        <div class="sub">${this.rampChart()}</div>
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:weather-night"></ha-icon>
          <h2>Night mode</h2>
        </div>
        <div class="caption" style="margin-bottom:6px">
          Night latches when you fall asleep and lets go on the world's terms, not your posture —
          getting up at 3 am does not end it.
        </div>
        ${this.row("night_level")} ${this.row("night_release_lux")} ${this.row("alarm_lead_minutes")}
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:speedometer"></ha-icon>
          <h2>Timing &amp; transitions</h2>
        </div>
        ${this.numberRow("update_interval_min_s")} ${this.numberRow("update_interval_home_s")}
        ${this.numberRow("update_interval_max_s")} ${this.numberRow("lux_volatility_lx")}
        ${this.numberRow("dead_zone")} ${this.numberRow("rate_limit_step")}
        <div class="sub">
          <div class="eyebrow" style="margin-bottom:4px">Transition speeds (seconds)</div>
          ${this.numberRow("transition_on_s")} ${this.numberRow("transition_off_s")}
          ${this.numberRow("transition_mode_s")} ${this.numberRow("transition_setting_s")}
        </div>
        <div class="sub">${this.row("gamma")}</div>
      </div>

      ${this.renderAdvanced()}
    </div>`}};U.styles=[M,y`
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(440px, 1fr));
        gap: 14px;
      }
      @media (max-width: 520px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }
      .full {
        grid-column: 1 / -1;
      }
      .sub {
        margin-top: 14px;
        padding-top: 12px;
        border-top: 1px solid var(--sol-hair);
      }
      .ramp {
        display: grid;
        grid-template-columns: 1fr 1fr auto;
        gap: 10px;
        align-items: center;
      }
      .ramp .eyebrow {
        padding-bottom: 2px;
      }
      input[type="time"] {
        font: inherit;
        font-size: 12.5px;
        color: var(--sol-text);
        background: var(--sol-control);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: var(--sol-r-control);
        padding: 6px 8px;
        width: 100%;
        box-sizing: border-box;
      }
      button.icon {
        all: unset;
        cursor: pointer;
        color: var(--sol-text-4);
        display: inline-flex;
        border-radius: var(--sol-r-control);
      }
      button.icon:hover {
        color: var(--sol-amber);
      }
      button.add {
        all: unset;
        cursor: pointer;
        margin-top: 10px;
        font-size: 12px;
        color: var(--sol-cyan);
        display: inline-flex;
        align-items: center;
        gap: 5px;
        border-radius: var(--sol-r-control);
      }
      .adv-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(330px, 1fr));
        gap: 0 24px;
      }
      .toggle {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 12.5px;
        color: var(--sol-text-2);
        cursor: pointer;
      }
      .warn {
        display: flex;
        gap: 8px;
        align-items: flex-start;
        margin-top: 10px;
        padding: 9px 11px;
        background: var(--sol-amber-surface);
        border-radius: var(--sol-r-block);
        font-size: 11.5px;
        line-height: 1.5;
        color: var(--sol-amber-light);
      }
      code {
        background: var(--sol-control);
        border-radius: 4px;
        padding: 0 4px;
        font-size: 11px;
      }
      .warn ha-icon {
        --mdc-icon-size: 16px;
        flex: 0 0 auto;
        color: var(--sol-amber);
      }
    `],d([m({attribute:!1})],U.prototype,"hass",2),d([m({attribute:!1})],U.prototype,"snap",2),d([T()],U.prototype,"draft",2),U=d([A("sol-tab-lighting")],U);var Vt=[{id:"home",label:"Home"},{id:"lighting",label:"Lighting"},{id:"colour",label:"Colour"}],L=class extends ${constructor(){super(...arguments);this.narrow=!1;this.tab="home";this.snap=null;this.error=null}connectedCallback(){super.connectedCallback(),this.connect()}disconnectedCallback(){super.disconnectedCallback(),this.unsub?.(),this.unsub=void 0}updated(e){e.has("hass")&&this.hass&&!this.unsub&&this.connect()}async connect(){if(!(!this.hass?.connection||this.unsub))try{this.unsub=await ht(this.hass,e=>{this.snap=e,this.error=null})}catch(e){this.error=e?.message??"Solace is not set up yet, or the integration failed to load."}}renderTab(){if(!this.snap)return p;switch(this.tab){case"lighting":return h`<sol-tab-lighting .hass=${this.hass} .snap=${this.snap}></sol-tab-lighting>`;case"colour":return h`<sol-tab-colour .hass=${this.hass} .snap=${this.snap}></sol-tab-colour>`;default:return h`<sol-tab-home .hass=${this.hass} .snap=${this.snap}></sol-tab-home>`}}render(){let e=this.snap?.world.healthy??!1;return h`
      <header>
        <div class="bar">
          <ha-icon class="brand" icon="mdi:tune"></ha-icon>
          <h1>Lighting Engine</h1>
          ${this.snap?h`<span class="pill">
                <span class="dot ${e?"":"bad"}"></span>
                ${e?"Engine running":"Engine stalled"}
              </span>`:p}
        </div>
        <nav role="tablist">
          ${Vt.map(t=>h`<button
              role="tab"
              aria-selected=${this.tab===t.id}
              @click=${()=>this.tab=t.id}
            >
              ${t.label}
            </button>`)}
        </nav>
      </header>
      <main>
        ${this.snap?this.renderTab():h`<div class="placeholder">
              <ha-icon icon=${this.error?"mdi:alert-circle-outline":"mdi:progress-clock"}></ha-icon>
              <p>
                ${this.error??"Waiting for the engine\u2026"}
              </p>
              ${this.error?h`<p>
                    Add the integration under <code>Settings → Devices &amp; services</code>, then
                    add a room for each zone you want Solace to drive.
                  </p>`:p}
            </div>`}
      </main>
    `}};L.styles=[M,y`
      :host {
        background: var(--sol-page);
        min-height: 100vh;
      }
      header {
        position: sticky;
        top: 0;
        z-index: 10;
        background: var(--sol-card);
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.4);
      }
      .bar {
        height: 56px;
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 0 18px;
        box-sizing: border-box;
      }
      .bar ha-icon.brand {
        --mdc-icon-size: 22px;
        color: var(--sol-text-3);
      }
      h1 {
        margin: 0;
        font-size: 19px;
        font-weight: 400;
        flex: 1;
        min-width: 0;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 7px;
        font-size: 12px;
        color: var(--sol-text-3);
        background: var(--sol-control);
        border-radius: 12px;
        padding: 5px 11px;
        white-space: nowrap;
      }
      .dot {
        width: 7px;
        height: 7px;
        border-radius: 50%;
        background: var(--sol-green);
      }
      .dot.bad {
        background: var(--error-color, #ef5350);
      }
      nav {
        display: flex;
        gap: 0;
        padding: 0 18px 12px;
      }
      nav button {
        all: unset;
        cursor: pointer;
        font-size: 13.5px;
        font-weight: 500;
        letter-spacing: 0.6px;
        text-transform: uppercase;
        color: var(--sol-text-3);
        padding: 4px 14px 8px;
        border-bottom: 2px solid transparent;
      }
      nav button[aria-selected="true"] {
        color: var(--sol-cyan);
        border-bottom-color: var(--sol-cyan);
      }
      main {
        max-width: 1180px;
        margin: 0 auto;
        padding: 18px 20px 48px;
      }
      .placeholder {
        max-width: 560px;
        margin: 64px auto;
        text-align: center;
        color: var(--sol-text-3);
        font-size: 13.5px;
        line-height: 1.7;
      }
      .placeholder ha-icon {
        --mdc-icon-size: 40px;
        color: var(--sol-text-4);
      }
      code {
        background: var(--sol-control);
        border-radius: 4px;
        padding: 1px 5px;
        font-size: 12px;
      }
    `],d([m({attribute:!1})],L.prototype,"hass",2),d([m({type:Boolean})],L.prototype,"narrow",2),d([T()],L.prototype,"tab",2),d([T()],L.prototype,"snap",2),d([T()],L.prototype,"error",2),L=d([A("solace-panel")],L);export{L as SolacePanel};
