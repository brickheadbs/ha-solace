/* Solace lighting panel — generated by panel/build.mjs. Do not edit by hand. */
var zt=Object.defineProperty;var Ct=Object.getOwnPropertyDescriptor;var h=(n,s,e,t)=>{for(var a=t>1?void 0:t?Ct(s,e):s,i=n.length-1,o;i>=0;i--)(o=n[i])&&(a=(t?o(s,e,a):o(a))||a);return t&&a&&zt(s,e,a),a};var xe=globalThis,$e=xe.ShadowRoot&&(xe.ShadyCSS===void 0||xe.ShadyCSS.nativeShadow)&&"adoptedStyleSheets"in Document.prototype&&"replace"in CSSStyleSheet.prototype,ze=Symbol(),Ge=new WeakMap,oe=class{constructor(s,e,t){if(this._$cssResult$=!0,t!==ze)throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");this.cssText=s,this.t=e}get styleSheet(){let s=this.o,e=this.t;if($e&&s===void 0){let t=e!==void 0&&e.length===1;t&&(s=Ge.get(e)),s===void 0&&((this.o=s=new CSSStyleSheet).replaceSync(this.cssText),t&&Ge.set(e,s))}return s}toString(){return this.cssText}},Je=n=>new oe(typeof n=="string"?n:n+"",void 0,ze),$=(n,...s)=>{let e=n.length===1?n[0]:s.reduce((t,a,i)=>t+(o=>{if(o._$cssResult$===!0)return o.cssText;if(typeof o=="number")return o;throw Error("Value passed to 'css' function must be a 'css' function result: "+o+". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.")})(a)+n[i+1],n[0]);return new oe(e,n,ze)},Xe=(n,s)=>{if($e)n.adoptedStyleSheets=s.map(e=>e instanceof CSSStyleSheet?e:e.styleSheet);else for(let e of s){let t=document.createElement("style"),a=xe.litNonce;a!==void 0&&t.setAttribute("nonce",a),t.textContent=e.cssText,n.appendChild(t)}},Ce=$e?n=>n:n=>n instanceof CSSStyleSheet?(s=>{let e="";for(let t of s.cssRules)e+=t.cssText;return Je(e)})(n):n;var{is:St,defineProperty:Lt,getOwnPropertyDescriptor:Pt,getOwnPropertyNames:Ht,getOwnPropertySymbols:Nt,getPrototypeOf:Dt}=Object,ye=globalThis,Ye=ye.trustedTypes,Ot=Ye?Ye.emptyScript:"",Ft=ye.reactiveElementPolyfillSupport,re=(n,s)=>n,le={toAttribute(n,s){switch(s){case Boolean:n=n?Ot:null;break;case Object:case Array:n=n==null?n:JSON.stringify(n)}return n},fromAttribute(n,s){let e=n;switch(s){case Boolean:e=n!==null;break;case Number:e=n===null?null:Number(n);break;case Object:case Array:try{e=JSON.parse(n)}catch{e=null}}return e}},_e=(n,s)=>!St(n,s),Qe={attribute:!0,type:String,converter:le,reflect:!1,useDefault:!1,hasChanged:_e};Symbol.metadata??=Symbol("metadata"),ye.litPropertyMetadata??=new WeakMap;var S=class extends HTMLElement{static addInitializer(s){this._$Ei(),(this.l??=[]).push(s)}static get observedAttributes(){return this.finalize(),this._$Eh&&[...this._$Eh.keys()]}static createProperty(s,e=Qe){if(e.state&&(e.attribute=!1),this._$Ei(),this.prototype.hasOwnProperty(s)&&((e=Object.create(e)).wrapped=!0),this.elementProperties.set(s,e),!e.noAccessor){let t=Symbol(),a=this.getPropertyDescriptor(s,t,e);a!==void 0&&Lt(this.prototype,s,a)}}static getPropertyDescriptor(s,e,t){let{get:a,set:i}=Pt(this.prototype,s)??{get(){return this[e]},set(o){this[e]=o}};return{get:a,set(o){let l=a?.call(this);i?.call(this,o),this.requestUpdate(s,l,t)},configurable:!0,enumerable:!0}}static getPropertyOptions(s){return this.elementProperties.get(s)??Qe}static _$Ei(){if(this.hasOwnProperty(re("elementProperties")))return;let s=Dt(this);s.finalize(),s.l!==void 0&&(this.l=[...s.l]),this.elementProperties=new Map(s.elementProperties)}static finalize(){if(this.hasOwnProperty(re("finalized")))return;if(this.finalized=!0,this._$Ei(),this.hasOwnProperty(re("properties"))){let e=this.properties,t=[...Ht(e),...Nt(e)];for(let a of t)this.createProperty(a,e[a])}let s=this[Symbol.metadata];if(s!==null){let e=litPropertyMetadata.get(s);if(e!==void 0)for(let[t,a]of e)this.elementProperties.set(t,a)}this._$Eh=new Map;for(let[e,t]of this.elementProperties){let a=this._$Eu(e,t);a!==void 0&&this._$Eh.set(a,e)}this.elementStyles=this.finalizeStyles(this.styles)}static finalizeStyles(s){let e=[];if(Array.isArray(s)){let t=new Set(s.flat(1/0).reverse());for(let a of t)e.unshift(Ce(a))}else s!==void 0&&e.push(Ce(s));return e}static _$Eu(s,e){let t=e.attribute;return t===!1?void 0:typeof t=="string"?t:typeof s=="string"?s.toLowerCase():void 0}constructor(){super(),this._$Ep=void 0,this.isUpdatePending=!1,this.hasUpdated=!1,this._$Em=null,this._$Ev()}_$Ev(){this._$ES=new Promise(s=>this.enableUpdating=s),this._$AL=new Map,this._$E_(),this.requestUpdate(),this.constructor.l?.forEach(s=>s(this))}addController(s){(this._$EO??=new Set).add(s),this.renderRoot!==void 0&&this.isConnected&&s.hostConnected?.()}removeController(s){this._$EO?.delete(s)}_$E_(){let s=new Map,e=this.constructor.elementProperties;for(let t of e.keys())this.hasOwnProperty(t)&&(s.set(t,this[t]),delete this[t]);s.size>0&&(this._$Ep=s)}createRenderRoot(){let s=this.shadowRoot??this.attachShadow(this.constructor.shadowRootOptions);return Xe(s,this.constructor.elementStyles),s}connectedCallback(){this.renderRoot??=this.createRenderRoot(),this.enableUpdating(!0),this._$EO?.forEach(s=>s.hostConnected?.())}enableUpdating(s){}disconnectedCallback(){this._$EO?.forEach(s=>s.hostDisconnected?.())}attributeChangedCallback(s,e,t){this._$AK(s,t)}_$ET(s,e){let t=this.constructor.elementProperties.get(s),a=this.constructor._$Eu(s,t);if(a!==void 0&&t.reflect===!0){let i=(t.converter?.toAttribute!==void 0?t.converter:le).toAttribute(e,t.type);this._$Em=s,i==null?this.removeAttribute(a):this.setAttribute(a,i),this._$Em=null}}_$AK(s,e){let t=this.constructor,a=t._$Eh.get(s);if(a!==void 0&&this._$Em!==a){let i=t.getPropertyOptions(a),o=typeof i.converter=="function"?{fromAttribute:i.converter}:i.converter?.fromAttribute!==void 0?i.converter:le;this._$Em=a;let l=o.fromAttribute(e,i.type);this[a]=l??this._$Ej?.get(a)??l,this._$Em=null}}requestUpdate(s,e,t,a=!1,i){if(s!==void 0){let o=this.constructor;if(a===!1&&(i=this[s]),t??=o.getPropertyOptions(s),!((t.hasChanged??_e)(i,e)||t.useDefault&&t.reflect&&i===this._$Ej?.get(s)&&!this.hasAttribute(o._$Eu(s,t))))return;this.C(s,e,t)}this.isUpdatePending===!1&&(this._$ES=this._$EP())}C(s,e,{useDefault:t,reflect:a,wrapped:i},o){t&&!(this._$Ej??=new Map).has(s)&&(this._$Ej.set(s,o??e??this[s]),i!==!0||o!==void 0)||(this._$AL.has(s)||(this.hasUpdated||t||(e=void 0),this._$AL.set(s,e)),a===!0&&this._$Em!==s&&(this._$Eq??=new Set).add(s))}async _$EP(){this.isUpdatePending=!0;try{await this._$ES}catch(e){Promise.reject(e)}let s=this.scheduleUpdate();return s!=null&&await s,!this.isUpdatePending}scheduleUpdate(){return this.performUpdate()}performUpdate(){if(!this.isUpdatePending)return;if(!this.hasUpdated){if(this.renderRoot??=this.createRenderRoot(),this._$Ep){for(let[a,i]of this._$Ep)this[a]=i;this._$Ep=void 0}let t=this.constructor.elementProperties;if(t.size>0)for(let[a,i]of t){let{wrapped:o}=i,l=this[a];o!==!0||this._$AL.has(a)||l===void 0||this.C(a,void 0,i,l)}}let s=!1,e=this._$AL;try{s=this.shouldUpdate(e),s?(this.willUpdate(e),this._$EO?.forEach(t=>t.hostUpdate?.()),this.update(e)):this._$EM()}catch(t){throw s=!1,this._$EM(),t}s&&this._$AE(e)}willUpdate(s){}_$AE(s){this._$EO?.forEach(e=>e.hostUpdated?.()),this.hasUpdated||(this.hasUpdated=!0,this.firstUpdated(s)),this.updated(s)}_$EM(){this._$AL=new Map,this.isUpdatePending=!1}get updateComplete(){return this.getUpdateComplete()}getUpdateComplete(){return this._$ES}shouldUpdate(s){return!0}update(s){this._$Eq&&=this._$Eq.forEach(e=>this._$ET(e,this[e])),this._$EM()}updated(s){}firstUpdated(s){}};S.elementStyles=[],S.shadowRootOptions={mode:"open"},S[re("elementProperties")]=new Map,S[re("finalized")]=new Map,Ft?.({ReactiveElement:S}),(ye.reactiveElementVersions??=[]).push("2.1.2");var Oe=globalThis,et=n=>n,we=Oe.trustedTypes,tt=we?we.createPolicy("lit-html",{createHTML:n=>n}):void 0,rt="$lit$",D=`lit$${Math.random().toFixed(9).slice(2)}$`,lt="?"+D,Ut=`<${lt}>`,q=document,de=()=>q.createComment(""),he=n=>n===null||typeof n!="object"&&typeof n!="function",Fe=Array.isArray,Bt=n=>Fe(n)||typeof n?.[Symbol.iterator]=="function",Se=`[ 	
\f\r]`,ce=/<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g,st=/-->/g,at=/>/g,I=RegExp(`>|${Se}(?:([^\\s"'>=/]+)(${Se}*=${Se}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`,"g"),it=/'/g,nt=/"/g,ct=/^(?:script|style|textarea|title)$/i,Ue=n=>(s,...e)=>({_$litType$:n,strings:s,values:e}),d=Ue(1),Z=Ue(2),os=Ue(3),W=Symbol.for("lit-noChange"),p=Symbol.for("lit-nothing"),ot=new WeakMap,K=q.createTreeWalker(q,129);function dt(n,s){if(!Fe(n)||!n.hasOwnProperty("raw"))throw Error("invalid template strings array");return tt!==void 0?tt.createHTML(s):s}var jt=(n,s)=>{let e=n.length-1,t=[],a,i=s===2?"<svg>":s===3?"<math>":"",o=ce;for(let l=0;l<e;l++){let c=n[l],g,u,r=-1,f=0;for(;f<c.length&&(o.lastIndex=f,u=o.exec(c),u!==null);)f=o.lastIndex,o===ce?u[1]==="!--"?o=st:u[1]!==void 0?o=at:u[2]!==void 0?(ct.test(u[2])&&(a=RegExp("</"+u[2],"g")),o=I):u[3]!==void 0&&(o=I):o===I?u[0]===">"?(o=a??ce,r=-1):u[1]===void 0?r=-2:(r=o.lastIndex-u[2].length,g=u[1],o=u[3]===void 0?I:u[3]==='"'?nt:it):o===nt||o===it?o=I:o===st||o===at?o=ce:(o=I,a=void 0);let b=o===I&&n[l+1].startsWith("/>")?" ":"";i+=o===ce?c+Ut:r>=0?(t.push(g),c.slice(0,r)+rt+c.slice(r)+D+b):c+D+(r===-2?l:b)}return[dt(n,i+(n[e]||"<?>")+(s===2?"</svg>":s===3?"</math>":"")),t]},pe=class n{constructor({strings:s,_$litType$:e},t){let a;this.parts=[];let i=0,o=0,l=s.length-1,c=this.parts,[g,u]=jt(s,e);if(this.el=n.createElement(g,t),K.currentNode=this.el.content,e===2||e===3){let r=this.el.content.firstChild;r.replaceWith(...r.childNodes)}for(;(a=K.nextNode())!==null&&c.length<l;){if(a.nodeType===1){if(a.hasAttributes())for(let r of a.getAttributeNames())if(r.endsWith(rt)){let f=u[o++],b=a.getAttribute(r).split(D),R=/([.?@])?(.*)/.exec(f);c.push({type:1,index:i,name:R[2],strings:b,ctor:R[1]==="."?Pe:R[1]==="?"?He:R[1]==="@"?Ne:te}),a.removeAttribute(r)}else r.startsWith(D)&&(c.push({type:6,index:i}),a.removeAttribute(r));if(ct.test(a.tagName)){let r=a.textContent.split(D),f=r.length-1;if(f>0){a.textContent=we?we.emptyScript:"";for(let b=0;b<f;b++)a.append(r[b],de()),K.nextNode(),c.push({type:2,index:++i});a.append(r[f],de())}}}else if(a.nodeType===8)if(a.data===lt)c.push({type:2,index:i});else{let r=-1;for(;(r=a.data.indexOf(D,r+1))!==-1;)c.push({type:7,index:i}),r+=D.length-1}i++}}static createElement(s,e){let t=q.createElement("template");return t.innerHTML=s,t}};function ee(n,s,e=n,t){if(s===W)return s;let a=t!==void 0?e._$Co?.[t]:e._$Cl,i=he(s)?void 0:s._$litDirective$;return a?.constructor!==i&&(a?._$AO?.(!1),i===void 0?a=void 0:(a=new i(n),a._$AT(n,e,t)),t!==void 0?(e._$Co??=[])[t]=a:e._$Cl=a),a!==void 0&&(s=ee(n,a._$AS(n,s.values),a,t)),s}var Le=class{constructor(s,e){this._$AV=[],this._$AN=void 0,this._$AD=s,this._$AM=e}get parentNode(){return this._$AM.parentNode}get _$AU(){return this._$AM._$AU}u(s){let{el:{content:e},parts:t}=this._$AD,a=(s?.creationScope??q).importNode(e,!0);K.currentNode=a;let i=K.nextNode(),o=0,l=0,c=t[0];for(;c!==void 0;){if(o===c.index){let g;c.type===2?g=new ue(i,i.nextSibling,this,s):c.type===1?g=new c.ctor(i,c.name,c.strings,this,s):c.type===6&&(g=new De(i,this,s)),this._$AV.push(g),c=t[++l]}o!==c?.index&&(i=K.nextNode(),o++)}return K.currentNode=q,a}p(s){let e=0;for(let t of this._$AV)t!==void 0&&(t.strings!==void 0?(t._$AI(s,t,e),e+=t.strings.length-2):t._$AI(s[e])),e++}},ue=class n{get _$AU(){return this._$AM?._$AU??this._$Cv}constructor(s,e,t,a){this.type=2,this._$AH=p,this._$AN=void 0,this._$AA=s,this._$AB=e,this._$AM=t,this.options=a,this._$Cv=a?.isConnected??!0}get parentNode(){let s=this._$AA.parentNode,e=this._$AM;return e!==void 0&&s?.nodeType===11&&(s=e.parentNode),s}get startNode(){return this._$AA}get endNode(){return this._$AB}_$AI(s,e=this){s=ee(this,s,e),he(s)?s===p||s==null||s===""?(this._$AH!==p&&this._$AR(),this._$AH=p):s!==this._$AH&&s!==W&&this._(s):s._$litType$!==void 0?this.$(s):s.nodeType!==void 0?this.T(s):Bt(s)?this.k(s):this._(s)}O(s){return this._$AA.parentNode.insertBefore(s,this._$AB)}T(s){this._$AH!==s&&(this._$AR(),this._$AH=this.O(s))}_(s){this._$AH!==p&&he(this._$AH)?this._$AA.nextSibling.data=s:this.T(q.createTextNode(s)),this._$AH=s}$(s){let{values:e,_$litType$:t}=s,a=typeof t=="number"?this._$AC(s):(t.el===void 0&&(t.el=pe.createElement(dt(t.h,t.h[0]),this.options)),t);if(this._$AH?._$AD===a)this._$AH.p(e);else{let i=new Le(a,this),o=i.u(this.options);i.p(e),this.T(o),this._$AH=i}}_$AC(s){let e=ot.get(s.strings);return e===void 0&&ot.set(s.strings,e=new pe(s)),e}k(s){Fe(this._$AH)||(this._$AH=[],this._$AR());let e=this._$AH,t,a=0;for(let i of s)a===e.length?e.push(t=new n(this.O(de()),this.O(de()),this,this.options)):t=e[a],t._$AI(i),a++;a<e.length&&(this._$AR(t&&t._$AB.nextSibling,a),e.length=a)}_$AR(s=this._$AA.nextSibling,e){for(this._$AP?.(!1,!0,e);s!==this._$AB;){let t=et(s).nextSibling;et(s).remove(),s=t}}setConnected(s){this._$AM===void 0&&(this._$Cv=s,this._$AP?.(s))}},te=class{get tagName(){return this.element.tagName}get _$AU(){return this._$AM._$AU}constructor(s,e,t,a,i){this.type=1,this._$AH=p,this._$AN=void 0,this.element=s,this.name=e,this._$AM=a,this.options=i,t.length>2||t[0]!==""||t[1]!==""?(this._$AH=Array(t.length-1).fill(new String),this.strings=t):this._$AH=p}_$AI(s,e=this,t,a){let i=this.strings,o=!1;if(i===void 0)s=ee(this,s,e,0),o=!he(s)||s!==this._$AH&&s!==W,o&&(this._$AH=s);else{let l=s,c,g;for(s=i[0],c=0;c<i.length-1;c++)g=ee(this,l[t+c],e,c),g===W&&(g=this._$AH[c]),o||=!he(g)||g!==this._$AH[c],g===p?s=p:s!==p&&(s+=(g??"")+i[c+1]),this._$AH[c]=g}o&&!a&&this.j(s)}j(s){s===p?this.element.removeAttribute(this.name):this.element.setAttribute(this.name,s??"")}},Pe=class extends te{constructor(){super(...arguments),this.type=3}j(s){this.element[this.name]=s===p?void 0:s}},He=class extends te{constructor(){super(...arguments),this.type=4}j(s){this.element.toggleAttribute(this.name,!!s&&s!==p)}},Ne=class extends te{constructor(s,e,t,a,i){super(s,e,t,a,i),this.type=5}_$AI(s,e=this){if((s=ee(this,s,e,0)??p)===W)return;let t=this._$AH,a=s===p&&t!==p||s.capture!==t.capture||s.once!==t.once||s.passive!==t.passive,i=s!==p&&(t===p||a);a&&this.element.removeEventListener(this.name,this,t),i&&this.element.addEventListener(this.name,this,s),this._$AH=s}handleEvent(s){typeof this._$AH=="function"?this._$AH.call(this.options?.host??this.element,s):this._$AH.handleEvent(s)}},De=class{constructor(s,e,t){this.element=s,this.type=6,this._$AN=void 0,this._$AM=e,this.options=t}get _$AU(){return this._$AM._$AU}_$AI(s){ee(this,s)}};var It=Oe.litHtmlPolyfillSupport;It?.(pe,ue),(Oe.litHtmlVersions??=[]).push("3.3.3");var ht=(n,s,e)=>{let t=e?.renderBefore??s,a=t._$litPart$;if(a===void 0){let i=e?.renderBefore??null;t._$litPart$=a=new ue(s.insertBefore(de(),i),i,void 0,e??{})}return a._$AI(n),a};var Be=globalThis,x=class extends S{constructor(){super(...arguments),this.renderOptions={host:this},this._$Do=void 0}createRenderRoot(){let s=super.createRenderRoot();return this.renderOptions.renderBefore??=s.firstChild,s}update(s){let e=this.render();this.hasUpdated||(this.renderOptions.isConnected=this.isConnected),super.update(s),this._$Do=ht(e,this.renderRoot,this.renderOptions)}connectedCallback(){super.connectedCallback(),this._$Do?.setConnected(!0)}disconnectedCallback(){super.disconnectedCallback(),this._$Do?.setConnected(!1)}render(){return W}};x._$litElement$=!0,x.finalized=!0,Be.litElementHydrateSupport?.({LitElement:x});var Kt=Be.litElementPolyfillSupport;Kt?.({LitElement:x});(Be.litElementVersions??=[]).push("4.2.2");var w=n=>(s,e)=>{e!==void 0?e.addInitializer(()=>{customElements.define(n,s)}):customElements.define(n,s)};var qt={attribute:!0,type:String,converter:le,reflect:!1,hasChanged:_e},Wt=(n=qt,s,e)=>{let{kind:t,metadata:a}=e,i=globalThis.litPropertyMetadata.get(a);if(i===void 0&&globalThis.litPropertyMetadata.set(a,i=new Map),t==="setter"&&((n=Object.create(n)).wrapped=!0),i.set(e.name,n),t==="accessor"){let{name:o}=e;return{set(l){let c=s.get.call(this);s.set.call(this,l),this.requestUpdate(o,c,n,!0,l)},init(l){return l!==void 0&&this.C(o,void 0,n,l),l}}}if(t==="setter"){let{name:o}=e;return function(l){let c=this[o];s.call(this,l),this.requestUpdate(o,c,n,!0,l)}}throw Error("Unsupported decorator location: "+t)};function m(n){return(s,e)=>typeof e=="object"?Wt(n,s,e):((t,a,i)=>{let o=a.hasOwnProperty(i);return a.constructor.createProperty(i,t),o?Object.getOwnPropertyDescriptor(a,i):void 0})(n,s,e)}function M(n){return m({...n,state:!0,attribute:!1})}var pt=(n,s)=>n.connection.subscribeMessage(s,{type:"solace/subscribe"}),V=(n,s)=>n.connection.sendMessagePromise({type:"solace/set_house",values:s}),Ae=(n,s)=>n.connection.sendMessagePromise({type:"solace/set_ramp",ramp:s}),je=(n,s,e)=>n.connection.sendMessagePromise({type:"solace/set_room",subentry_id:s,values:e}),ut=(n,s,e,t)=>n.connection.sendMessagePromise({type:"solace/set_light",subentry_id:s,entity_id:e,values:t}),mt=(n,s,e)=>n.connection.sendMessagePromise({type:"solace/set_zones",subentry_id:s,zones:e.map(({clear:t,...a})=>a)});var Me=(n,s,e,t)=>n.connection.sendMessagePromise({type:"solace/room_action",subentry_id:s,action:e,...t===void 0?{}:{level:t}}),bt=(n,s)=>n.connection.sendMessagePromise({type:"solace/set_remotes",remotes:s});var k=$`
  :host {
    --sol-page: var(--primary-background-color, #111213);
    --sol-card: var(--ha-card-background, var(--card-background-color, #1c1c1d));
    --sol-block: var(--secondary-background-color, #232325);
    --sol-control: #242426;

    --sol-text: var(--primary-text-color, #e8eaed);
    --sol-text-2: #c8ccd0;
    --sol-text-3: var(--secondary-text-color, #9aa0a6);
    --sol-text-4: #6f7377;
    --sol-faint: #5c6063;

    --sol-cyan: var(--primary-color, #4fc3f7);
    --sol-cyan-tint: rgba(79, 195, 247, 0.14);
    --sol-cyan-track: #3b4a52;
    --sol-amber: var(--state-icon-active-color, #ffb74d);
    --sol-amber-light: #ffcc80;
    --sol-amber-surface: #2a2418;
    --sol-amber-track: #4a3f28;
    --sol-green: var(--success-color, #66bb6a);

    --sol-track: #3a3d40;
    --sol-track-sm: #35383b;
    --sol-hair: var(--divider-color, rgba(255, 255, 255, 0.08));
    --sol-hair-faint: rgba(255, 255, 255, 0.05);

    --sol-r-pill: 14px;
    --sol-r-card: 12px;
    --sol-r-block: 8px;
    --sol-r-control: 6px;
    --sol-shadow: 0 1px 3px rgba(0, 0, 0, 0.35);

    --sol-series-ambience: #5c8dd6;
    --sol-series-start: var(--primary-color, #4fc3f7);
    --sol-series-sunset: #ffb74d;
    --sol-series-dusk: #7e57c2;
    --sol-series-full: #f06292;

    display: block;
    color: var(--sol-text);
    font-family: var(--ha-font-family-body, Roboto, system-ui, sans-serif);
  }

  /* Numeric readouts are tabular everywhere — a value that shifts width while a slider
     is dragged reads as flicker. */
  .tab-num {
    font-variant-numeric: tabular-nums;
  }

  .card {
    background: var(--sol-card);
    border-radius: var(--sol-r-card);
    box-shadow: var(--sol-shadow);
    padding: 15px 18px 16px;
  }

  .card-head {
    display: flex;
    align-items: center;
    gap: 9px;
    margin-bottom: 12px;
  }
  .card-head ha-icon {
    --mdc-icon-size: 19px;
    color: var(--sol-cyan);
  }
  .card-head h2 {
    margin: 0;
    font-size: 15px;
    font-weight: 500;
  }

  .eyebrow {
    font-size: 10.5px;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: var(--sol-text-4);
  }

  .caption {
    font-size: 11.5px;
    line-height: 1.5;
    color: var(--sol-text-4);
  }

  /* Focus ring, everywhere, per the handoff. */
  :focus-visible {
    outline: 2px solid var(--sol-cyan);
    outline-offset: 2px;
  }
`;var me=760,be=258,_={x0:58,x1:744,y0:16,y1:206},se=null,Zt=n=>(se||(se=document.createElement("canvas").getContext("2d"),se&&(se.font="10px Roboto, system-ui, sans-serif")),se?se.measureText(n).width:n.length*5.4),A=class extends x{constructor(){super(...arguments);this.series=[];this.refLines=[];this.xTicks=[];this.yTicks=[];this.facts=[];this.xDomain=[0,1];this.yDomain=[0,1];this.marker=null;this.xTitle="";this.yTitle="";this.shade=null;this.shadeLabel=""}sx(e){let[t,a]=this.xDomain;return _.x0+(e-t)/(a-t||1)*(_.x1-_.x0)}sy(e){let[t,a]=this.yDomain;return _.y1-(e-t)/(a-t||1)*(_.y1-_.y0)}path(e){let t="",a=!1;for(let[i,o]of e){if(!isFinite(i)||!isFinite(o)){a=!1;continue}t+=`${a?"L":"M"}${this.sx(i).toFixed(2)} ${this.sy(o).toFixed(2)} `,a=!0}return t.trim()}packed(){let e=[];return this.refLines.filter(t=>isFinite(t.x)).map(t=>({...t,px:this.sx(t.x)})).sort((t,a)=>t.px-a.px).map(t=>{let a=Zt(t.label)/2+4,i=0;for(;e[i]!==void 0&&e[i]>t.px-a;)i++;return e[i]=t.px+a,{...t,row:i}})}render(){let e=this.packed();return d`
      <div class="plot">
        <svg viewBox="0 0 ${me} ${be}" role="img">
          <defs>
            <linearGradient id="kgrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stop-color="#cfe0ff" />
              <stop offset="50%" stop-color="#ffe4c4" />
              <stop offset="100%" stop-color="#ff9a3c" />
            </linearGradient>
          </defs>

          ${this.shade?Z`<rect
                x=${this.sx(this.shade[0])}
                y=${_.y0}
                width=${Math.max(0,this.sx(this.shade[1])-this.sx(this.shade[0]))}
                height=${_.y1-_.y0}
                fill="rgba(240,98,146,.10)" />`:p}

          ${this.yTicks.map(t=>Z`<line x1=${_.x0} x2=${_.x1} y1=${this.sy(t.value)} y2=${this.sy(t.value)} stroke="rgba(255,255,255,.07)" stroke-width="1" />`)}
          ${this.xTicks.map(t=>Z`<line y1=${_.y0} y2=${_.y1} x1=${this.sx(t.value)} x2=${this.sx(t.value)} stroke="rgba(255,255,255,.07)" stroke-width="1" />`)}
          ${this.refLines.map(t=>Z`<line y1=${_.y0} y2=${_.y1} x1=${this.sx(t.x)} x2=${this.sx(t.x)} stroke=${t.colour} stroke-width="1" stroke-dasharray="4 3" />`)}
          ${this.series.map(t=>Z`<path d=${this.path(t.points)} fill="none"
                stroke=${t.gradient?`url(#${t.gradient})`:t.colour}
                stroke-width=${t.width??2}
                stroke-linejoin="round"
                stroke-linecap="round"
                stroke-dasharray=${t.dashed?"4 3":"0"} />`)}
          ${this.marker?Z`<circle cx=${this.sx(this.marker.x)} cy=${this.sy(this.marker.y)} r="5"
                fill="var(--sol-cyan)" stroke="var(--sol-card)" stroke-width="1.5" />`:p}
        </svg>

        ${this.yTicks.map(t=>d`<div
              class="lbl y"
              style="left:${_.x0/me*100}%;top:${this.sy(t.value)/be*100}%"
            >
              ${t.label}
            </div>`)}
        ${this.xTicks.map(t=>d`<div
              class="lbl x"
              style="left:${this.sx(t.value)/me*100}%;top:${_.y1/be*100}%"
            >
              ${t.label}
            </div>`)}
        ${e.map(t=>d`<div
              class="lbl ann"
              style="left:${t.px/me*100}%;top:${(_.y0+2+t.row*13)/be*100}%;color:${t.textColour}"
            >
              ${t.label}
            </div>`)}
        ${this.shadeLabel&&this.shade?d`<div
              class="lbl ann"
              style="left:${(this.sx(this.shade[0])+this.sx(this.shade[1]))/2/me*100}%;top:${(_.y1-16)/be*100}%;color:var(--sol-series-full)"
            >
              ${this.shadeLabel}
            </div>`:p}
        ${this.xTitle?d`<div class="axis-title" style="left:50%;bottom:-2px;transform:translateX(-50%)">
              ${this.xTitle}
            </div>`:p}
      </div>

      ${this.facts.length?d`<div class="facts">
            ${this.facts.map(t=>d`<div class="row"><span>${t.label}</span><span>${t.value}</span></div>`)}
          </div>`:p}
    `}};A.styles=[k,$`
      :host {
        display: flex;
        gap: 14px;
        align-items: flex-start;
        flex-wrap: wrap;
      }
      .plot {
        position: relative;
        flex: 1 1 420px;
        min-width: 300px;
      }
      svg {
        display: block;
        width: 100%;
        height: auto;
      }
      .lbl {
        position: absolute;
        font-size: 10px;
        color: var(--sol-text-4);
        white-space: nowrap;
        pointer-events: none;
        font-variant-numeric: tabular-nums;
      }
      .lbl.y {
        transform: translate(-100%, -50%);
        padding-right: 6px;
      }
      .lbl.x {
        transform: translate(-50%, 2px);
      }
      .lbl.ann {
        transform: translate(-50%, 0);
        font-size: 10px;
      }
      .axis-title {
        position: absolute;
        font-size: 10.5px;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        color: var(--sol-text-4);
      }
      .facts {
        flex: 0 0 198px;
        background: #232426;
        border: 1px solid rgba(255, 255, 255, 0.07);
        border-radius: var(--sol-r-block);
        padding: 10px 12px;
        box-sizing: border-box;
      }
      .facts .row {
        display: flex;
        justify-content: space-between;
        gap: 8px;
        font-size: 11px;
        line-height: 1.75;
      }
      .facts .row span:first-child {
        color: var(--sol-text-4);
      }
      .facts .row span:last-child {
        color: var(--sol-text-2);
        font-variant-numeric: tabular-nums;
      }
      @media (max-width: 720px) {
        .facts {
          flex: 1 1 100%;
        }
      }
    `],h([m({attribute:!1})],A.prototype,"series",2),h([m({attribute:!1})],A.prototype,"refLines",2),h([m({attribute:!1})],A.prototype,"xTicks",2),h([m({attribute:!1})],A.prototype,"yTicks",2),h([m({attribute:!1})],A.prototype,"facts",2),h([m({attribute:!1})],A.prototype,"xDomain",2),h([m({attribute:!1})],A.prototype,"yDomain",2),h([m({attribute:!1})],A.prototype,"marker",2),h([m()],A.prototype,"xTitle",2),h([m()],A.prototype,"yTitle",2),h([m({attribute:!1})],A.prototype,"shade",2),h([m()],A.prototype,"shadeLabel",2),A=h([w("sol-chart")],A);var Vt=n=>{let s=n>0?"+":n<0?"\u2212":"",e=Math.abs(n);return s+(e%1===0?e.toFixed(0):String(parseFloat(e.toFixed(2))))},O=n=>n===0?"0 stops":`${Vt(n)} ${Math.abs(n)===1?"stop":"stops"}`,F=(n,s)=>Math.round(100*Math.pow(Math.max(0,Math.min(254,n))/254,s)),Ie=(n,s)=>n===null?"\u2014":`\u2192 level ${n} (${F(n,s)} % light)`,y=n=>{if(n==null||!isFinite(n))return"\u2014";let s=Math.round(n*60)%1440;return s<0&&(s+=1440),`${String(Math.floor(s/60)).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`},gt=n=>{let[s,e]=n.split(":");return(parseInt(s,10)||0)+(parseInt(e,10)||0)/60},v=(n,s=0)=>n.toLocaleString("en-GB",{maximumFractionDigits:s}),vt=n=>{if(n===null)return"\u2014";let s=Math.max(0,Math.round(n));return`${Math.floor(s/60)}:${String(s%60).padStart(2,"0")}`},ft=n=>{if(!n)return"never";let s=(Date.now()-new Date(n).getTime())/1e3;return!isFinite(s)||s<0?"just now":s<60?`${Math.round(s)}s ago`:s<3600?`${Math.round(s/60)}m ago`:`${Math.round(s/3600)}h ago`},xt=n=>isFinite(n)?`${v(n,n<10?1:0)} lx`:"no reading";var G=class extends x{constructor(){super(...arguments);this.text="";this.flip=!1}render(){return this.text?d`
      <button tabindex="0" aria-label=${this.text}>
        <ha-icon icon="mdi:information-outline"></ha-icon>
      </button>
      <div class="bubble" role="tooltip">${this.text}</div>
    `:p}};G.styles=[k,$`
      :host {
        display: inline-flex;
        position: relative;
      }
      button {
        all: unset;
        cursor: help;
        display: inline-flex;
        border-radius: 50%;
        color: var(--sol-text-4);
      }
      ha-icon {
        --mdc-icon-size: 14px;
      }
      .bubble {
        position: absolute;
        bottom: calc(100% + 9px);
        left: 0;
        width: max-content;
        max-width: 272px;
        min-width: 240px;
        background: #33363a;
        color: #dfe3e6;
        border-radius: var(--sol-r-block);
        padding: 9px 11px;
        font-size: 11.5px;
        line-height: 1.5;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.55);
        opacity: 0;
        transition: opacity 0.12s ease;
        /* Never intercept the pointer — the bubble sits over controls. */
        pointer-events: none;
        z-index: 20;
      }
      :host([flip]) .bubble {
        left: auto;
        right: 0;
      }
      button:hover + .bubble,
      button:focus-visible + .bubble,
      :host(:focus-within) .bubble {
        opacity: 1;
      }
    `],h([m()],G.prototype,"text",2),h([m({type:Boolean})],G.prototype,"flip",2),G=h([w("sol-help")],G);var E=class extends x{constructor(){super(...arguments);this.value=0;this.min=-2;this.max=2;this.step=.25;this.neutral=0;this.small=!1;this.tone="cyan";this.noReset=!1}_emit(e,t){this.value=e,this.dispatchEvent(new CustomEvent("value-changed",{detail:{value:e,final:t},bubbles:!0,composed:!0}))}render(){let e=this.min<this.neutral&&this.max>this.neutral,t=(this.neutral-this.min)/(this.max-this.min)*100,a=this.value!==this.neutral;return d`
      <div class="wrap">
        <input
          type="range"
          .min=${String(this.min)}
          .max=${String(this.max)}
          .step=${String(this.step)}
          .value=${String(this.value)}
          @input=${i=>this._emit(parseFloat(i.target.value),!1)}
          @change=${i=>this._emit(parseFloat(i.target.value),!0)}
        />
        ${e?d`<div class="tick" style="left:${t}%"></div>`:p}
      </div>
      ${this.noReset?p:a?d`<button
              class="reset"
              title="Reset"
              @click=${()=>this._emit(this.neutral,!0)}
            >
              <ha-icon icon="mdi:backup-restore"></ha-icon>
            </button>`:d`<span class="spacer"></span>`}
    `}};E.styles=[k,$`
      :host {
        display: flex;
        align-items: center;
        gap: 8px;
        flex: 1;
        min-width: 0;
      }
      .wrap {
        position: relative;
        flex: 1;
        display: flex;
        align-items: center;
        min-width: 0;
      }
      input {
        -webkit-appearance: none;
        appearance: none;
        width: 100%;
        background: transparent;
        margin: 0;
        height: 20px;
        cursor: pointer;
      }
      input::-webkit-slider-runnable-track {
        height: 4px;
        border-radius: 2px;
        background: var(--sol-track);
      }
      input::-moz-range-track {
        height: 4px;
        border-radius: 2px;
        background: var(--sol-track);
      }
      :host([small]) input::-webkit-slider-runnable-track {
        height: 3px;
        background: var(--sol-track-sm);
      }
      :host([small]) input::-moz-range-track {
        height: 3px;
        background: var(--sol-track-sm);
      }
      /* Amber is reserved for light output — it is the level slider's track, and it is
         never used to make an ordinary control look lively. */
      :host([tone="amber"]) input::-webkit-slider-runnable-track {
        background: linear-gradient(90deg, #5b4a26, var(--sol-amber));
      }
      :host([tone="amber"]) input::-moz-range-track {
        background: linear-gradient(90deg, #5b4a26, var(--sol-amber));
      }
      input::-webkit-slider-thumb {
        -webkit-appearance: none;
        width: 14px;
        height: 14px;
        margin-top: -5px;
        border-radius: 50%;
        background: var(--sol-cyan);
        border: none;
      }
      input::-moz-range-thumb {
        width: 14px;
        height: 14px;
        border: none;
        border-radius: 50%;
        background: var(--sol-cyan);
      }
      :host([tone="amber"]) input::-webkit-slider-thumb {
        background: var(--sol-amber);
      }
      :host([tone="amber"]) input::-moz-range-thumb {
        background: var(--sol-amber);
      }
      :host([small]) input::-webkit-slider-thumb {
        width: 11px;
        height: 11px;
        margin-top: -4px;
      }
      :host([small]) input::-moz-range-thumb {
        width: 11px;
        height: 11px;
      }
      input:focus-visible::-webkit-slider-thumb {
        box-shadow: 0 0 0 6px rgba(79, 195, 247, 0.25);
      }
      input:focus-visible::-moz-range-thumb {
        box-shadow: 0 0 0 6px rgba(79, 195, 247, 0.25);
      }
      .tick {
        position: absolute;
        top: 4px;
        bottom: 4px;
        width: 1px;
        background: rgba(255, 255, 255, 0.22);
        pointer-events: none;
      }
      button.reset {
        all: unset;
        cursor: pointer;
        display: inline-flex;
        color: var(--sol-text-4);
        border-radius: var(--sol-r-control);
      }
      button.reset:hover {
        color: var(--sol-text-2);
      }
      button.reset ha-icon {
        --mdc-icon-size: 15px;
      }
      .spacer {
        width: 15px;
        flex: 0 0 15px;
      }
    `],h([m({type:Number})],E.prototype,"value",2),h([m({type:Number})],E.prototype,"min",2),h([m({type:Number})],E.prototype,"max",2),h([m({type:Number})],E.prototype,"step",2),h([m({type:Number})],E.prototype,"neutral",2),h([m({type:Boolean})],E.prototype,"small",2),h([m()],E.prototype,"tone",2),h([m({type:Boolean})],E.prototype,"noReset",2),E=h([w("sol-slider")],E);var T=class extends x{constructor(){super(...arguments);this.value=null;this.min=null;this.max=null;this.step=1;this.suffix="";this.clearable=!1;this.width=60}_commit(e){let t=e.trim()===""?null:parseFloat(e);t!==null&&!isFinite(t)||this.dispatchEvent(new CustomEvent("value-changed",{detail:{value:t===null&&!this.clearable?this.min??0:t},bubbles:!0,composed:!0}))}render(){return d`
      <input
        type="number"
        class=${this.value!==null?"set":""}
        style="width:${this.width}px"
        placeholder="—"
        .value=${this.value===null?"":String(this.value)}
        .min=${this.min===null?"":String(this.min)}
        .max=${this.max===null?"":String(this.max)}
        .step=${String(this.step)}
        @change=${e=>this._commit(e.target.value)}
      />
      ${this.suffix?d`<span class="suffix">${this.suffix}</span>`:p}
    `}};T.styles=[k,$`
      :host {
        display: inline-flex;
        align-items: center;
        gap: 5px;
      }
      input {
        font: inherit;
        font-size: 12.5px;
        font-variant-numeric: tabular-nums;
        color: var(--sol-text);
        background: transparent;
        border: 1px solid rgba(255, 255, 255, 0.07);
        border-radius: var(--sol-r-control);
        padding: 6px 7px;
        box-sizing: border-box;
        text-align: right;
      }
      input.set {
        background: var(--sol-control);
        border-color: rgba(79, 195, 247, 0.35);
      }
      input::placeholder {
        color: var(--sol-faint);
      }
      /* The spinners are noise at this density and steal the width. */
      input::-webkit-outer-spin-button,
      input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
      }
      input[type="number"] {
        -moz-appearance: textfield;
      }
      .suffix {
        font-size: 11px;
        color: var(--sol-text-4);
      }
    `],h([m({type:Number})],T.prototype,"value",2),h([m({type:Number})],T.prototype,"min",2),h([m({type:Number})],T.prototype,"max",2),h([m({type:Number})],T.prototype,"step",2),h([m()],T.prototype,"suffix",2),h([m({type:Boolean})],T.prototype,"clearable",2),h([m({type:Number})],T.prototype,"width",2),T=h([w("sol-number")],T);var J=class extends x{constructor(){super(...arguments);this.options=[];this.value=""}render(){return d`<div class="track">
      ${this.options.map(e=>d`<button
          class="${e.value===this.value?"on":""} ${e.tone==="amber"?"amber":""}"
          aria-pressed=${e.value===this.value}
          @click=${()=>this.dispatchEvent(new CustomEvent("segment-changed",{detail:{value:e.value},bubbles:!0,composed:!0}))}
        >
          ${e.label}
        </button>`)}
    </div>`}};J.styles=[k,$`
      .track {
        display: inline-flex;
        background: var(--sol-control);
        border-radius: var(--sol-r-pill);
        padding: 2px;
      }
      button {
        all: unset;
        cursor: pointer;
        font-size: 11.5px;
        font-weight: 500;
        padding: 5px 12px;
        border-radius: 12px;
        color: var(--sol-text-3);
        text-align: center;
      }
      button.on {
        background: var(--sol-cyan-track);
        color: var(--sol-cyan);
      }
      button.on.amber {
        background: var(--sol-amber-track);
        color: var(--sol-amber);
      }
    `],h([m({attribute:!1})],J.prototype,"options",2),h([m()],J.prototype,"value",2),J=h([w("sol-segmented")],J);var X=class extends x{constructor(){super(...arguments);this.label="";this.help=""}render(){return d`
      <div class="label">
        <span>${this.label}</span>
        ${this.help?d`<sol-help .text=${this.help}></sol-help>`:p}
      </div>
      <div class="control"><slot></slot></div>
      <div class="value"><slot name="value"></slot></div>
    `}};X.styles=[k,$`
      :host {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 9px 0;
      }
      .label {
        flex: 0 0 176px;
        display: flex;
        align-items: center;
        gap: 6px;
        font-size: 12.5px;
        color: var(--sol-text-2);
      }
      .control {
        flex: 1;
        min-width: 0;
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .value {
        flex: 0 0 88px;
        text-align: right;
        font-size: 12.5px;
        font-variant-numeric: tabular-nums;
        color: var(--sol-text);
      }
      @media (max-width: 560px) {
        :host {
          flex-wrap: wrap;
        }
        .label {
          flex-basis: 100%;
        }
      }
    `],h([m()],X.prototype,"label",2),h([m()],X.prototype,"help",2),X=h([w("sol-row")],X);var Gt={day_kelvin:"The colour held through the day, until civil dusk starts the glide down.",night_kelvin:"The colour held after the glide, until the morning release.",colour_glide_minutes:"How long the glide from day to night colour takes, starting at civil dusk. The glide is interpolated in mireds, which is what makes it look even.",colour_trim_kelvin:"A live trim added to whatever the curve says, at any time of day. Use it to nudge the whole house warmer or cooler without touching the curve.",colour_step_mired:"Step size for bulbs that cannot glide colour while their brightness is fading \u2014 they skip steps while one is running, so they are walked in bigger, less frequent moves. Smaller is not better here: a finer walk would fall behind the curve rather than track it.",colour_step_mired_smooth:"Step size for bulbs that can glide colour during a brightness fade. They never skip a step, so they can be walked finely and look continuous. This is the setting that makes the good bulbs look good.",colour_catch_up_steps:"How far a single move may reach when a bulb has fallen behind the curve. Safe at any size \u2014 a bigger jump over the same short fade is further from the failure threshold, not closer \u2014 so this is about how large a visible colour jump you will accept.",colour_step_transition_s:"The fade applied to each individual colour step. This is the number the hardware limit is computed from, never the gap between steps."},Jt={ikea:"IKEA TR\xC5DFRI",aqara_cct:"Aqara CCT",aqara_rgb:"Aqara RGB"},Ee=["#4fc3f7","#ffb74d","#81c784"],Y=n=>1e6/Math.max(n,1),ae=n=>1e6/Math.max(n,1),L=class extends x{constructor(){super(...arguments);this.view="day";this.draft={}}schema(e){return this.snap.house_schema.find(t=>t.key===e)}value(e){return this.draft[e]??this.snap.house[e]??this.schema(e)?.default??0}async push(e,t,a=!0){this.draft={...this.draft,[e]:t},await V(this.hass,{[e]:t}),a&&window.setTimeout(()=>{let i={...this.draft};delete i[e],this.draft=i},700)}row(e,t){let a=this.schema(e);if(!a)return p;let i=this.value(e);return d`<sol-row .label=${a.name} .help=${Gt[e]??""}>
      <sol-slider
        .value=${i}
        .min=${a.min}
        .max=${a.max}
        .step=${a.step}
        .neutral=${a.default}
        .noReset=${a.default!==0}
        @value-changed=${o=>this.push(e,o.detail.value,o.detail.final)}
      ></sol-slider>
      <span slot="value"
        >${t?t(i):`${v(i)}${a.unit?` ${a.unit}`:""}`}</span
      >
    </sol-row>`}kelvinAt(e){let t=this.snap.world.dusk_hour,a=this.value("morning_release_hour"),i=this.value("day_kelvin"),o=this.value("night_kelvin"),l=Math.max(this.value("colour_glide_minutes"),0)/60,c=(e-t+24)%24,g=(a-t+24)%24,u=Math.max(this.value("morning_glide_minutes"),0)/60,r;if(c>=g){let f=c-g;u<=0||f>=u?r=i:r=ae(Y(o)+f/u*(Y(i)-Y(o)))}else l<=0||c>=l?r=o:r=ae(Y(i)+c/l*(Y(o)-Y(i)));return Math.max(2e3,Math.min(7e3,r+this.value("colour_trim_kelvin")))}chart(){let e=this.snap.world,t=e.dusk_hour,a=[],i,o;if(this.view==="day"){for(let r=0;r<=288;r++){let f=r/288*24;a.push([f,this.kelvinAt(f)])}i=[0,24],o=[0,3,6,9,12,15,18,21,24].map(r=>({value:r,label:y(r)}))}else{for(let r=-60;r<=240;r+=2)a.push([r,this.kelvinAt((t+r/60+24)%24)]);i=[-60,240],o=[-60,0,60,120,180,240].map(r=>({value:r,label:`${r>0?"+":""}${r}`}))}let l=r=>r===null?NaN:this.view==="day"?r:((r-t+36)%24-12)*60,c=[{hour:e.sunrise_hour,label:"sunrise",c:"rgba(255,183,77,.5)",t:"var(--sol-amber)"},{hour:e.sunset_hour,label:"sunset",c:"rgba(255,183,77,.5)",t:"var(--sol-amber)"},{hour:t,label:"civil dusk",c:"rgba(149,117,205,.75)",t:"#b39ddb"},{hour:this.value("morning_release_hour"),label:"release",c:"rgba(79,195,247,.45)",t:"var(--sol-cyan)"}].map(r=>({x:l(r.hour),label:r.label,colour:r.c,textColour:r.t})).filter(r=>isFinite(r.x)&&r.x>=i[0]&&r.x<=i[1]),g=this.value("night_kelvin")+this.value("colour_trim_kelvin"),u=`${v(this.value("day_kelvin"))} \u2192 ${v(this.value("night_kelvin"))} K`;return d`<sol-chart
      .series=${[{points:a,colour:"#ffe4c4",width:2.5,gradient:"kgrad"}]}
      .refLines=${c}
      .xTicks=${o}
      .yTicks=${[2e3,3e3,4e3,5e3,6e3,7e3].map(r=>({value:r,label:`${r/1e3}k`}))}
      .xDomain=${i}
      .yDomain=${[2e3,7e3]}
      .marker=${{x:l(e.clock_hour),y:this.kelvinAt(e.clock_hour)}}
      xTitle=${this.view==="day"?"time of day":"minutes from civil dusk"}
      .facts=${[{label:"Now",value:`${y(e.clock_hour)} \xB7 ${v(this.kelvinAt(e.clock_hour))} K`},{label:"Sunset",value:y(e.sunset_hour)},{label:"Civil dusk",value:y(t)},{label:"Day \u2192 night",value:u},{label:"Glide",value:`${v(this.value("colour_glide_minutes"))} min`},{label:"Trim",value:`${this.value("colour_trim_kelvin")>0?"+":""}${v(this.value("colour_trim_kelvin"))} K`},{label:"Night holds at",value:`${v(Math.max(2e3,g))} K`}]}
    ></sol-chart>`}walk(e,t){let a=Math.max(this.snap.fade?.interval_s??60,1),i=this.snap.world.dusk_hour,o=u=>Y(this.kelvinAt((i+u/60+24)%24)),l=o(0),c=[[0,ae(l)]],g=0;for(let u=a;u<=t*60;u+=a){let r=u/60,f=o(r)-l;if(Math.abs(f)>=e.step_mired){let b=Math.min(Math.floor(Math.abs(f)/e.step_mired)*e.step_mired,e.max_step_mired);c.push([r,ae(l)]),l+=f>0?b:-b,c.push([r,ae(l)]),g++}}return c.push([t,ae(l)]),{points:c,moves:g,perHour:Math.round(g/t*60)}}grouped(e){let t=[];for(let a of e){let i=`${a.step_mired}|${a.max_step_mired}|${a.step_transition_s}|${a.concurrent}`,o=t.find(c=>`${c.step_mired}|${c.max_step_mired}|${c.step_transition_s}|${c.concurrent}`===i),l=Jt[a.family]??a.family;o?(o.label+=` + ${l}`,o.count+=a.count):t.push({...a,label:l})}return t}stepChart(e){let a=this.snap.world.dusk_hour,i=[];for(let r=0;r<=20;r+=.25)i.push([r,this.kelvinAt((a+r/60+24)%24)]);let o=e.map((r,f)=>({f:r,colour:Ee[f%Ee.length],...this.walk(r,20)})),l=[...i.map(r=>r[1]),...o.flatMap(r=>r.points.map(f=>f[1]))],c=Math.min(...l),g=Math.max(...l),u=Math.max((g-c)*.12,20);return d`<sol-chart
      .series=${[{points:i,colour:"rgba(255,255,255,.28)",width:1.5,dashed:!0},...o.map(r=>({points:r.points,colour:r.colour,width:2}))]}
      .xTicks=${[0,5,10,15,20].map(r=>({value:r,label:`+${r}`}))}
      .yTicks=${[c,(c+g)/2,g].map(r=>({value:r,label:`${v(Math.round(r))}`}))}
      .xDomain=${[0,20]}
      .yDomain=${[c-u,g+u]}
      xTitle="minutes into the glide"
      yTitle="K"
      .facts=${[{label:"Tick",value:`every ${v(Math.round(this.snap.fade?.interval_s??0))} s`},...o.map(r=>({label:r.f.concurrent?"Smooth":"Stepped",value:`${r.f.step_mired} mired \xB7 ~${r.perHour}/h`}))]}
    ></sol-chart>`}fadeCard(){let e=this.snap.fade;if(!e||!e.families.length)return p;let t=this.grouped(e.families);return d`<div class="card full">
      <div class="card-head">
        <ha-icon icon="mdi:stairs"></ha-icon>
        <h2>How each bulb walks the curve</h2>
      </div>
      <div class="caption" style="margin-bottom:10px">
        Colour is stepped, not faded — a long colour transition underflows the bulb's own maths and
        strands it on a hardware rail, permanently and silently. So the curve is walked in small
        absolute writes. One clock serves every bulb; what differs is how big a step each family
        takes, and the dashed line is the curve they are all aiming at.
      </div>
      ${this.stepChart(t)}
      <div class="families">
        ${t.map((a,i)=>d`<div class="family">
            <div class="fam-head">
              <span class="swatch" style="background:${Ee[i%Ee.length]}">
              </span>
              <b>${a.label}</b>
              <span class="count">${a.count} bulb${a.count===1?"":"s"}</span>
              <span class="tag ${a.concurrent?"ok":"warn"}">
                ${a.concurrent?"glides during a fade":"waits for brightness"}
              </span>
            </div>
            <div class="caption">${a.reason}</div>
          </div>`)}
      </div>
      <div class="sub">
        ${this.row("colour_step_mired_smooth",a=>`${v(a)} mired`)}
        ${this.row("colour_step_mired",a=>`${v(a)} mired`)}
        ${this.row("colour_catch_up_steps",a=>`up to ${v(a)} steps`)}
        ${this.row("colour_step_transition_s",a=>`${v(a,1)} s`)}
        <div class="caption">
          The tick is paced for the finest family in the house. Coarser bulbs are not over-driven by
          it — they simply decline the ticks where the curve has not moved far enough to be worth a
          write.
        </div>
      </div>
    </div>`}clampedLights(){let e=this.value("day_kelvin")+this.value("colour_trim_kelvin"),t=[];for(let a of this.snap.rooms)for(let i of a.lights)i.max_kelvin<e&&t.push({name:i.name,max:i.max_kelvin});return t}render(){let e=this.value("colour_trim_kelvin"),t=this.value("day_kelvin")+e,a=this.clampedLights();return d`<div class="grid">
      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:palette"></ha-icon>
          <h2>Colour</h2>
        </div>
        <div class="kelvin-track"></div>
        ${this.row("day_kelvin",i=>`${v(i)} K`)}
        ${this.row("night_kelvin",i=>`${v(i)} K`)}
        ${this.row("colour_glide_minutes",i=>`${v(i)} min`)}
        ${this.row("colour_trim_kelvin",i=>`${i>0?"+":""}${v(i)} K`)}
        <div class="trim-caption">
          <span>${e<0?"warmer":"\xA0"}</span>
          <span>right now: <b>${v(this.kelvinAt(this.snap.world.clock_hour))} K</b></span>
          <span>${e>0?"cooler":"\xA0"}</span>
        </div>
        <div class="caption" style="margin-top:10px">
          Colour moves in steps, not one long fade. Step sizes differ per bulb family — see “How
          each bulb walks the curve” below.
        </div>
        ${a.length?d`<div class="advisory">
              <ha-icon icon="mdi:alert-outline"></ha-icon>
              <div>
                ${a.length} bulb${a.length===1?" is":"s are"} held below the day
                target by their own hardware. They accept the command and report success, so this
                is the only place it shows:
                <ul>
                  ${a.map(i=>d`<li>${i.name} — capped at ${v(i.max)} K</li>`)}
                </ul>
              </div>
            </div>`:p}
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:thermometer-lines"></ha-icon>
          <h2>What each bulb can actually do</h2>
        </div>
        <div class="caption" style="margin-bottom:8px">
          One Kelvin goes to the whole house; each bulb pins it into its own range and reports
          success either way. Broadcasting to mixed families is safe, but the families then
          visually diverge — which is the part worth seeing.
        </div>
        <div class="ranges">
          ${this.snap.rooms.flatMap(i=>i.lights.map(o=>d`<div class="range-row ${o.kelvin<t?"capped":""}">
                <span class="n" title=${o.name}>${o.name}</span>
                <span class="r tab-num">${v(o.min_kelvin)}–${v(o.max_kelvin)} K</span>
                <span class="a tab-num">${v(o.kelvin)} K${o.kelvin_clamped?" \u2691":""}</span>
              </div>`))}
        </div>
      </div>

      <div class="card full">
        <div class="head-row">
          <div class="card-head" style="margin:0">
            <ha-icon icon="mdi:chart-line"></ha-icon>
            <h2>Colour temperature</h2>
          </div>
          <sol-segmented
            .options=${[{value:"day",label:"Day"},{value:"hour",label:"Hour"}]}
            .value=${this.view}
            @segment-changed=${i=>this.view=i.detail.value}
          ></sol-segmented>
        </div>
        ${this.chart()}
      </div>

      ${this.fadeCard()}
    </div>`}};L.styles=[k,$`
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(440px, 1fr));
        gap: 14px;
      }
      @media (max-width: 520px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }
      .full {
        grid-column: 1 / -1;
      }
      .head-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 12px;
      }
      .trim-caption {
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-size: 11.5px;
        color: var(--sol-text-4);
        margin-top: 2px;
      }
      .trim-caption b {
        color: var(--sol-text);
        font-weight: 500;
        font-variant-numeric: tabular-nums;
      }
      .advisory {
        display: flex;
        gap: 8px;
        align-items: flex-start;
        margin-top: 14px;
        padding: 9px 11px;
        background: var(--sol-amber-surface);
        border-radius: var(--sol-r-block);
        font-size: 11.5px;
        line-height: 1.5;
        color: var(--sol-amber-light);
      }
      .advisory ha-icon {
        --mdc-icon-size: 16px;
        flex: 0 0 auto;
        color: var(--sol-amber);
      }
      .advisory ul {
        margin: 4px 0 0;
        padding-left: 16px;
      }
      /* One block per bulb family, each carrying the reason it is walked that way. A
         stepped family looks like a bug without it. */
      .families {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 8px;
        margin: 12px 0 4px;
      }
      .family {
        padding: 8px 10px;
        background: rgba(255, 255, 255, 0.03);
        border-radius: var(--sol-r-block);
      }
      .fam-head {
        display: flex;
        align-items: center;
        gap: 7px;
        font-size: 12px;
        margin-bottom: 4px;
        flex-wrap: wrap;
      }
      .swatch {
        width: 9px;
        height: 9px;
        border-radius: 2px;
        flex: 0 0 auto;
      }
      .fam-head .count {
        color: var(--sol-text-4);
        font-size: 11px;
      }
      .tag {
        margin-left: auto;
        padding: 1px 7px;
        border-radius: 999px;
        font-size: 10.5px;
        white-space: nowrap;
      }
      .tag.ok {
        background: rgba(129, 199, 132, 0.14);
        color: #a5d6a7;
      }
      .tag.warn {
        background: var(--sol-amber-surface);
        color: var(--sol-amber-light);
      }
      /* The Kelvin gradient is a real reference, not decoration — it is what the numbers
         on the slider mean. */
      .ranges {
        display: flex;
        flex-direction: column;
      }
      .range-row {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 104px 76px;
        gap: 8px;
        align-items: center;
        padding: 6px 0;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        font-size: 12px;
      }
      .range-row .n {
        color: var(--sol-text-2);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .range-row .r {
        color: var(--sol-text-4);
        text-align: right;
      }
      .range-row .a {
        text-align: right;
        color: var(--sol-text);
      }
      /* Amber = a hardware clamp. That is the whole reason amber is reserved. */
      .range-row.capped .a {
        color: var(--sol-amber);
      }
      .kelvin-track {
        height: 4px;
        border-radius: 2px;
        margin: 2px 0 8px;
        background: linear-gradient(
          90deg,
          #ff9a3c,
          #ffc689,
          #ffe4c4,
          #fff6ec,
          #f2f4ff,
          #cfe0ff
        );
      }
    `],h([m({attribute:!1})],L.prototype,"hass",2),h([m({attribute:!1})],L.prototype,"snap",2),h([M()],L.prototype,"view",2),h([M()],L.prototype,"draft",2),L=h([w("sol-tab-colour")],L);var P={houseBias:"Shifts every room at once, in stops. One stop is a doubling of light. Rooms keep their own offsets, so this moves the whole house without flattening the differences between rooms.",roomBias:"This room's offset from the house, in stops. Added to the house bias, never multiplied by it.",zoneBias:"A layer between the room and its individual lights. Use it when part of a room wants a different level from the rest.",ambience:"A resting glow floor for this room while you are awake and it is dark outside. Replaces off \u2014 never lowers a light that is already active. 0 means this room follows the house-wide setting.",diminish:"Kitchen behaviour. When the near sensor reads clear the lights reduce by this much and stay there \u2014 they never switch off from diminish alone. 0 means no effect.",perLight:"Per-light offsets, in stops, added on top of the house, room and zone biases. Min is a cutoff: demand below it turns the light off entirely. Max is a clamp: the light never exceeds it.",min:"A cutoff, not a floor. If the computed level falls below this, the light goes off rather than sitting at a useless glow.",max:"A hard clamp applied last, after everything else. The light never exceeds it \u2014 this is what makes a glare cap a rule rather than a suggestion.",manual:"Manual hands this room to you and stops Solace writing to it. A touch on a physical switch does the same for a while; this switch holds until you turn it off.",zone:"A part of this area with its own bias \u2014 the office end of a living room, the sink end of a kitchen. Same four walls, so it shares the area's presence and its dials; the zone bias is an offset on top.",zoneDiminish:"When this zone's own sensor reads clear, its lights reduce by this much and stay there. They never switch off from diminish alone. 0 means no effect.",nightOff:"When you are asleep this room goes fully dark instead of dropping to the night level. Once you are up it rejoins the house at the night level, so the room you are standing in is never the dark one."},H=class extends x{constructor(){super(...arguments);this.open=new Set;this.draft={};this._debounceTimers=new Map}connectedCallback(){super.connectedCallback(),this.timer=window.setInterval(()=>this.requestUpdate(),1e3)}disconnectedCallback(){super.disconnectedCallback(),this.timer&&clearInterval(this.timer);for(let e of this._debounceTimers.values())clearTimeout(e);this._debounceTimers.clear()}key(...e){return e.join("|")}local(e,t){return this.draft[e]??t}async pushHouse(e,t,a){let i=this.key("h",e);this.draft={...this.draft,[i]:t};let o=this._debounceTimers.get(i);if(o&&clearTimeout(o),a)this._debounceTimers.delete(i),await V(this.hass,{[e]:t}),this.clearDraft(i);else{let l=window.setTimeout(async()=>{this._debounceTimers.delete(i),await V(this.hass,{[e]:t})},1e3);this._debounceTimers.set(i,l)}}async pushRoom(e,t,a,i=!0){let o=this.key(e,t);typeof a=="number"&&(this.draft={...this.draft,[o]:a});let l=this._debounceTimers.get(o);if(l&&clearTimeout(l),i||typeof a=="boolean")this._debounceTimers.delete(o),await je(this.hass,e,{[t]:a}),typeof a=="number"&&this.clearDraft(o);else{let c=window.setTimeout(async()=>{this._debounceTimers.delete(o),await je(this.hass,e,{[t]:a})},1e3);this._debounceTimers.set(o,c)}}async pushLight(e,t,a,i){await ut(this.hass,e,t,{[a]:i})}clearDraft(e){window.setTimeout(()=>{let t={...this.draft};delete t[e],this.draft=t},700)}roomIcon(e){let t=e.toLowerCase();return t.includes("bed")?"mdi:bed":t.includes("kitchen")||t.includes("diner")?"mdi:countertop":t.includes("entry")||t.includes("hall")?"mdi:door-open":t.includes("office")?"mdi:desk":"mdi:sofa"}renderStrip(){let e=this.snap.world,t=e.night_active?"Night":e.asleep?"Asleep":"Normal",a=(e.dusk_hour-e.clock_hour+24)%24*60;return d`<div class="strip">
      <div>
        <ha-icon icon=${e.night_active?"mdi:weather-night":"mdi:weather-sunset"}></ha-icon>
        <b>${t}</b>
      </div>
      <div><span>Outdoor</span><b>${xt(e.lux)}</b></div>
      <div><span>Colour</span><b>${v(e.kelvin)} K</b></div>
      <div>
        <span>Civil dusk</span>
        <b>${y(e.dusk_hour)}</b>
        ${a<720?d`<span>(${Math.round(a)} min)</span>`:p}
      </div>
      <div><span>Updated</span><b>${ft(e.updated_at)}</b></div>
      <div>
        <span>Every</span
        ><b>${e.interval_s?`${Math.round(e.interval_s)} s`:"\u2014"}</b>
      </div>
    </div>`}renderHouseBias(){let e=this.snap.house.gamma??2.39,t=this.key("h","bias_stops"),a=this.local(t,this.snap.house.bias_stops??0),i=Math.max(0,...this.snap.rooms.map(o=>o.level??0));return d`<div class="house">
      <div class="name">
        <ha-icon icon="mdi:brightness-6"></ha-icon>
        <span>Whole house bias</span>
        <sol-help .text=${P.houseBias}></sol-help>
      </div>
      <sol-slider
        .value=${a}
        .min=${-2}
        .max=${2}
        .step=${.1}
        @value-changed=${o=>this.pushHouse("bias_stops",o.detail.value,o.detail.final)}
      ></sol-slider>
      <div class="readout tab-num">${O(a)}</div>
      <div class="cons">${i?Ie(i,e):"\u2192 all rooms dark"}</div>
    </div>`}renderLight(e,t){let a=this.snap.house.gamma??2.39,i=this.key(e.subentry_id,t.entity_id,"bias"),o=this.local(i,t.bias_stops),l=t.kelvin_clamped?`held at ${v(t.max_kelvin)} K by its bulb`:"";return d`<div class="lrow">
      <div class="lname">
        <div class="n" title=${t.name}>${t.name}</div>
        <div class="s">
          ${t.group_size>1?d`<span>group of ${t.group_size} · </span>`:p}
          ${l?d`<span class="hw">${l} · </span>`:p}
          <span class="tab-num"
            >level ${t.level??"\u2014"}${t.level!==null?` (${F(t.level,a)} %)`:""}</span
          >
        </div>
      </div>
      <div class="adj">
        <sol-slider
          small
          .value=${o}
          .min=${-2}
          .max=${2}
          .step=${.1}
          @value-changed=${c=>{this.draft={...this.draft,[i]:c.detail.value},this.pushLight(e.subentry_id,t.entity_id,"bias_stops",c.detail.value),c.detail.final&&this.clearDraft(i)}}
        ></sol-slider>
        <span class="sv ${o!==0?"moved":""}">${O(o).replace(" stops","").replace(" stop","")}</span>
      </div>
      <sol-number
        clearable
        .value=${t.clamp_min}
        .min=${0}
        .max=${254}
        .width=${54}
        @value-changed=${c=>this.pushLight(e.subentry_id,t.entity_id,"clamp_min",c.detail.value)}
      ></sol-number>
      <sol-number
        clearable
        .value=${t.clamp_max}
        .min=${1}
        .max=${254}
        .width=${54}
        @value-changed=${c=>this.pushLight(e.subentry_id,t.entity_id,"clamp_max",c.detail.value)}
      ></sol-number>
    </div>`}renderGrouped(e){if(!e.zones.length)return e.lights.map(a=>this.renderLight(e,a));let t=e.lights.filter(a=>!a.zone_id);return d`
      ${e.zones.map(a=>{let i=e.lights.filter(o=>o.zone_id===a.zone_id);return i.length?d`
          <div class="zone-head">
            <span class="eyebrow">${a.name}</span>
            ${a.clear===null?p:d`<span class="pill ${a.clear?"off":"on"}"
                  >${a.clear?"clear":"occupied"}</span
                >`}
            <span class="grow"></span>
            <sol-slider
              small
              .value=${a.bias_stops}
              .min=${-2}
              .max=${2}
              .step=${.1}
              @value-changed=${o=>this.pushZone(e,a,{bias_stops:o.detail.value})}
            ></sol-slider>
            <span class="zv tab-num">${O(a.bias_stops)}</span>
            ${a.presence.length?d`<sol-number
                  .value=${a.diminish_pct}
                  .min=${0}
                  .max=${100}
                  .width=${48}
                  suffix="% when clear"
                  @value-changed=${o=>this.pushZone(e,a,{diminish_pct:o.detail.value})}
                ></sol-number>`:p}
          </div>
          ${i.map(o=>this.renderLight(e,o))}
        `:p})}
      ${t.length?d`<div class="zone-head warnrow">
              <span class="eyebrow">Not in a zone</span>
              <sol-help
                flip
                .text=${"These lights take the area's own zone bias. Assign them to a zone if you want them to follow one."}
              ></sol-help>
            </div>
            ${t.map(a=>this.renderLight(e,a))}`:p}
    `}async pushZone(e,t,a){let i=e.zones.map(o=>o.zone_id===t.zone_id?{...o,...a}:o);await mt(this.hass,e.subentry_id,i)}renderRoom(e){let t=this.snap.house.gamma??2.39,a=(e.level??0)>0,i=this.open.has(e.subentry_id),o=e.manual.active,l=this.key(e.subentry_id,"bias_stops"),c=this.local(l,Number(e.settings.bias_stops??0)),g=this.key(e.subentry_id,"zone_bias_stops"),u=this.local(g,Number(e.settings.zone_bias_stops??0)),r=this.key(e.subentry_id,"ambience_level"),f=this.local(r,Number(e.settings.ambience_level??0));return d`<div class="room">
      <div class="room-head">
        <ha-icon class="room-icon ${a?"lit":""}" icon=${this.roomIcon(e.name)}></ha-icon>
        <div class="title">
          <div style="display:flex;align-items:center;gap:4px">
            <h3>${e.name}</h3>
            <ha-icon
              class="motion-icon ${e.occupied?"motion-active":""}"
              icon="mdi:motion-sensor"
              title="${e.occupied?"Occupied":"Clear"}"
            ></ha-icon>
          </div>
          <div class="sub">
            ${e.lights.length} light${e.lights.length===1?"":"s"} ·
            ${e.occupied?"occupied":"clear"} ·
            ${e.ambience_open?"dark":"daylight"}
          </div>
        </div>
        <sol-segmented
          .options=${[{value:"auto",label:"Auto"},{value:"manual",label:"Manual",tone:"amber"}]}
          .value=${o?"manual":"auto"}
          @segment-changed=${b=>Me(this.hass,e.subentry_id,b.detail.value==="manual"?"manual":"auto")}
        ></sol-segmented>
      </div>

      ${o?d`<div class="manual-block">
            <div class="top">
              <ha-icon icon="mdi:clock-outline"></ha-icon>
              <span
                >${e.manual.switch?"Held until you switch back":`Auto resumes in ${vt(e.manual.remaining_s)}`}</span
              >
              <button class="ghost" @click=${()=>Me(this.hass,e.subentry_id,"auto")}>
                Resume now
              </button>
            </div>
            <div class="bias-row">
              <sol-slider
                tone="amber"
                noReset
                .value=${Math.max(...e.lights.map(b=>b.current_level),0)}
                .min=${0}
                .max=${254}
                .step=${1}
                @value-changed=${b=>Me(this.hass,e.subentry_id,"level",Math.round(b.detail.value))}
              ></sol-slider>
              <div class="readout tab-num">
                ${Math.max(...e.lights.map(b=>b.current_level),0)} ·
                ${F(Math.max(...e.lights.map(b=>b.current_level),0),t)} %
              </div>
            </div>
            <div class="note">A physical switch left on holds this room indefinitely.</div>
          </div>`:d`<div class="status">
            <div>
              <span class="eyebrow">Demand</span>
              <span class="v tab-num"
                >${e.demand===null?"\u2014":`${Math.round(e.demand*100)} %`}</span
              >
            </div>
            <div>
              <span class="eyebrow">Output</span>
              <span class="v tab-num"
                >${e.level===null?"\u2014":`${F(e.level,t)} %`}</span
              >
            </div>
            <div>
              <span class="eyebrow">Mode</span>
              <span class="v">${e.mode??"\u2014"}</span>
            </div>
          </div>`}

      <div class="bias-row">
        <span class="lab">Room bias <sol-help .text=${P.roomBias}></sol-help></span>
        <sol-slider
          .value=${c}
          .min=${-2}
          .max=${2}
          .step=${.1}
          @value-changed=${b=>this.pushRoom(e.subentry_id,"bias_stops",b.detail.value,b.detail.final)}
        ></sol-slider>
        <span class="readout tab-num">${O(c)}</span>
        <span class="cons">${Ie(e.level,t)}</span>
      </div>

      <!-- Tape-measure Ambience Slider -->
      <div class="bias-row">
        <span class="lab">Ambience <sol-help .text=${P.ambience}></sol-help></span>
        <div class="tape-measure">
          <sol-slider
            tone="cyan"
            noReset
            .value=${f}
            .min=${0}
            .max=${254}
            .step=${1}
            @value-changed=${b=>this.pushRoom(e.subentry_id,"ambience_level",b.detail.value,b.detail.final)}
          ></sol-slider>
          <div class="tape-ticks">
            <span>0%</span>
            <span>10%</span>
            <span>25%</span>
            <span>50%</span>
            <span>75%</span>
            <span>100%</span>
          </div>
        </div>
        <span class="readout tab-num">
          ${f===0?"Follows house":`L${f} (${F(f,t)} %)`}
        </span>
      </div>

      ${e.zones&&e.zones.length>1?d`<div class="bias-row">
            <span class="lab">Zone bias <sol-help .text=${P.zoneBias}></sol-help></span>
            <sol-slider
              .value=${u}
              .min=${-1}
              .max=${1}
              .step=${.1}
              @value-changed=${b=>this.pushRoom(e.subentry_id,"zone_bias_stops",b.detail.value,b.detail.final)}
            ></sol-slider>
            <span class="readout tab-num">${O(u)}</span>
          </div>`:p}

      ${e.has_near&&!e.zones.some(b=>b.presence.length)?d`<div class="bias-row">
            <span class="lab">Diminish <sol-help .text=${P.diminish}></sol-help></span>
            <span class="pill ${e.near_clear?"off":"on"}"
              >near ${e.near_clear?"clear":"occupied"}</span
            >
            <span class="grow"></span>
            <sol-number
              .value=${Number(e.settings.diminish_pct??0)}
              .min=${0}
              .max=${100}
              suffix="% when clear"
              .width=${52}
              @value-changed=${b=>this.pushRoom(e.subentry_id,"diminish_pct",b.detail.value)}
            ></sol-number>
          </div>`:p}

      <button
        class="disclose"
        aria-expanded=${i}
        @click=${()=>{let b=new Set(this.open);b.has(e.subentry_id)?b.delete(e.subentry_id):b.add(e.subentry_id),this.open=b}}
      >
        <ha-icon class=${i?"open":""} icon="mdi:chevron-right"></ha-icon>
        Per-light adjustments (${e.lights.length}${e.zones.length>1?` in ${e.zones.length} zones`:""})
      </button>

      ${i?d`<div class="lights">
            <div class="lhead">
              <span class="eyebrow">Light</span>
              <span class="eyebrow">Adjustment <sol-help .text=${P.perLight}></sol-help></span>
              <span class="eyebrow">Min <sol-help flip .text=${P.min}></sol-help></span>
              <span class="eyebrow">Max <sol-help flip .text=${P.max}></sol-help></span>
            </div>
            ${this.renderGrouped(e)}
          </div>`:p}

      <div class="footer">
        <span class="grow"></span>
        <span class="lab">
          <label style="display:flex;align-items:center;gap:6px;cursor:pointer">
            <input
              type="checkbox"
              .checked=${!!e.settings.night_off}
              @change=${b=>this.pushRoom(e.subentry_id,"night_off",b.target.checked)}
            />
            Off when asleep
          </label>
          <sol-help flip .text=${P.nightOff}></sol-help>
        </span>
      </div>
    </div>`}render(){return d`
      ${this.renderStrip()} ${this.renderHouseBias()}
      <div class="grid">${this.snap.rooms.map(e=>this.renderRoom(e))}</div>
    `}};H.styles=[k,$`
      .strip {
        background: var(--sol-card);
        border-radius: var(--sol-r-card);
        padding: 10px 14px;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        font-size: 12.5px;
        color: var(--sol-text-3);
        box-shadow: var(--sol-shadow);
      }
      .strip > div {
        display: flex;
        align-items: center;
        gap: 6px;
        padding: 0 14px;
        border-left: 1px solid rgba(255, 255, 255, 0.1);
      }
      .strip > div:first-child {
        border-left: none;
        padding-left: 0;
      }
      .strip ha-icon {
        --mdc-icon-size: 17px;
      }
      .strip b {
        color: var(--sol-text);
        font-weight: 500;
        font-variant-numeric: tabular-nums;
      }

      .house {
        background: var(--sol-card);
        border-radius: var(--sol-r-card);
        padding: 12px 16px;
        margin-top: 14px;
        display: flex;
        align-items: center;
        gap: 12px;
        box-shadow: var(--sol-shadow);
      }
      .house .name {
        display: flex;
        align-items: center;
        gap: 7px;
        font-size: 13.5px;
        font-weight: 500;
        flex: 0 0 auto;
      }
      .house ha-icon {
        --mdc-icon-size: 19px;
        color: var(--sol-cyan);
      }
      .readout {
        font-size: 13px;
        font-variant-numeric: tabular-nums;
        flex: 0 0 82px;
        text-align: right;
      }
      .cons {
        font-size: 11.5px;
        color: var(--sol-text-4);
        flex: 0 0 auto;
        white-space: nowrap;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(430px, 1fr));
        gap: 14px;
        align-items: start;
        margin-top: 14px;
      }
      @media (max-width: 500px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }

      .room {
        background: var(--sol-card);
        border-radius: var(--sol-r-card);
        padding: 14px 16px 12px;
        box-shadow: var(--sol-shadow);
      }
      .room-head {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .room-head .room-icon {
        --mdc-icon-size: 26px;
        color: var(--sol-faint);
        transition: color 0.15s ease;
      }
      .room-head .room-icon.lit {
        color: var(--sol-amber);
      }
      .room-head .motion-icon {
        --mdc-icon-size: 14px;
        color: var(--sol-faint);
        margin-left: -4px;
        margin-right: 4px;
        transition: color 0.15s ease;
      }
      .room-head .motion-icon.motion-active {
        color: var(--sol-amber, #ffb74d);
      }
      .room-head .title {
        flex: 1;
        min-width: 0;
      }
      .room-head h3 {
        margin: 0;
        font-size: 16px;
        font-weight: 500;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .room-head .sub {
        font-size: 11.5px;
        color: var(--sol-text-4);
      }

      .status {
        display: flex;
        gap: 24px;
        background: var(--sol-block);
        border-radius: var(--sol-r-block);
        padding: 9px 14px;
        margin-top: 11px;
      }
      .status div {
        display: flex;
        flex-direction: column;
        gap: 2px;
      }
      .status .v {
        font-size: 13px;
        font-variant-numeric: tabular-nums;
      }

      .manual-block {
        background: var(--sol-amber-surface);
        border-radius: var(--sol-r-block);
        padding: 10px 12px;
        margin-top: 11px;
      }
      .manual-block .top {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 12.5px;
        color: var(--sol-amber-light);
      }
      .manual-block ha-icon {
        --mdc-icon-size: 16px;
      }
      button.ghost {
        all: unset;
        cursor: pointer;
        font-size: 11.5px;
        padding: 4px 10px;
        border-radius: var(--sol-r-control);
        border: 1px solid rgba(255, 183, 77, 0.4);
        color: var(--sol-amber);
        white-space: nowrap;
        margin-left: auto;
      }
      .manual-block .note {
        font-size: 11px;
        color: var(--sol-text-4);
        margin-top: 6px;
      }

      .bias-row {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-top: 11px;
        font-size: 12.5px;
        color: var(--sol-text-2);
      }
      .bias-row .lab {
        flex: 0 0 92px;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .tape-measure {
        display: flex;
        flex-direction: column;
        flex: 1;
        min-width: 0;
        position: relative;
      }
      .tape-ticks {
        display: flex;
        justify-content: space-between;
        font-size: 9.5px;
        color: var(--sol-text-4);
        padding: 2px 2px 0;
        font-variant-numeric: tabular-nums;
      }

      .disclose {
        all: unset;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 6px;
        margin-top: 12px;
        font-size: 12px;
        color: var(--sol-text-3);
        border-radius: var(--sol-r-control);
      }
      .disclose ha-icon {
        --mdc-icon-size: 16px;
        transition: transform 0.15s ease;
      }
      .disclose ha-icon.open {
        transform: rotate(90deg);
      }

      .lights {
        margin-top: 8px;
      }
      .zone-head {
        display: flex;
        align-items: center;
        gap: 8px;
        background: var(--sol-block);
        border-radius: var(--sol-r-control);
        padding: 5px 9px;
        margin-top: 8px;
      }
      .zone-head sol-slider {
        max-width: 120px;
      }
      .zone-head .zv {
        font-size: 11px;
        color: var(--sol-text-4);
        min-width: 46px;
        text-align: right;
      }
      .zone-head.warnrow {
        background: var(--sol-amber-surface);
      }
      .lrow {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 152px 60px 60px;
        gap: 8px;
        align-items: center;
        padding: 8px 0;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
      }
      .lrow .lname {
        min-width: 0;
      }
      .lrow .lname .n {
        font-size: 12.5px;
        color: var(--sol-text-2);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .lrow .lname .s {
        font-size: 11px;
        color: var(--sol-text-4);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .lrow .lname .s .hw {
        color: var(--sol-amber);
      }
      .adj {
        display: flex;
        align-items: center;
        gap: 6px;
      }
      .adj .sv {
        font-size: 11.5px;
        font-variant-numeric: tabular-nums;
        color: var(--sol-text-4);
        flex: 0 0 38px;
        text-align: right;
      }
      .adj .sv.moved {
        color: var(--sol-text);
      }
      .lhead {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 152px 60px 60px;
        gap: 8px;
        align-items: center;
        padding-bottom: 4px;
      }
      .lhead .eyebrow {
        display: flex;
        align-items: center;
        gap: 4px;
      }
      .footer {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-top: 10px;
        padding-top: 9px;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        font-size: 12.5px;
        color: var(--sol-text-2);
      }
      .footer .lab {
        display: flex;
        align-items: center;
        gap: 5px;
      }
      .grow {
        flex: 1;
      }
      .pill {
        font-size: 10.5px;
        letter-spacing: 0.4px;
        text-transform: uppercase;
        padding: 2px 8px;
        border-radius: 10px;
        background: var(--sol-control);
        color: var(--sol-text-3);
      }
      .pill.on {
        background: var(--sol-cyan-tint);
        color: var(--sol-cyan);
      }
      .pill.off {
        color: var(--sol-faint);
      }
    `],h([m({attribute:!1})],H.prototype,"hass",2),h([m({attribute:!1})],H.prototype,"snap",2),h([M()],H.prototype,"open",2),h([M()],H.prototype,"draft",2),H=h([w("sol-tab-home")],H);var Q=Math.PI/180,N=[[75,11e4],[60,9e4],[45,65e3],[30,4e4],[20,22e3],[12,11e3],[8,6e3],[5,3200],[2,1200],[0,420],[-2,130],[-4,42],[-6,11],[-9,1.6],[-12,.35],[-18,.002]],$t=n=>23.44*Q*Math.sin(2*Math.PI*(284+n)/365),Xt=(n,s,e)=>{let t=$t(n),a=e*Q,i=(s-12)*15*Q;return Math.asin(Math.sin(a)*Math.sin(t)+Math.cos(a)*Math.cos(t)*Math.cos(i))/Q},Yt=(n,s,e)=>{let t=$t(n),a=e*Q,i=(Math.sin(s*Q)-Math.sin(a)*Math.sin(t))/(Math.cos(a)*Math.cos(t));return i>=1||i<=-1?null:12+Math.acos(i)/Q/15};var yt=n=>{let s=2*Math.PI*(n-81)/364;return 9.87*Math.sin(2*s)-7.53*Math.cos(s)-1.5*Math.sin(s)},_t=(n,s)=>{try{s||(s=Intl.DateTimeFormat().resolvedOptions().timeZone);let e=new Intl.DateTimeFormat("en-GB",{timeZone:s,hour12:!1,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit"}).formatToParts(new Date(n)),t=i=>Number(e.find(o=>o.type===i)?.value??0);return(Date.UTC(t("year"),t("month")-1,t("day"),t("hour")%24,t("minute"),t("second"))-n)/36e5}catch{return 0}},Ke=(n,s,e)=>{let t=Yt(n,s,e.lat);if(t===null)return null;let i=12-e.lon/15-yt(n)/60+(t-12),l=Date.UTC(e.year,0,1)+(n-1)*864e5+i*36e5;return(i+_t(l,e.timeZone)+24)%24};var wt=n=>{if(n>=N[0][0])return N[0][1];if(n<=N[N.length-1][0])return N[N.length-1][1];for(let s=0;s<N.length-1;s++){let[e,t]=N[s],[a,i]=N[s+1];if(n<=e&&n>=a){let o=(n-a)/(e-a);return Math.exp(Math.log(i)+o*(Math.log(t)-Math.log(i)))}}return .002},kt=(n,s,e)=>{let a=Date.UTC(e.year,0,1)+(n-1)*864e5+s*36e5,i=_t(a,e.timeZone),o=(s-i+24)%24,l=12-e.lon/15-yt(n)/60,c=12+(o-l);return Xt(n,c,e.lat)};var qe={ambience_start_lux:"Falling edge (dark threshold). At or below this outdoor lux level, ambience glow is permitted and minimum cutoff drops out. Does NOT decide whether normal lighting runs \u2014 demand and occupancy do that.",ambience_stop_lux:"Rising edge (bright threshold). At or above this level, ambience glow turns off. The gap between start and stop creates deliberate hysteresis so boundaries never flicker.",ambience_debounce_falling_s:"Extra delay before reading dark. Leave at 0 unless needed: mmWave sensors already enforce 15s in hardware.",ambience_debounce_rising_s:"Extra delay before reading bright again. Leave at 0 to avoid adding unnecessary lag.",lux_full:"Outdoor lux at or below which rooms want 100% lighting. The bottom of the demand curve.",lux_window:"How far above the full point demand fades to 0%. (lux_full + lux_window) is the outdoor level at which normal daytime lighting turns off completely.",min_cutoff:"Daylight cutoff. In daytime, demand below this turns lights off rather than producing an invisible faint glow. Ignored after dark (when low levels are intentional).",night_level:"Fixed low level (0-254) while night mode is latched. Predictable and glare-free when moving around half-asleep.",night_release_lux:"Night mode ends automatically when outdoor lux rises to this level. Getting out of bed does not end it, keeping late-night trips dim.",alarm_lead_minutes:"Night mode also releases this many minutes before your next scheduled alarm.",ambience_level:"Default resting glow level across the house instead of turning completely off once dark and awake. Set to 0 to disable house-wide.",ambience_ignores_occupancy:"On: the ambience glow stays on in empty rooms after dark. Off: ambience requires room occupancy.",rate_limit_step:"Maximum change allowed in one tick when tracking daylight. Prevents bulbs from noticeably chasing passing cloud wobbles.",dead_zone:"Smallest change required to write to bulbs. Suppresses minor noise to keep Zigbee mesh traffic low.",update_interval_min_s:"Recalculation interval during rapid outdoor lux swings / volatile weather (10s).",update_interval_home_s:"Recalculation interval when someone is home (60s). Keeps lighting smooth and responsive to room occupancy while avoiding mesh congestion.",update_interval_max_s:"Recalculation interval when the house is empty/unoccupied (600s / 10m). Minimizes mesh traffic and coordinator cycles when no one is there.",lux_volatility_lx:"Outdoor lux variation threshold across recent samples that triggers the fast update interval.",transition_on_s:"Fade duration (seconds) when turning on from off.",transition_off_s:"Fade duration (seconds) when turning off.",transition_mode_s:"Fade duration (seconds) for mode changes and normal daylight tracking.",transition_setting_s:"Fade duration (seconds) while dragging a slider on this panel. Kept short for instant responsiveness.",ramp_onset_minutes:"Minutes before dusk when the evening ramp begins shifting the demand curve.",evening_axis_hour:"Fixed reference clock hour for evening colour temperature and ramp transitions.",morning_release_hour:"Clock hour when the evening ramp releases and the standard daytime curve takes back over.",sunrise_fade_enabled:"Gradual wake-up fade in the bedroom leading up to your alarm.",sunrise_fade_minutes:"Duration of the virtual sunrise wake-up fade before alarm.",bedtime_dwell_enabled:"Auto wind-down dimming in the bedroom before sleep.",bedtime_dwell_hour:"Clock hour after which an occupied bedroom dims to the wind-down level.",bedtime_dwell_level:"Target light level (0-254) during bedtime wind-down.",lux_history_samples:"Number of historical lux samples kept to detect cloud volatility.",demand_floor_level:"Lowest non-zero command level during active demand.",gamma:"Display only. Converts a raw command level (0-254) into perceived human brightness (Stevens' power law curve) for UI readouts (e.g. 'level 51 = 2% light'). It is strictly out of the bulb command path. Set to 1.0 for linear power or 2.39 for calibrated human eye perception."},U=class extends x{constructor(){super(...arguments);this.draft={};this.placed=new Set}schema(e){return this.placed.add(e),this.snap.house_schema.find(t=>t.key===e)}value(e){return this.draft[e]??this.snap.house[e]??0}async push(e,t,a=!0){this.draft={...this.draft,[e]:t},await V(this.hass,{[e]:t}),a&&window.setTimeout(()=>{let i={...this.draft};delete i[e],this.draft=i},700)}row(e,t){let a=this.schema(e);if(!a)return p;let i=this.value(e);return d`<sol-row .label=${a.name} .help=${qe[e]??""}>
      <sol-slider
        noReset
        .value=${i}
        .min=${a.min}
        .max=${a.max}
        .step=${a.step}
        .neutral=${a.default}
        @value-changed=${o=>this.push(e,o.detail.value,o.detail.final)}
      ></sol-slider>
      <span slot="value"
        >${t?t(i):`${v(i,a.step<1?2:0)}${a.unit?` ${a.unit}`:""}`}</span
      >
    </sol-row>`}numberRow(e){let t=this.schema(e);return t?d`<sol-row .label=${t.name} .help=${qe[e]??""}>
      <span></span>
      <sol-number
        slot="value"
        .value=${this.value(e)}
        .min=${t.min}
        .max=${t.max}
        .step=${t.step}
        .width=${64}
        suffix=${t.unit??""}
        @value-changed=${a=>this.push(e,a.detail.value)}
      ></sol-number>
    </sol-row>`:p}demandChart(){let e=Math.max(this.value("lux_full"),.1),t=e+Math.max(this.value("lux_window"),1),a=this.value("ambience_start_lux"),i=[];for(let l=0;l<=160;l++){let c=Math.exp(Math.log(.1)+l/160*(Math.log(t*1.2)-Math.log(.1))),g=c<=e?1:c>=t?0:1-Math.log(c/e)/Math.log(t/e);i.push([Math.log10(c),g])}let o=[];for(let l=-1;l<=Math.ceil(Math.log10(t*1.2));l++)o.push({value:l,label:l<0?"0.1":v(Math.pow(10,l))});return d`<sol-chart
      .series=${[{points:i,colour:"var(--sol-series-start)",width:2.6}]}
      .refLines=${[{x:Math.log10(Math.max(a,.1)),label:`dark ${v(a)} lx`,colour:"rgba(240,98,146,.55)",textColour:"var(--sol-series-full)"}]}
      .xTicks=${o}
      .yTicks=${[0,.25,.5,.75,1].map(l=>({value:l,label:`${l*100} %`}))}
      .xDomain=${[-1,Math.ceil(Math.log10(t*1.2))]}
      .yDomain=${[0,1]}
      .shade=${[-1,Math.log10(Math.max(a,.1))]}
      .shadeLabel=${"dark \u2014 cutoff drops out, glow allowed"}
      xTitle="outdoor lux"
      .facts=${[{label:"Full at",value:`${v(e,1)} lx`},{label:"Out at",value:`${v(t)} lx`},{label:"Counts as dark",value:`${v(a)} lx`},{label:"Bright again",value:`${v(this.value("ambience_stop_lux"))} lx`},{label:"Now",value:v(this.snap.world.lux,1)+" lx"}]}
    ></sol-chart>`}dayResponseChart(){let e=this.snap.world,t={lat:e.latitude,lon:e.longitude,timeZone:e.time_zone||Intl.DateTimeFormat().resolvedOptions().timeZone,year:e.year||new Date().getFullYear()},a=e.day_of_year||1,i=Math.max(this.value("lux_full"),.1),o=i+Math.max(this.value("lux_window"),1),l=this.value("ambience_start_lux"),c=this.value("min_cutoff"),g=this.value("ambience_level"),u=this.snap.house.gamma??2.39,r=[],f=[],b=[];for(let C=0;C<=1440;C+=10){let ve=C/60,Re=kt(a,ve,t),fe=wt(Re),Tt=Math.max(0,Math.min(1,Re>0?Re/60:0));r.push([ve,Tt]);let Ze=fe<=i?1:fe>=o?0:1-Math.log(fe/i)/Math.log(o/i);f.push([ve,Ze]);let ne=Math.round(Ze*254),Ve=fe<=l;ne<c&&!Ve&&(ne=0),ne===0&&Ve&&g>0&&(ne=g);let Rt=F(ne,u)/100;b.push([ve,Rt])}let R=Ke(a,-.833,t),j=Ke(a,-6,t),At=this.value("ramp_onset_minutes")||90,ie=j!==null?(j-At/60+24)%24:null,Mt=[0,4,8,12,16,20,24].map(C=>({value:C,label:y(C%24)})),ge=[];R!==null&&ge.push({x:R,label:`Sunset ${y(R)}`,colour:"var(--sol-series-sunset)",textColour:"var(--sol-series-sunset)"}),j!==null&&ge.push({x:j,label:`Civil Dusk ${y(j)}`,colour:"var(--sol-series-dusk)",textColour:"var(--sol-series-dusk)"}),ie!==null&&ge.push({x:ie,label:`Ramp Onset ${y(ie)}`,colour:"var(--sol-series-start)",textColour:"var(--sol-series-start)"});let Et=[{points:r,colour:"rgba(255, 235, 59, 0.4)",width:1.5,dashed:!0},{points:f,colour:"var(--sol-series-full)",width:1.8},{points:b,colour:"var(--sol-series-start)",width:2.6}],Te=e.lux,We=Te<=i?1:Te>=o?0:1-Math.log(Te/i)/Math.log(o/i);return d`<sol-chart
      .series=${Et}
      .refLines=${ge}
      .xTicks=${Mt}
      .yTicks=${[0,.25,.5,.75,1].map(C=>({value:C,label:`${C*100} %`}))}
      .xDomain=${[0,24]}
      .yDomain=${[0,1]}
      .marker=${{x:e.clock_hour,y:We}}
      xTitle="time of day"
      .facts=${[{label:"Now",value:y(e.clock_hour)},{label:"Current Demand",value:`${Math.round(We*100)} %`},{label:"Sunset",value:R!==null?y(R):"\u2014"},{label:"Civil Dusk",value:j!==null?y(j):"\u2014"},{label:"Ramp Onset",value:ie!==null?y(ie):"\u2014"}]}
    ></sol-chart>`}renderRamp(){let e=this.snap.ramp.slice();return d`
      <div class="ramp" style="margin-bottom:6px">
        <span class="eyebrow">Time</span>
        <span class="eyebrow">Stops bias</span>
        <span></span>
      </div>
      ${e.map((t,a)=>d`
          <div class="ramp" style="margin-bottom:8px">
            <input
              type="time"
              .value=${y(t.hour)}
              @change=${i=>this.updateRampPoint(a,{...t,hour:gt(i.target.value)})}
            />
            <sol-slider
              .value=${t.stops}
              .min=${-2}
              .max=${2}
              .step=${.1}
              @value-changed=${i=>this.updateRampPoint(a,{...t,stops:i.detail.value},i.detail.final)}
            ></sol-slider>
            <button
              class="icon"
              title="Delete this point"
              ?disabled=${e.length<=1}
              @click=${()=>this.deleteRampPoint(a)}
            >
              <ha-icon icon="mdi:close"></ha-icon>
            </button>
          </div>
        `)}
      <button class="add" @click=${()=>this.addRampPoint()}>
        <ha-icon icon="mdi:plus"></ha-icon>
        Add ramp point
      </button>
    `}async updateRampPoint(e,t,a=!0){let i=this.snap.ramp.slice();i[e]=t,a&&await Ae(this.hass,i)}async deleteRampPoint(e){let t=this.snap.ramp.slice();t.splice(e,1),await Ae(this.hass,t)}async addRampPoint(){let e=this.snap.ramp.slice(),t=e[e.length-1],a=t?(t.hour+1)%24:21;e.push({hour:a,stops:t?t.stops-.5:-1}),await Ae(this.hass,e)}rampChart(){let e=[],t=this.snap.ramp.slice().sort((i,o)=>i.hour-o.hour);if(t.length===0)return p;for(let i=0;i<=1440;i+=15){let o=i/60,l=t[0].stops;for(let c of t)o>=c.hour&&(l=c.stops);e.push([o,l])}let a=[0,4,8,12,16,20,24].map(i=>({value:i,label:y(i%24)}));return d`
      <sol-chart
        .series=${[{points:e,colour:"var(--sol-series-start)",width:2.2}]}
        .xTicks=${a}
        .yTicks=${[-2,-1,0,1,2].map(i=>({value:i,label:O(i)}))}
        .xDomain=${[0,24]}
        .yDomain=${[-2,2]}
        xTitle="time of day"
      ></sol-chart>
      <div class="caption" style="margin-top:8px">
        Evening ramp profile across 24 hours. The evening ramp shifts room bias gradually across
        the configured phases until the morning release hour.
      </div>
    `}renderAdvanced(){let e=this.snap.house_schema.filter(t=>!this.placed.has(t.key));return e.length?d`<div class="card full">
      <div class="card-head">
        <ha-icon icon="mdi:wrench-outline"></ha-icon>
        <h2>Advanced</h2>
      </div>
      <div class="caption" style="margin-bottom:8px">
        Measured constants and plumbing. They have sensible defaults and you should not
        need them — but nothing in this engine is hardcoded, so they are here rather than
        buried in the source.
      </div>
      <div class="adv-grid">
        ${e.map(t=>this.numberRow(t.key))}
      </div>
    </div>`:p}render(){this.placed=new Set;let e=this.value("ambience_start_lux"),t=this.value("ambience_stop_lux"),i=this.value("lux_full")+this.value("lux_window"),o=this.value("ambience_debounce_falling_s")>0||this.value("ambience_debounce_rising_s")>0;return d`<div class="grid">
      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:chart-bell-curve-cumulative"></ha-icon>
          <h2>Demand curve</h2>
        </div>
        ${this.row("lux_full")} ${this.row("lux_window")} ${this.row("min_cutoff")}
        <div class="caption">
          Lights out at ${v(i)} lx — full point plus window.
        </div>
        <div class="sub">${this.demandChart()}</div>
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:theme-light-dark"></ha-icon>
          <h2>Ambience — the evening glow</h2>
        </div>
        <div class="caption" style="margin-bottom:6px">
          <b>These thresholds do not decide whether normal lighting runs.</b> That is the demand
          curve above, plus occupancy. These two numbers say when it is dark enough for the resting
          glow — and below them the minimum cutoff drops out so lights can dim to low night levels.
        </div>
        ${this.row("ambience_start_lux")} ${this.row("ambience_stop_lux")}
        <div class="caption">
          Hysteresis gap: ${v(Math.max(0,t-e))} lx — prevents threshold flickering.
        </div>
        <div class="sub">
          ${this.row("ambience_level")}
          <sol-row
            .label=${"Ambience in empty rooms"}
            .help=${qe.ambience_ignores_occupancy}
          >
            <label class="toggle">
              <input
                type="checkbox"
                .checked=${this.value("ambience_ignores_occupancy")>=1}
                @change=${l=>this.push("ambience_ignores_occupancy",l.target.checked?1:0)}
              />
              <span
                >${this.value("ambience_ignores_occupancy")>=1?"glow stays in empty rooms":"needs occupancy"}</span
              >
            </label>
          </sol-row>
        </div>
        <div class="sub">
          <div class="eyebrow" style="margin-bottom:4px">Debounce (seconds)</div>
          <div class="caption">
            Leave these at 0 unless needed. The mmWave sensors already enforce hardware delay.
          </div>
          ${this.row("ambience_debounce_falling_s")} ${this.row("ambience_debounce_rising_s")}
          ${o?d`<div class="warn">
                <ha-icon icon="mdi:alert-outline"></ha-icon>
                <span>A debounce is set. Every second here stacks on top of hardware delays.</span>
              </div>`:p}
        </div>
      </div>

      <div class="card full">
        <div class="card-head">
          <ha-icon icon="mdi:weather-sunset"></ha-icon>
          <h2>24-hour daylight &amp; adaptive response</h2>
        </div>
        ${this.dayResponseChart()}
        <div class="caption" style="margin-top:10px">
          Simulates today's solar daylight cycle (yellow dashed), the resulting lighting demand (blue),
          and the projected Solace output level (solid line) across the 24 hours of today for your latitude.
        </div>
      </div>

      <div class="card full">
        <div class="card-head">
          <ha-icon icon="mdi:ray-start-arrow"></ha-icon>
          <h2>Evening ramp</h2>
        </div>
        <div class="caption" style="margin-bottom:10px">
          An ordered list of points, not two fixed phases — add as many as you want.
        </div>
        ${this.renderRamp()}
        <div class="sub">
          ${this.row("ramp_onset_minutes")}
          ${this.row("evening_axis_hour",l=>y(l))}
          ${this.row("morning_release_hour",l=>y(l))}
        </div>
        <div class="sub">${this.rampChart()}</div>
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:weather-night"></ha-icon>
          <h2>Night &amp; bedtime mode</h2>
        </div>
        <div class="caption" style="margin-bottom:6px">
          Night mode latches when you fall asleep and lets go when sunlight reaches release lux or before alarm.
        </div>
        ${this.row("night_level")} ${this.row("night_release_lux")} ${this.row("alarm_lead_minutes")}
        <div class="sub">
          <div class="eyebrow" style="margin-bottom:4px">Bedtime Wind-Down</div>
          ${this.numberRow("bedtime_dwell_hour")} ${this.numberRow("bedtime_dwell_level")}
        </div>
      </div>

      <div class="card">
        <div class="card-head">
          <ha-icon icon="mdi:speedometer"></ha-icon>
          <h2>Timing &amp; transitions</h2>
        </div>
        ${this.numberRow("update_interval_min_s")} ${this.numberRow("update_interval_home_s")}
        ${this.numberRow("update_interval_max_s")} ${this.numberRow("lux_volatility_lx")}
        ${this.numberRow("dead_zone")} ${this.numberRow("rate_limit_step")}
        <div class="sub">
          <div class="eyebrow" style="margin-bottom:4px">Transition speeds (seconds)</div>
          ${this.numberRow("transition_on_s")} ${this.numberRow("transition_off_s")}
          ${this.numberRow("transition_mode_s")} ${this.numberRow("transition_setting_s")}
        </div>
        <div class="sub">${this.row("gamma")}</div>
      </div>

      ${this.renderAdvanced()}
    </div>`}};U.styles=[k,$`
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(440px, 1fr));
        gap: 14px;
      }
      @media (max-width: 520px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }
      .full {
        grid-column: 1 / -1;
      }
      .sub {
        margin-top: 14px;
        padding-top: 12px;
        border-top: 1px solid var(--sol-hair);
      }
      .ramp {
        display: grid;
        grid-template-columns: 1fr 1fr auto;
        gap: 10px;
        align-items: center;
      }
      .ramp .eyebrow {
        padding-bottom: 2px;
      }
      input[type="time"] {
        font: inherit;
        font-size: 12.5px;
        color: var(--sol-text);
        background: var(--sol-control);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: var(--sol-r-control);
        padding: 6px 8px;
        width: 100%;
        box-sizing: border-box;
      }
      button.icon {
        all: unset;
        cursor: pointer;
        color: var(--sol-text-4);
        display: inline-flex;
        border-radius: var(--sol-r-control);
      }
      button.icon:hover {
        color: var(--sol-amber);
      }
      button.add {
        all: unset;
        cursor: pointer;
        margin-top: 10px;
        font-size: 12px;
        color: var(--sol-cyan);
        display: inline-flex;
        align-items: center;
        gap: 5px;
        border-radius: var(--sol-r-control);
      }
      .adv-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 8px 24px;
      }
      .toggle {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 12.5px;
        color: var(--sol-text-2);
        cursor: pointer;
      }
      .warn {
        display: flex;
        gap: 8px;
        align-items: flex-start;
        margin-top: 10px;
        padding: 9px 11px;
        background: var(--sol-amber-surface);
        border-radius: var(--sol-r-block);
        font-size: 11.5px;
        line-height: 1.5;
        color: var(--sol-amber-light);
      }
      code {
        background: var(--sol-control);
        border-radius: 4px;
        padding: 0 4px;
        font-size: 11px;
      }
      .warn ha-icon {
        --mdc-icon-size: 16px;
        flex: 0 0 auto;
        color: var(--sol-amber);
      }
    `],h([m({attribute:!1})],U.prototype,"hass",2),h([m({attribute:!1})],U.prototype,"snap",2),h([M()],U.prototype,"draft",2),U=h([w("sol-tab-lighting")],U);var Qt=[{value:"cycle_preset_levels",label:"Toggle Preset Loop (Auto \u2192 50% \u2192 80% \u2192 100%)"},{value:"toggle_auto_manual",label:"Toggle Auto / Manual"},{value:"turn_off",label:"Turn Off Room Lights"},{value:"nudge_bias_up",label:"Nudge Bias Up (+0.5 stops)"},{value:"nudge_bias_down",label:"Nudge Bias Down (-0.5 stops)"},{value:"toggle_manual",label:"Hold Manual Switch"},{value:"toggle_sleep",label:"Toggle Sleep / Night Mode"},{value:"leaving_5_min",label:"Leaving in 5 Minutes (Countdown \u2192 Away)"},{value:"none",label:"Disabled / No Action"}],B=class extends x{constructor(){super(...arguments);this.activeLayer={}}async updateRemote(e,t,a){let i=JSON.parse(JSON.stringify(this.snap.remotes||[]));if(i[e]){i[e][t]=a;try{await bt(this.hass,i)}catch(o){console.error("Failed to update remote settings",o)}}}renderButtonSelect(e,t,a,i,o,l){let c=l||(a.startsWith("hold")?"none":"cycle_preset_levels"),g=Object.entries(e).filter(([u])=>u!==a&&(u.startsWith("button_")||u.startsWith("hold_"))).map(([u,r])=>r).filter(u=>u&&u!=="none");return d`
      <div class="button-row">
        <span class="btn-label">
          <ha-icon icon=${o}></ha-icon>
          ${i}
        </span>
        <select
          @change=${u=>this.updateRemote(t,a,u.target.value)}
        >
          ${Qt.map(u=>{let r=u.value!=="none"&&g.includes(u.value)&&u.value!==c;return d`<option
              value=${u.value}
              ?selected=${c===u.value}
              ?disabled=${r}
            >
              ${u.label}${r?" (Already assigned)":""}
            </option>`})}
        </select>
      </div>
    `}render(){let e=this.snap.remotes||[];return e.length===0?d`
        <div class="card" style="padding: 24px; text-align: center; color: var(--sol-text-3);">
          No remotes configured. Add sensors to <code>options.remotes</code>.
        </div>
      `:d`
      <div class="grid">
        ${e.map((t,a)=>{let i=this.activeLayer[t.remote_id]||"press";return d`
            <div class="card remote-card">
              <div class="remote-header">
                <ha-icon icon="mdi:remote"></ha-icon>
                <span class="remote-title">${t.name}</span>
                <span class="remote-room-tag">${t.room_name||"Unassigned"}</span>
              </div>
              <div class="entity-sub">${t.action_entity||"sensor.*_action"}</div>

              <div class="layer-nav">
                <button
                  class=${i==="press"?"active":""}
                  @click=${()=>{this.activeLayer={...this.activeLayer,[t.remote_id]:"press"}}}
                >
                  <ha-icon icon="mdi:gesture-tap"></ha-icon> Single Press
                </button>
                <button
                  class=${i==="hold"?"active":""}
                  @click=${()=>{this.activeLayer={...this.activeLayer,[t.remote_id]:"hold"}}}
                >
                  <ha-icon icon="mdi:gesture-tap-hold"></ha-icon> Long Press / Hold
                </button>
              </div>

              <div class="button-list">
                ${i==="press"?d`
                      ${this.renderButtonSelect(t,a,"button_up","Top / Arrow Up","mdi:arrow-up-bold",t.button_up||t.button_on)}
                      ${this.renderButtonSelect(t,a,"button_down","Bottom / Arrow Down","mdi:arrow-down-bold",t.button_down||t.button_off)}
                      ${this.renderButtonSelect(t,a,"button_left","Left Arrow","mdi:arrow-left-bold",t.button_left)}
                      ${this.renderButtonSelect(t,a,"button_right","Right Arrow","mdi:arrow-right-bold",t.button_right)}
                    `:d`
                      ${this.renderButtonSelect(t,a,"hold_up","Top Hold","mdi:arrow-up-bold-box-outline",t.hold_up)}
                      ${this.renderButtonSelect(t,a,"hold_down","Bottom Hold","mdi:arrow-down-bold-box-outline",t.hold_down)}
                      ${this.renderButtonSelect(t,a,"hold_left","Left Hold","mdi:arrow-left-bold-box-outline",t.hold_left)}
                      ${this.renderButtonSelect(t,a,"hold_right","Right Hold","mdi:arrow-right-bold-box-outline",t.hold_right)}
                    `}
              </div>
            </div>
          `})}
      </div>
    `}};B.styles=[k,$`
      :host {
        display: block;
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 16px;
      }
      .remote-card {
        padding: 16px;
      }
      .remote-header {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 8px;
      }
      .remote-header ha-icon {
        --mdc-icon-size: 24px;
        color: var(--sol-cyan);
      }
      .remote-title {
        flex: 1;
        font-size: 16px;
        font-weight: 500;
        color: var(--sol-text-1);
      }
      .remote-room-tag {
        font-size: 11.5px;
        padding: 3px 8px;
        border-radius: 6px;
        background: var(--sol-control);
        color: var(--sol-text-3);
      }
      .entity-sub {
        font-size: 11.5px;
        color: var(--sol-text-4);
        margin-bottom: 12px;
        font-family: monospace;
      }
      .layer-nav {
        display: flex;
        gap: 8px;
        margin-bottom: 12px;
        border-bottom: 1px solid var(--sol-hair);
        padding-bottom: 8px;
      }
      .layer-nav button {
        all: unset;
        cursor: pointer;
        font-size: 12px;
        font-weight: 500;
        padding: 4px 10px;
        border-radius: 6px;
        color: var(--sol-text-3);
        background: var(--sol-control);
      }
      .layer-nav button.active {
        background: var(--sol-cyan-track);
        color: var(--sol-cyan);
      }
      .button-list {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .button-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        padding: 8px 10px;
        background: var(--sol-card-inner);
        border-radius: 8px;
      }
      .btn-label {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 13px;
        color: var(--sol-text-2);
      }
      .btn-label ha-icon {
        --mdc-icon-size: 18px;
        color: var(--sol-text-3);
      }
      select {
        background: var(--sol-control);
        color: var(--sol-text-1);
        border: 1px solid var(--sol-border, rgba(255, 255, 255, 0.1));
        border-radius: 6px;
        padding: 5px 8px;
        font-size: 12px;
        outline: none;
        cursor: pointer;
        max-width: 210px;
      }
      select:focus {
        border-color: var(--sol-cyan);
      }
      option:disabled {
        color: var(--sol-text-4);
      }
    `],h([m({attribute:!1})],B.prototype,"hass",2),h([m({attribute:!1})],B.prototype,"snap",2),h([M()],B.prototype,"activeLayer",2),B=h([w("sol-tab-remotes")],B);var es=[{id:"home",label:"Home"},{id:"lighting",label:"Lighting"},{id:"colour",label:"Colour"},{id:"remotes",label:"Remotes"}],z=class extends x{constructor(){super(...arguments);this.narrow=!1;this.tab="home";this.snap=null;this.error=null}connectedCallback(){super.connectedCallback(),this.connect()}disconnectedCallback(){super.disconnectedCallback(),this.unsub?.(),this.unsub=void 0}updated(e){e.has("hass")&&this.hass&&!this.unsub&&this.connect()}async connect(){if(!(!this.hass?.connection||this.unsub))try{this.unsub=await pt(this.hass,e=>{this.snap=e,this.error=null})}catch(e){this.error=e?.message??"Solace is not set up yet, or the integration failed to load."}}renderStatusHeader(){if(!this.snap)return p;let e=this.snap.world,t=d`<span class="status-badge badge-normal">Normal</span>`;e.away?t=d`<span class="status-badge badge-away"><ha-icon icon="mdi:airplane"></ha-icon> Away Mode</span>`:e.sunrise_progress!==null&&e.sunrise_progress!==void 0?t=d`<span class="status-badge badge-sunrise"><ha-icon icon="mdi:weather-sunset-up"></ha-icon> Sunrise Fade (${Math.round(e.sunrise_progress*100)}%)</span>`:e.night_active?t=d`<span class="status-badge badge-night"><ha-icon icon="mdi:weather-night"></ha-icon> Night Latched</span>`:e.bedtime_dwell_active&&(t=d`<span class="status-badge badge-night"><ha-icon icon="mdi:bed"></ha-icon> Bedtime Wind-Down</span>`);let a=Math.round(e.lux).toLocaleString()+" lx",i=e.lux>5e3?"Full Sun":e.lux>500?"Daylight":e.lux>50?"Twilight":"Dark";return d`
      <div class="status-banner">
        <div class="status-group">
          <span class="status-item">
            <ha-icon icon="mdi:white-balance-sunny"></ha-icon>
            <strong>${a}</strong> (${i})
          </span>
          <span class="status-item">
            <ha-icon icon="mdi:home-clock"></ha-icon>
            ${t}
          </span>
          <span class="status-item">
            <ha-icon icon="mdi:thermometer"></ha-icon>
            ${e.kelvin} K
          </span>
        </div>
        <div class="room-badges">
          ${this.snap.rooms.map(o=>{let l=(o.level??0)>0;return d`<span class="room-chip ${l?"lit":""}">
              ${o.name}: ${o.manual.active?"Manual":l?`L${o.level}`:"Off"}
            </span>`})}
        </div>
      </div>
    `}renderTab(){if(!this.snap)return p;switch(this.tab){case"lighting":return d`<sol-tab-lighting .hass=${this.hass} .snap=${this.snap}></sol-tab-lighting>`;case"colour":return d`<sol-tab-colour .hass=${this.hass} .snap=${this.snap}></sol-tab-colour>`;case"remotes":return d`<sol-tab-remotes .hass=${this.hass} .snap=${this.snap}></sol-tab-remotes>`;default:return d`<sol-tab-home .hass=${this.hass} .snap=${this.snap}></sol-tab-home>`}}render(){let e=this.snap?.world.healthy??!1;return d`
      <header>
        <div class="bar">
          <ha-icon class="brand" icon="mdi:tune"></ha-icon>
          <h1>Lighting Engine</h1>
          ${this.snap?d`<span class="pill">
                <span class="dot ${e?"":"bad"}"></span>
                ${e?"Engine running":"Engine stalled"}
              </span>`:p}
        </div>
        <nav role="tablist">
          ${es.map(t=>d`<button
              role="tab"
              aria-selected=${this.tab===t.id}
              @click=${()=>this.tab=t.id}
            >
              ${t.label}
            </button>`)}
        </nav>
        ${this.renderStatusHeader()}
      </header>
      <main>
        ${this.snap?this.renderTab():d`<div class="placeholder">
              <ha-icon icon=${this.error?"mdi:alert-circle-outline":"mdi:progress-clock"}></ha-icon>
              <p>
                ${this.error??"Waiting for the engine\u2026"}
              </p>
              ${this.error?d`<p>
                    Add the integration under <code>Settings → Devices &amp; services</code>, then
                    add a room for each zone you want Solace to drive.
                  </p>`:p}
            </div>`}
      </main>
    `}};z.styles=[k,$`
      :host {
        background: var(--sol-page);
        min-height: 100vh;
      }
      header {
        position: sticky;
        top: 0;
        z-index: 10;
        background: var(--sol-card);
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.4);
      }
      .bar {
        height: 56px;
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 0 18px;
        box-sizing: border-box;
      }
      .bar ha-icon.brand {
        --mdc-icon-size: 22px;
        color: var(--sol-text-3);
      }
      h1 {
        margin: 0;
        font-size: 19px;
        font-weight: 400;
        flex: 1;
        min-width: 0;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 7px;
        font-size: 12px;
        color: var(--sol-text-3);
        background: var(--sol-control);
        border-radius: 12px;
        padding: 5px 11px;
        white-space: nowrap;
      }
      .dot {
        width: 7px;
        height: 7px;
        border-radius: 50%;
        background: var(--sol-green);
      }
      .dot.bad {
        background: var(--error-color, #ef5350);
      }
      nav {
        display: flex;
        gap: 0;
        padding: 0 18px 12px;
      }
      nav button {
        all: unset;
        cursor: pointer;
        font-size: 13.5px;
        font-weight: 500;
        letter-spacing: 0.6px;
        text-transform: uppercase;
        color: var(--sol-text-3);
        padding: 4px 14px 8px;
        border-bottom: 2px solid transparent;
      }
      nav button[aria-selected="true"] {
        color: var(--sol-cyan);
        border-bottom-color: var(--sol-cyan);
      }
      .status-banner {
        background: var(--sol-card-inner, rgba(0, 0, 0, 0.2));
        border-bottom: 1px solid var(--sol-border, rgba(255, 255, 255, 0.08));
        padding: 10px 18px;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        font-size: 12.5px;
      }
      .status-group {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 14px;
      }
      .status-item {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: var(--sol-text-2);
      }
      .status-item ha-icon {
        --mdc-icon-size: 16px;
        color: var(--sol-cyan);
      }
      .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 5px;
        padding: 3px 8px;
        border-radius: 6px;
        font-size: 11.5px;
        font-weight: 500;
      }
      .badge-normal {
        background: rgba(76, 175, 80, 0.15);
        color: #81c784;
      }
      .badge-night {
        background: rgba(156, 39, 176, 0.15);
        color: #ce93d8;
      }
      .badge-away {
        background: rgba(239, 83, 80, 0.15);
        color: #ef5350;
      }
      .badge-sunrise {
        background: rgba(255, 152, 0, 0.15);
        color: #ffb74d;
      }
      .room-badges {
        display: flex;
        align-items: center;
        gap: 8px;
        flex-wrap: wrap;
      }
      .room-chip {
        font-size: 11.5px;
        padding: 2px 7px;
        border-radius: 4px;
        background: var(--sol-control);
        color: var(--sol-text-3);
      }
      .room-chip.lit {
        color: var(--sol-text-1);
        border: 1px solid var(--sol-cyan);
      }
      main {
        max-width: 1180px;
        margin: 0 auto;
        padding: 18px 20px 48px;
      }
      .placeholder {
        max-width: 560px;
        margin: 64px auto;
        text-align: center;
        color: var(--sol-text-3);
        font-size: 13.5px;
        line-height: 1.7;
      }
      .placeholder ha-icon {
        --mdc-icon-size: 40px;
        color: var(--sol-text-4);
      }
      code {
        background: var(--sol-control);
        border-radius: 4px;
        padding: 1px 5px;
        font-size: 12px;
      }
    `],h([m({attribute:!1})],z.prototype,"hass",2),h([m({type:Boolean})],z.prototype,"narrow",2),h([M()],z.prototype,"tab",2),h([M()],z.prototype,"snap",2),h([M()],z.prototype,"error",2),z=h([w("solace-panel")],z);export{z as SolacePanel};
